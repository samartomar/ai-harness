version: 2
updates:
  # Runtime dependency bytes ship in the package, so npm updates default to
  # `semver:patch`; re-label when the actual public surface requires another class.
  #
  # Two groups, deliberately: a single "*" group let one high-risk toolchain bump
  # (Biome's new lint rules, TypeScript's declaration-build break) block every routine
  # update riding with it. The toolchain lands on its own PR and can be triaged alone.
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - "semver:patch"
    groups:
      toolchain:
        patterns:
          - "@biomejs/biome"
          - "typescript"
          - "tsup"
      dependencies:
        patterns:
          - "*"
        exclude-patterns:
          - "@biomejs/biome"
          - "typescript"
          - "tsup"
  # Keep GitHub Actions pinned versions current (supply-chain hygiene).
  #
  # Group the CodeQL actions: `github/codeql-action/init` and `.../analyze` run together
  # in one workflow and their bundle versions must match — split PRs failed with
  # "Loaded a configuration file for version 'X', but running version 'Y'" (#368). One
  # grouped PR moves the paired steps together.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github_actions
      - "semver:none"
    groups:
      codeql:
        patterns:
          - "github/codeql-action*"
      actions:
        patterns:
          - "*"
        exclude-patterns:
          - "github/codeql-action*"
