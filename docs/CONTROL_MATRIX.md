# Control Matrix

> Status: shipped control matrix for the open-source CLI. This document maps
> public claims to implementation seams and regression tests; if a claim is not
> tied to source and tests here, do not treat it as an assurance claim.

Run the whole gate with:

```bash
npm run verify
```

## Claim to Proof

| ID | Public claim | Implementation seam | Regression proof |
| --- | --- | --- | --- |
| <a id="CM-01"></a>CM-01 | Managed project changes are dry-run first, and `--apply` is the explicit mutation boundary. | `src/internals/execute.ts`, `src/commands/run.ts`, and command specs that declare `readOnly` or emit typed actions. | `tests/internals/execute.test.ts` (`dry-run reports planned writes but writes nothing`, `runs exec actions only on apply and records the exit code`), `tests/contract/json-envelope.test.ts` (`omits the report key entirely when verification did not run (dry-run, no --verify)`). |
| <a id="CM-02"></a>CM-02 | The action model is local by default; remote mutation is not a normal plan action. Explicit signing/provenance flows are the documented carve-out. | `src/internals/plan.ts`, `src/internals/execute.ts`, `src/commands/index.ts`, `src/bundle/index.ts`, `src/evidence/build.ts`, and `src/marketplace/publish.ts`. | `tests/internals/plan-purity.test.ts` (`every command's plan(ctx) only execs read-only, allowlisted binaries`), `tests/internals/exec-locality.test.ts` (`checks every registered CommandSpec plus explicit signing scenarios`). |
| <a id="CM-03"></a>CM-03 | Every built-in command spec is covered by the command registry used by proofs and contract snapshots. | `src/commands/index.ts` exports `ALL_COMMAND_SPECS`, `GROUPED_COMMAND_SPECS`, and `ALL_COMMAND_SPEC_PATHS`. | `tests/cli.test.ts` (`keeps the canonical CommandSpec registry complete for every registered built-in spec`), `tests/contract/command-surface.test.ts` (`matches the committed command-surface fixture`), `tests/docs/readme-assets.test.ts` (`keeps the README and command docs aligned with the contract command surface`). |
| <a id="CM-04"></a>CM-04 | Writes are contained, transactional, and protected from unrelated dirty worktree loss. | `src/internals/execute.ts` path containment, backup, rollback, symlink-parent, unchanged-write, and dirty-worktree gates; `src/internals/fsxn.ts` symlink-aware file reads. | `tests/internals/execute.test.ts` (`path containment: a repo-scoped write escaping the root fails closed`, `fault injection during a transaction restores every touched path byte-for-byte`, `refuses to overwrite a dirty file the write would CHANGE (nothing written, no backup)`, `refuses repo writes through a symlinked parent even when it resolves inside the repo`, `refuses doc-file writes through a symlinked parent`, `does not claim 'Applied' when an --apply run has only unchanged writes`), `tests/internals/fsxn.test.ts` (`refuses a symlink instead of following it`). |
| <a id="CM-05"></a>CM-05 | Skill installs are approval-gated at enterprise posture, skill vet records selected-artifact evidence and curated source scope for named multi-skill sources, and pack refs fail closed on missing or mismatched pins. | `src/workspace/acquire.ts`, `src/pack/status.ts`, `src/pack/install.ts`, `src/skill/approve.ts`, `src/skill/card.ts`, `src/skill/lockfile.ts`, `src/skill/vet.ts`, `src/skill/license.ts`, and `src/trust/fetch.ts`. | `tests/workspace/acquire.test.ts` (`REFUSES at enterprise posture when a promoted skill has no committed approval (coded check)`, `does NOT let a same-named approval from another source satisfy the gate (source-bound)`, `does NOT let a same-named local approval from another local source satisfy the gate`, `phase 2 refuses duplicate physical skill dirs with the same promoted name`, `phase 2 refuses case-variant promoted names that collide on case-insensitive filesystems`), `tests/skill/license.test.ts` (`uses the selected skill folder license before falling back to the source root`, `does not use a sibling skill license for the selected skill`, `fails when neither the selected skill nor the source root has license evidence`, `prefers selected skill frontmatter over conflicting root license evidence`), `tests/skill/vet.test.ts` (`writes scoped evidence for a selected skill without scanning a hostile sibling`, `uses selected skill license evidence while excluding a flagged sibling`, `refuses scoped vet evidence for nested skill boundaries`, `refuses scoped vet evidence for nested skill boundaries with dot-prefixed names`, `refuses scoped vet evidence when multiple physical skills share the selected name`, `refuses scoped vet evidence when case variants share the selected promotion path`, `uses selected skill license evidence for an applied GitHub source`), `tests/skill/approve.test.ts` (`approves scoped evidence even when source-wide evidence is RED for another skill`, `approves scoped evidence for a skill directory named skills`, `refuses hand-edited scoped evidence whose sourceScope does not exactly match --name`, `refuses hand-edited scoped evidence with overlapping included and excluded paths`, `refuses hand-edited scoped evidence whose included path does not promote to --name`, `refuses hand-edited scoped evidence whose sourceScope check does not match`, `refuses hand-edited scoped evidence without included paths at the schema boundary`, `refuses hand-edited scoped evidence with unsafe sourceScope paths at the schema boundary`), `tests/trust/fetch.test.ts` (`materializes a contained tar symlink as a regular copy for scanning`, `fails closed when a quarantined tar symlink escapes its extraction root`, `fails closed when a quarantined tar symlink target is absent`), `tests/pack/status.test.ts` (`flags a ref whose name has no lock entry as missing-approval (pack blocked)`, `fails closed as pin-mismatch when the manifest commit disagrees with the lock`), `tests/pack/install.test.ts` (`refuses before any scan when a ref has no committed approval`). |
| <a id="CM-06"></a>CM-06 | Evidence bundles are deterministic and carry a harness provenance block. | `src/evidence/build.ts`, `src/evidence/manifest.ts`, and shared fleet-bundle layout code. | `tests/evidence/build.test.ts` (`is byte-identical across two builds over identical inputs`, `indexes every kind that exists, name-sorted, with packaged-byte hashes`, `keeps older evidence indexes without a harness block valid`). |
| <a id="CM-07"></a>CM-07 | Evidence and fleet-bundle signatures can be required in gated environments, and generated signing execs bind the expected `SHA256SUMS` digest. | `src/evidence/build.ts`, `src/bundle/index.ts`, and support code metadata for `bundle.signature`. | `tests/evidence/build.test.ts` (`emits no exec by default and a best-effort cosign exec under --sign`, `makes signing strict under enterprise posture or --require-signature`, `emits a coded verification failure when strict signing is requested without a signer`, `records a coded failure when strict signing exec fails`), `tests/bundle/bundle.test.ts` (`honors --include and emits signing as a thin cosign exec`, `keeps missing signatures optional unless --require-signature is set`, `fails strict GitHub attestation verification when --repo is missing`). |
| <a id="CM-08"></a>CM-08 | Org policy has a trusted-channel verification path and surfaces local drift/tamper signals. Claude-owned policy probes are scoped to the repo's committed target set: a repo that does not target Claude is not failed for a projection `aih policy project` would never write there; stale Claude artifacts are reported as dropped-target residue naming a repair the operator can actually perform, never a projection that emits no actions; aih's own `.aih-config.json` marker is never classified as residue; and only the committed marker may narrow a probe — a missing, malformed, empty, or partly-unrecognized marker, a `--cli` flag, `--detect`, and on-disk adapter inference all narrow nothing. The same target scope binds the GENERATION side: standalone `aih mcp` writes no Claude managed allowlist for a repo whose resolved targets exclude Claude, states why, and modifies no existing one. Dropped-target residue splits by proven ownership, because these commands are run by agents: an exact marker-proven managed-MCP pair is repairable and the finding names exactly one runnable command, `aih prune`, which subtracts only `allowManagedMcpServersOnly` + `allowedMcpServers` and clears the ownership record in that order, leaving every other key's value unchanged; everything else — no active ownership record, a drifted pair, a non-regular-file path, or `organizationPolicy` / `sandbox`, which carry no provenance — carries the distinct `org-policy.dropped-target-unowned` code plus its reason so an agent escalates instead of looping. Three deliberate exceptions keep aih from claiming operator content as its own: an unowned allowlist that still matches `.mcp.json` stays `pass` and, having nothing to compare, skips when `.mcp.json` carries no servers; and prune stays silent when no active ownership record exists, because it has nothing to subtract and nothing to revoke. The repairable verdict is decided first, before the org policy and `.mcp.json` are read at all, because its ownership depends on neither and each can fail in a way that would mask it. The projected file is never deleted; every ownership decision reads it as a regular file with no symlink follow, so a symlink (including a dangling one), a directory, or a path reached through a symlinked parent is classified `not a readable regular file`, no subtraction is planned, and the claim is revoked instead — the symlinked-parent case matters because the executor refuses those, so a repairable verdict there would name a command guaranteed to refuse. The executor independently refuses to write through a symlink at commit. `aih uninstall` subtracts marker-proven content before removing the marker that is its only evidence. When the context directory is also a registered live CLI configuration directory, uninstall leaves that co-owned tree intact, reports both generated and operator-owned content, and subtracts only separately proven managed content. | `src/org-policy/validate.ts`, `src/org-policy/drift.ts`, `src/mcp/allowlist.ts`, `src/mcp/managed-projection.ts`, `src/mcp/index.ts`, `src/prune/detect.ts`, `src/prune/index.ts`, `src/uninstall/index.ts`, `src/internals/cli-registry.ts`, `src/doctor.ts`, and `src/report/local.ts`. | `tests/org-policy/validate.test.ts` (`passes when the active policy matches a pinned sha256`, `fails closed when the pinned sha256 does not match`, `passes when the active policy matches a fleet-bundle policy copy`), `tests/org-policy/org-policy.test.ts` (`flags AIH_ORG_POLICY env overrides prominently at enterprise posture`, `flags working-tree policy drift from HEAD`, `emits a report digest when policy integrity has a visible signal`, `reports dropped-target residue when an untargeted artifact is still on disk`, `does not prescribe policy projection for an untargeted managed allowlist`, `names aih prune for a marker-proven dropped-target residue`, `uses a distinct code when the dropped-target residue is not repairable`), `tests/doctor.test.ts` (`does not fail a Kiro-only repo for a missing Claude projection`, `does not prescribe policy projection for a Kiro-only repo's stale allowlist`, `never reports aih's own marker as dropped-target residue`, `does not prescribe a repair command that cannot remove the residue`, `does not narrow Claude probes on %s`, `does not narrow Claude probes on inferred on-disk adapters`, `still fails a Claude-targeted repo when the projection is missing`, `config-marker probe warns when the marker is present but invalid (%s)`), `tests/prune/command.test.ts` (`subtracts exactly the two marker-proven keys, ownership last, never a delete`, `leaves every other key's value unchanged under --apply`, `reconciles the residue even after the dropped CLI's adapter is already gone`, `never weakens the projection for a still-committed but unrunnable claude`, `revokes rather than crashing when a directory sits at the projected path`, `revokes a claim whose projected path is a dangling symlink`, `never names a repair for a residue behind a symlinked parent`, `revokes rather than overwrites a drifted pair`, `stays silent and mutates nothing when no ownership marker proves the residue`, `converges — a second prune says nothing about an already-reconciled residue`), `tests/mcp/mcp.test.ts` (`writes no Claude managed allowlist when the committed targets exclude claude`, `modifies no existing Claude managed settings when the committed targets exclude claude`, `keeps writing the Claude managed allowlist for a repo with no committed marker`, `fails a marker-proven allowlist for a dropped target even when it still matches .mcp.json`, `fails a marker-proven allowlist for a dropped target with no .mcp.json to compare`), and `tests/uninstall/uninstall.test.ts` (`subtracts the marker-proven managed-MCP keys before removing the marker`, `names the managed-MCP keys it will subtract in a dry run without touching disk`, `reports an unprovable managed-MCP projection instead of subtracting it`, `previews a co-owned .claude context as advisory and names both sides of the boundary`, `leaves a co-owned .claude context in place under --apply`). |
| <a id="CM-09"></a>CM-09 | Published releases can be verified against npm signatures, GitHub checksums, cosign bundle, and tarball hash without overstating skipped legs. | `src/release/verify-release.ts` and `src/version.ts`. | `tests/release/verify-release.test.ts` (`runs npm, GitHub release, cosign, and tarball hash checks without overstating skips`, `skips only the cosign leg when cosign is unavailable`). |
| <a id="CM-10"></a>CM-10 | Telemetry setup does not transmit by default; generated collector/fetcher assets require operator action. | `src/telemetry/index.ts`, `src/telemetry/templates.ts`, and `src/telemetry/docs.ts`. | `tests/telemetry/telemetry.test.ts` (`HARD BOUNDARY: emits no exec actions at all (no local mutation needed)`, `HARD BOUNDARY: cron install + API call live in doc, not write/exec`). |
| <a id="CM-11"></a>CM-11 | Reports are offline artifacts by default; live refresh/demo/team-branch data are opt-in flags or degraded local views. | `src/report/`, especially `src/report/artifact.ts`, `src/report/index.ts`, and `src/report/repo.ts`. | `tests/report/artifact.test.ts` (`is a self-contained page: passed title in <title>, brand in <h1>, no external assets`, `embeds a meta-refresh only when a refresh interval is given (live mode)`), `tests/report/report.test.ts` (`is a read-only digest — only digest actions, never calls out`), `tests/report/repo.test.ts` (`without --team, shows the hint and makes NO gh call`). |
| <a id="CM-12"></a>CM-12 | Public claim ledger entries resolve from `<!-- aih:claim CM-xx -->` markers through control-matrix rows to named regression tests, and changed feature files without a docs or matrix update are treated as drift. | `src/docs-lint/index.ts` claim marker parsing, control-matrix proof validation, and Git diff drift check. | `tests/docs-lint/docs-lint.test.ts` (`fails a README claim marker with no CM mapping`, `fails a matrix row that cites a non-existent named test`, `fails a changed feature file when no docs or matrix file changed`, `emits coded advisory findings without failing the report for prose guidance`). |
| <a id="CM-13"></a>CM-13 | The optional project-truth sidecar records a code-commit binding, stages agent-proposed truth packs outside the repo, detects drift in commit/version/claim/decision assertions, runs acceptance preflight blockers, re-runs declared agent-evidence file probes, and can help govern evidence bundles as a hashed artifact. | `src/truth/index.ts`, `src/init/index.ts`, `src/evidence/build.ts`, `src/evidence/manifest.ts`, and `src/internals/verify.ts`. | `tests/truth/truth.test.ts` (`init --sidecar plans an external sidecar and commit binding`, `init --sidecar fails closed when git HEAD cannot be bound`, `init --sidecar rejects a sidecar path inside the repo`, `init --sidecar rejects an external link that resolves inside the repo`, `truth pack writes Markdown and JSON under the token budget`, `truth pack keeps non-ASCII truncated Markdown under the token budget`, `truth pack refuses to stage when sidecar drift is detected`, `truth pack refuses an explicit sidecar path inside the repo`, `truth verify --sidecar-path uses the explicit sidecar binding`, `truth verify fails closed on sidecar drift`, `truth verify fails closed on malformed sidecar assertions`, `truth verify fails closed on unsafe sidecar decision IDs`, `truth verify fails closed on unsafe sidecar staging directories`, `truth verify fails closed on unsafe sidecar acceptance assertions`, `truth verify fails closed on unsafe agent evidence paths`, `truth verify fails closed when an agent evidence path resolves into a denied hidden directory`, `truth verify fails closed when an agent evidence path resolves into secrets`, `truth pack preflight flags absent credentials before writes`, `truth verify flags vendor-specific requirements inside vendor-neutral work`, `truth verify re-runs and records agent evidence claims`, `truth verify fails closed when agent evidence no longer matches`, `truth verify preserves whitespace in agent evidence claims`, `truth verify does not run agent evidence after foundational sidecar drift`, `evidence build indexes a verified truth pack`, `evidence source accepts legacy truth packs when no assertion fingerprints are needed`, `evidence source fails closed on malformed truth pack assertion fingerprints`, `evidence build fails closed on an unverified truth pack`, `evidence build fails closed when truth assertions drift after packing`, `evidence build fails closed on over-budget truth pack metadata`). |
| <a id="CM-14"></a>CM-14 | Windows command-shim launchers are routed through `cmd /c` only where needed, and generated launcher arguments are rejected before emission when they contain unsafe command-shell metacharacters. | `src/tools/install.ts` launcher wrapping, `src/mcp/hygiene.ts` npm pin probes, and `src/vdi/index.ts` junction setup. | `tests/tools/tools.test.ts` (`rejects unsafe Windows cmd-shim arguments before wrapping with cmd /c`), `tests/mcp/mcp.test.ts` (`--verify routes npm MCP package pin probes through cmd on Windows`), `tests/vdi/vdi.test.ts` (`rejects unsafe HOME/USERPROFILE values before emitting mklink through cmd`). |
| <a id="CM-15"></a>CM-15 | Plugin commands cannot shadow the shared runner option surface, and report live-mode flags imply apply/open behavior only for specs that opt into that behavior. | `src/plugins/registry.ts` structural plugin gate, `src/commands/index.ts` read-only-aware command registration, and `src/commands/run.ts` option extraction/live-mode handling. | `tests/plugins/registry.test.ts` (`skips specs claiming shared runner option keys by alternate spelling`, `plugin report-like option names do not imply apply mode`, `sanitizes bidi controls from warning labels`), `tests/commands/run.test.ts` (`uses Commander attribute names for negatable custom options`, `rejects --refresh with --json before emitting a mixed JSON/live stream`, `rejects fractional or zero refresh intervals`), `tests/contract/command-surface.test.ts` (`read-only nested commands do not expose ignored mutating flags`). |
| <a id="CM-16"></a>CM-16 | Secret checks do not emit detected secret values, `.env*` and root `secrets/**` contents are not read by the filename scan, known MCP config files are inspected as contained regular files for hardcoded credentials, and `--verify` is warning-only at `vibe` but non-zero at `enterprise`. | `src/secrets/scan.ts`, `src/secrets/probes.ts`, `src/secrets/index.ts`, `src/guardrails/token-patterns.ts`, and `src/config/posture.ts`. | `tests/secrets/secrets.test.ts` (`--since-style scans flag root secrets/ when Git reports a changed child`, `flags a literal value under a secret-looking key in JSONC MCP config`, `flags a literal value under a secret-looking key in malformed MCP JSON`, `does not follow symlinked MCP config paths outside the repo`, `never emits the secret value itself, only file/key/kind`, `each secret yields a fail verdict that flips the gate exit code`, `keeps plaintext findings warning-only at the default vibe posture`). |
| <a id="CM-17"></a>CM-17 | Guardrail generation writes policy files and CI workflow definitions only; active enforcement depends on installed tools plus Git hook or CI wiring, and generated command policies do not allow broad shell secret readers or unverified inline code by default. A missing local `gitleaks` is an advisory skip at `vibe` and a failing check at `enterprise`, since the generated config enforces nothing until the tool is installed. | `src/guardrails/index.ts`, `src/guardrails/command-policy.ts`, `src/guardrails/gitleaks.ts`, `src/guardrails/precommit.ts`, `src/guardrails/sca.ts`, `src/scaffold/hooks.ts`, and `.githooks/pre-commit`. | `tests/guardrails/guardrails.test.ts` (`gitleaks.toml allowlists local scratch only (never source)`, `preserves user-authored pre-commit config even if a comment mentions the marker`, `sca workflow verifies the pinned gitleaks tarball before extraction`, `verify: gitleaks probe SKIPS at vibe when the tool is absent (never fails)`, `verify: gitleaks probe FAILS at enterprise when the tool is absent`), `tests/guardrails/command-policy.test.ts` (`maps each tier to Bash(<exact pattern>) with no transformation or dedupe`), `tests/scaffold/scaffold.test.ts` (`generated pre-commit hook runs lint and test scripts when package.json defines them`). |
| <a id="CM-18"></a>CM-18 | MCP generation targets each CLI's native config shape, keeps a bare first run on Claude unless targets are explicitly detected or committed, replaces generated same-name JSON entries instead of retaining stale credentials, applies policy-narrowed managed allowlist checks, scans known global MCP configs for redacted secret literals, binds egress approvals to the current server subject, and uses `--mcp-compliant` to omit denied generated entries from targeted configs. A package resolver is pinned only when a bare `npx` or `uvx` directly launches exact package operands with no config-provided environment; aliases, source/interpreter options, and environment overrides fail closed consistently in generation, verification, workspace policy, and baseline attestation. | `src/mcp/index.ts`, `src/mcp/pins.ts`, `src/mcp/policy.ts`, `src/mcp/allowlist.ts`, `src/mcp/render.ts`, `src/mcp/servers.ts`, `src/mcp/hygiene.ts`, `src/trust/mcp-classify.ts`, `src/workspace/index.ts`, `src/baseline/attestation.ts`, `src/heal/mcp-probe.ts`, `src/secrets/index.ts`, and `src/secrets/scan.ts`. | `tests/mcp/mcp.test.ts` (`replaces generated JSON server entries so stale auth fields do not survive`, `a bare first run stays on the Claude project config even when global CLIs are runnable`, `hardens every generated uvx MCP launcher against startup fetches and .env reads`, `fails closed on mutable resolver modes and recognizes npx's -- package operand`, `does not verify an exact npx pin with a configured registry override`, `--mcp-compliant drops denied servers, quarantines them in guidance, and verifies clean`, `plans mcp approve as a local org-policy write with review evidence`), `tests/trust/mcp-classify.test.ts` (`does not trust path-qualified resolver lookalikes as package pins`, `fails closed when a resolver environment contains a non-string override`), `tests/workspace/workspace.test.ts` (`under enterprise keeps malformed resolver environments unpinned`), `tests/baseline/attestation.test.ts` (`does not bind a malformed resolver environment to an allowlisted catalog server`), `tests/mcp/policy.test.ts` (`DENIES when a third-party approval subject no longer matches the server shape`), `tests/doctor.test.ts` (`fails closed on malformed managed-settings JSON`, `passes when the managed allowlist matches org-policy narrowed MCP servers`), `tests/secrets/secrets.test.ts` (`scans known global MCP configs for redacted secret literals`), `tests/heal/heal.test.ts` (`malformed .mcp.json text mentioning npx does not count as a launcher`, `does not follow a symlinked .mcp.json while checking for npx launchers`). |
| <a id="CM-19"></a>CM-19 | Trust scan preserves four separate layers: raw analyzer occurrences, normalized AIH findings, policy dispositions, and the selected active-profile verdict. Duplicate analyzer rows remain in raw evidence but normalize once. Lexical HTTP/code/documentation language is not prompt injection without override or exfiltration intent; boolean negation is not bang auto-run; ordinary prose Unicode and prose/comment variation selectors warn or pass, while actual bidi/zero-width/control smuggling and confusables in executable tokens fail closed. Generic third-party heuristics warn unless contextual AIH evidence elevates them; credible egress remains review-required. | `src/trust/evidence.ts`, `src/trust/fingerprint.ts`, `src/trust/lint.ts`, `src/trust/manifest.ts`, `src/trust/detectors.ts`, `src/trust/grade.ts`, `src/trust/scan.ts`, and `src/baseline-evidence/profiles.ts`. | `tests/trust/evidence.test.ts` (`retains duplicate raw occurrences while normalizing one policy finding`), `tests/trust/lint.test.ts` (`keeps explicit credential extraction blocking`, `warns for confusable examples in prose but blocks them in executable identifiers`, `warns for emoji variation selectors in prose and comments`), `tests/trust/manifest.test.ts` (`does not treat markdown image syntax as bang auto-run`), `tests/trust/scan.test.ts` (`deduplicates identical scanner/rule/path/line results before verdict aggregation`), `tests/trust/grade.test.ts` (`keeps the review and warning code sets explicit`), and `tests/baseline-evidence/profiles.test.ts` (`does not let an unselected component block ECC Lean`, `reports the exact line and value for a selected review finding`). |
| <a id="CM-20"></a>CM-20 | ECC and Superpowers baseline bytes are acquired at exact pins, authorized per component by shipped vendor or attributable org evidence, and re-hashed before install action construction. Enterprise requires an exact org override before packaged vendor evidence is read; Vibe retains its packaged fallback. Missing or same-source stale Enterprise evidence emits blocking `baseline.org-evidence-required`; exact blocked evidence denies at every posture. Covered seats do not rerun release analyzers. | `src/baseline-evidence/org.ts`, `src/baseline-evidence/pipeline.ts`, `src/baseline-evidence/run.ts`, `src/baseline-evidence/verify.ts`, `src/support/findings.ts`, `src/ecc/pipeline.ts`, `src/ecc/verified.ts`, `src/superpowers/pipeline.ts`, `src/superpowers/verified.ts`, `src/org-policy/schema.ts`, and `.github/workflows/baseline-evidence.yml`. | `tests/baseline-evidence/verify.test.ts` (`authorizes an exact covered component without running an analyzer`, `authorizes a newer pin through matching signed org evidence`, `does not let org evidence replace an exact vendor-blocked verdict for the same bytes`), `tests/baseline-evidence/run.test.ts` (`detects a post-clearance mutation and never constructs install actions`), `tests/baseline-evidence/org.test.ts` (`keeps Vibe absence empty so the packaged fallback remains available`, `names only the first exact same-source stale override and never leaks unrelated entries`, `uses the live requested pin when an old same-source override is stale`), `tests/ecc/pipeline.test.ts` (`never constructs install actions when signed evidence blocks`, `blocks Enterprise absence before reading vendor evidence or constructing a baseline gate`, `uses AIH_ECC_REF as the live Enterprise pin before any vendor evidence work`, `keeps Vibe on the existing evidence fallback when no org override is configured`, `rejects fetched metadata that does not bind the catalog pin`), `tests/internals/check-code.test.ts` (`wires every CheckCode union member to a real emitter (excl. verify.ts)`), `tests/superpowers/evidence.test.ts` (`emits receipts and manual guidance without mutable remote execs`), `tests/baseline-evidence/commands.test.ts` (`writes one typed baseline report after an exact local checkout is vetted`), and `tests/baseline-evidence/package.test.ts` (`ships the auditable vendor lock in the actual npm pack file list`, `runs a non-publishing vet-once workflow at both canonical source pins`). |
| <a id="CM-21"></a>CM-21 | ECC registration defaults to the common + project-required + validated-MCP component union, accepts repeatable advance declarations, preserves prior projects additively, filters manifest operations/state at leaf granularity, and commits an evidence-bearing project/target ledger only after successful installs. Codex uses its real on-demand skill path and idempotent add-only MCP/AGENTS blocks; scoped unsupported targets remain guidance-only. | `src/ecc/components.ts`, `src/ecc/materialize.ts`, `src/ecc/mcp.ts`, `src/ecc/pipeline.ts`, `src/ecc/registration.ts`, `src/ecc/verified.ts`, `src/ecc/index.ts`, and `src/ecc/codex.ts`. | `tests/ecc/components.test.ts` (`keeps the default minimal closure deliberate and free of automatic platform or MCP surfaces`, `treats repeatable advance declarations as additive and stable`, `modulates security content and GitHub MCP by posture without defaulting egress`, `uses full only when explicitly requested`), `tests/ecc/pipeline.test.ts` (`builds the additive machine union from explicit declarations and prior projects`), `tests/ecc/materialize.test.ts` (`keeps selected files and target scaffolding while filtering unrelated agents and skills`, `fails closed on unsupported operation shapes`), `tests/ecc/registration.test.ts` (`merges canonical per-project contributions into a deterministic machine union`, `preserves the previous valid ledger when final rename fails`), and `tests/ecc/verified.test.ts` (`commits the ledger only after every install step succeeds`, `registers only the current project's validated MCPs in project-local config`, `does not treat its own scoped HTTP GitHub registration as a rerun collision`, `materializes selected Codex skills into the real on-demand skill directory`, `degrades scoped Kiro registration to guidance instead of installing the whole surface`). |
| <a id="CM-22"></a>CM-22 | `aih prune` retires missing scoped-ECC project registrations, preserves the live shared component/MCP union and user-owned content, and coordinates strict state-recorded removals, upstream target uninstall, target-state writes, and the primary ledger in one phased transaction with the ledger last. Aih-owned failures and supported interrupts roll back; possibly mutating upstream work emits every affected target/path as divergence evidence and never advances the ledger. | `src/ecc/reconcile.ts`, `src/ecc/prune-reconcile.ts`, `src/ecc/reconcile-driver.ts`, `src/ecc/registration.ts`, `src/prune/index.ts`, `src/internals/plan.ts`, and `src/internals/execute.ts`. | `tests/ecc/reconcile.test.ts` (`retires only missing projects and filters targets to the live union`, `keeps shared components until their last live contributor is gone`, `fails closed for symlinked or non-directory registered roots`, `strictly rejects malformed states, duplicate destinations, and unsupported operations`), `tests/ecc/reconcile-driver.test.ts` (`removes orphan files, preserves user JSON, writes target state, and commits the ledger last`, `runs upstream uninstall between owned removals and target-state writes`, `rolls back aih-owned changes and reports target divergence when upstream mutates then fails`, `names every affected target after an earlier target uninstall already succeeded`, `sizes the outer driver timeout above the sequential upstream uninstall budget`, `rejects an unknown mutation phase before applying any change`, `rolls every mutation back when the final ledger step fails`, `fails before mutation when an input changes after planning`, `fails before mutation when a recorded managed JSON subset has drifted`, `retries one transient rename and is byte-idempotent after replanning`, `filters only Codex managed MCP and skill entries while preserving user content`, `fails before mutation when a claimed Codex managed marker is malformed`, `rejects a symlink substituted for a managed destination`), and `tests/prune/command.test.ts` (`plans and applies a deterministic ledger-last diff even without committed CLI intent`, `coordinates a whole-target uninstall inside the ledger-last transaction`, `moves dropped Codex config, block, and state removals into the coordinated payload`, `falls back to standalone Codex cleanup when the ledger has no Codex target record`, `fails closed on a malformed primary registration ledger`). |
| <a id="CM-23"></a>CM-23 | Generic third-party detector findings and genuinely unresolved skill-license metadata are review-required: they warn below enterprise and require an exact-fingerprint acknowledgement with a recorded reason at enterprise. Generic findings on regular non-executable legal text are visible warnings at every posture. A top-level repository license resolves a skill's omitted frontmatter license for scanner classification; absent repository evidence remains review-required, while the independent native approval license gate is unchanged. Known executable danger rules remain blocking and cannot be acknowledged. | `src/trust/detectors.ts`, `src/trust/grade.ts`, `src/trust/acknowledge.ts`, `src/skill/vet.ts`, `src/baseline-evidence/vet.ts`, `src/internals/fsxn.ts`, and `src/internals/verify.ts`. | `tests/trust/scan.test.ts` (`warns on generic legal-text findings while keeping executable and mapped danger distinct`, `reclassifies the Cisco missing-license metadata finding as a graded trust-origin finding`, `recognizes repository-level license inheritance for a skill manifest`, `deduplicates identical scanner/rule/path/line results before verdict aggregation`), `tests/skill/vet.test.ts` (`keeps generic LICENSE.txt findings visible without blocking promotion`), `tests/baseline-evidence/vet.test.ts` (`scans one path-preserving isolated projection per component and removes it`), `tests/internals/fsxn.test.ts` (`refuses an oversized regular file before reading from the opened descriptor`), `tests/trust/acknowledge.test.ts` (`acknowledges the trust-origin missing-license finding with a fingerprint and reason`), and `tests/trust/grade.test.ts` (`keeps the review and warning code sets explicit`). |
| <a id="CM-24"></a>CM-24 | Repository AI guidance routes Token Savior to read-only orientation, Serena to exact semantic work, Token Optimizer to quiet on-demand overhead analysis, and code-review-graph to advisory blast-area and reviewer context only. Client-specific MCP and hook launchers are not committed; optional local projections degrade to source and tests when absent. AIH never runs against the AI Harness checkout, including read-only project-governance forms. The repo manually preserves the useful AIH-shaped canon, validates it with direct repository checks, and exercises product behavior only through temporary fixture roots. | `ai-coding/SELF-HOSTING.md`, `ai-coding/rules/repo-ai-tools.md`, `ai-coding/rules/project-canon-extension.md`, `AGENTS.md`, `CLAUDE.md`, `package.json`, and `.github/workflows/ci.yml`. | `tests/bootstrap-ai/bootstrap-ai.test.ts` (`the shared block carries secrets safety and advisory graph routing`), `tests/self-hosting/self-hosting.test.ts` (`states the hard rule on every always-loaded canon surface`, `keeps bootloader blocks byte-aligned with the manual shared source`, `does not expose a repository script or CI gate that self-applies AIH`, `keeps the manual contract mirror aligned with live repository facts`), `tests/internals/repo-ai-tools.test.ts` (`keeps client-specific MCP and hook launchers out of the repository`, `keeps the repository hook path limited to the non-AI pre-commit guardrail`, `routes overlapping tools in the repo-owned canon`, `makes graph use advisory and consistent in every session bootloader`, `keeps this repo's manual canon authoritative without generator self-use`). |
| <a id="CM-25"></a>CM-25 | Workspace-child graph safety accepts only a graph scoped to the checked child and populated with both files and nodes. A valid generated exact offline workspace alias remains preferred; when it is absent, an installed `code-review-graph` binary may prove the same child-scoped graph state. Empty, failed, or unscoped graph checks do not pass. | `src/scale-safety.ts` and `src/doctor.ts`. | `tests/doctor.test.ts` (`checks graph coverage against each present child repo, not just the workspace parent`, `accepts a populated graph binary as workspace child graph coverage`, `fails closed before probing graph coverage for linked child repo paths`). |
| <a id="CM-26"></a>CM-26 | GitHub trust-source fetches follow only a bounded chain of credential-free default-port HTTPS redirects between the canonical GitHub API and codeload endpoints; every other redirect fails before a connection is attempted. | `src/trust/fetch.ts`. | `tests/trust/fetch.test.ts` (`rejects a redirect outside the trusted GitHub fetch endpoints before connecting`, `permits only bounded HTTPS redirects on canonical GitHub fetch hosts`). |
| <a id="CM-27"></a>CM-27 | Project framework binding records exact source identity (git: repository + exact 40-char commit SHA + tree digest; npm: package + exact version + SRI integrity; refs are never recorded identity), rejects a second methodology framework independently at the schema, plan, and provision layers, and cannot provision without a genuine scan-gate disposition for the exact resolved digest — forged, blocked, or digest-mismatched dispositions fail closed even when an adapter omits its own guard, and cached checkouts are re-cloned unless verified at the exact commit and clean. Fast inspection produces all eleven bounded static dimensions with incomplete coverage failing closed outside an explicit low-strictness opt-in and a danger floor that blocks at every posture; scan caches are derived and rebuildable; the applied-state lock ties `match` to actual digest equality and a missing lock degrades removal planning to drift-report-only. | `src/binding/schema.ts`, `src/binding/lock.ts`, `src/binding/adapter.ts`, `src/binding/scan-gate.ts`, and `src/config/marker.ts`. | `tests/binding/schema.test.ts` (`rejects a second framework smuggled via an extra framework key (strict, not stripped)`, `rejects a ref/branch name in the commitSha identity field`, `rejects a tag/dist-tag as the npm exactVersion`, `round-trips a git declaration through parse -> serialize -> parse`), `tests/binding/adapter.test.ts` (`rejects a forged, unbranded disposition`, `rejects a disposition whose digest does not match the resolved source`, `rejects a block-verdict disposition`, `plan re-rejects a second framework (layer 2), independent of provision`, `provision re-rejects a second framework (layer 3), even with a valid allow disposition`), `tests/binding/review-fixes.test.ts` (`blocks a sloppy adapter's provision on a forged disposition via the registry wrapper`, `re-clones when the cached checkout is not at the requested commit`, `rejects a leading-dash repository at the resolver layer`), `tests/binding/scan-gate.test.ts` (`blocks at enterprise when a (future deep) dimension is unavailable (incomplete coverage fails closed)`, `blocks on a danger finding even at vibe with incomplete allowance (danger floor)`, `keeps the validation outcome unchanged after the derived caches are deleted`), `tests/binding/inspectors.test.ts` (`covers all eleven D12 dimensions as produced`), `tests/binding/lock.test.ts` (`rejects match:true when scanned and loaded digests differ (fail closed on drift)`, `degrades to drift-report-only when the lock is missing (never guess-delete)`), `tests/binding/index.test.ts` (`keeps validation outcomes unchanged when derived caches are deleted; the committed declaration is untouched`), `tests/binding/coverage-identity.test.ts` (`forces incomplete coverage when an identity file is absent from the inventory`, `fails closed when a source carries no identity file list (cold and warm cache)`, `keeps a clean tree with benign vendored/build content COMPLETE and allowed (no false incompletes)`). |
| <a id="CM-28"></a>CM-28 | The Claude host binding engine owns shared JSON settings/MCP files by FIELD — owning the whole container only when the bind itself created it — plus one marker-fenced CLAUDE.md block and digest-owned rule/skill/agent files. Conservative removal restores recorded pre-existing state, preserves every unrelated entry and every user-modified owned entry with drift reported, never replaces a whole shared JSON file, reports (never guesses through) malformed lock targets, and leaves no AIH-created empty container or directory behind. | `src/binding/hosts/claude/managed-writes.ts`, `src/binding/hosts/claude/removal.ts`, `src/binding/hosts/claude/surfaces.ts`. | `tests/binding/hosts/claude/removal.test.ts` (`unrelated content survives; the modified owned entry is preserved + reported, never deleted`, `reports drift for a json-pointer entry whose target carries no pointer`, `removes the whole container AIH created, leaving no empty object behind`, `preserves a user-added sibling in an AIH-created container and reports drift`, `keeps a directory that still holds a user file after the owned file is removed`), `tests/binding/hosts/claude/managed-writes.test.ts` (`refuses a path outside the D4.3 owned-file surfaces`, `rejects a Windows backslash path at the schema boundary`, `produces lock-valid ownership entries after apply`), `tests/binding/hosts/claude/roundtrip.test.ts` (`binds all mechanisms, locks, removes from the re-read lock, and leaves a clean tree`). |
| <a id="CM-29"></a>CM-29 | Claude plugin binding registers the scanned checkout itself as the marketplace source, asserts a genuine scan-gate disposition for the exact resolved digest before any Claude CLI invocation, re-digests the loaded plugin cache tree with the same canonical tree-hash routine the scan gate records, and fails closed on mismatch (disable + uninstall, no lock written). Machine-scope removal treats an unreadable or drifted cache tree as preserved-and-reported drift — only a genuinely missing tree is an idempotent no-op — and hostile plugin or marketplace names are refused before reaching the CLI or a JSON pointer. | `src/binding/hosts/claude/plugins.ts`, `src/binding/hosts/claude/plugin-identity.ts`. | `tests/binding/hosts/claude/plugins.test.ts` (`rejects a forged (unbranded) disposition and runs no CLI`, `rejects a disposition whose digest does not match the resolved source`, `rejects a blocked-verdict disposition (danger floor) and runs no CLI`, `disables + uninstalls, throws a typed identity error, and writes no lock/ownership`, `marketplaceAdd refuses a non-absolute source path (option-injection guard)`), `tests/binding/hosts/claude/plugin-identity.test.ts` (`ignores a .git dir so a checkout and its copy digest equally`, `throws the distinguishable missing-cache error for a missing tree`, `throws a plain (NON-missing) plugin error for an empty tree`). |
| <a id="CM-30"></a>CM-30 | User-scope cleanup remediation is opt-in and previewed: destructive steps execute only after every affected file is copied into a backup and a schema-validated manifest is durably written; edits are targeted JSON key or hook removals with remove/edit targets re-validated against fixed allowlists at apply time — a whole shared JSON file is never deleted or replaced, and a malformed JSON root fails the step rather than silently completing; rollback refuses a schema-invalid manifest outright and skips digest-mismatched backups instead of overwriting live files. The contamination scan feeding this remediation covers ECC's CURRENT machine layout (`~/.claude/ecc/skills/`, `~/.claude/ecc/.agents/skills/`, namespaced `agents/ecc/`) in addition to the flat/legacy locations, name-deduped per the `machineEccSkillNames` canon — a skills-only ECC install under `~/.claude/ecc/` can never render a false "clean" verdict input. | `src/binding/hosts/claude/cleanup.ts`, `src/binding/hosts/claude/contamination.ts`. | `tests/binding/hosts/claude/cleanup.test.ts` (`a step failure after the manifest leaves backup + manifest intact and nothing removed`, `fails a disable step on a non-object settings root, with backup + manifest intact`), `tests/binding/hosts/claude/contamination.test.ts` (`counts every user-scope surface exactly with correct attributions`, `a project-scope surface never appears in the user-scope report`, `flags a skills-only ECC install living solely under ~/.claude/ecc/ (no legacy namespace)`, `name-dedupes a skill present in both current ECC roots (canon: machineEccSkillNames)`, `name-dedupes a current-root skill against the same name under ~/.claude/skills/`). |
| <a id="CM-32"></a>CM-32 | The Superpowers framework adapter binds only through the scan-authorized plugin mechanism: planning rejects a second methodology framework and any declared feature key, provisioning requires a genuine disposition for the exact resolved digest before any Claude CLI call, and the loaded plugin cache is verified against a scan-time digest of the plugin source subtree — with telemetry disable owned as a managed settings field in the same lock, and machine-scope surfaces recorded for conservative removal. | `src/binding/frameworks/superpowers.ts`, `src/binding/frameworks/registry.ts`. | `tests/binding/frameworks/superpowers.test.ts` (`rejects planning when a different framework is already bound (D8)`, `rejects any declared feature key — superpowers accepts none`, `rejects a forged (unbranded) disposition and runs no CLI`, `rejects a disposition whose digest does not match the resolved source`, `binds the plugin, writes a schema-valid lock, and owns enabledPlugins/telemetry/home entries`, `produces a writes/ownership preview covering enabledPlugins, telemetry, and home: entries with no I/O`). |
| <a id="CM-33"></a>CM-33 | ECC Lean binds only an exact component allowlist through the pin-bound upstream installer plan: the preview is diffed against the allowlist before anything installs and any extra surface, undeliverable component, or runtime-surface escape (hooks, MCP configs, learned skills, continuous-learning paths, and Claude settings files in either scope) fails the bind closed with zero installer invocations and no lock; post-install verification attests runtime-surface absence. | `src/binding/frameworks/ecc.ts`. | `tests/binding/frameworks/ecc.test.ts` (`the real pinned preview delivers the entire allowlist with no runtime escapes`, `flags an extra runtime surface an allowlisted component would drag in`, `provision fails closed when the preview cannot deliver an allowlisted component (no install, no lock)`, `eccRuntimeSurfaceHit flags .claude/settings(.local).json in BOTH scopes`, `provision fails closed (no lock) when the installer writes a project-scope settings file`, `installs the vetted allowlist, writes a schema-valid lock, records ownership, attests runtime-surface absence`, `rejects a forged (unbranded) disposition and runs no installer`). |
| <a id="CM-34"></a>CM-34 | ECC Lean and Full are mutually exclusive per project in both directions, Full's strict-containment label is decided by a static state-write inventory over the scanned tree (the legacy continuous-learning home write is detected; a surface is excluded only through a supported upstream selection mechanism, otherwise the label downgrades with shared writes enumerated), and doctor detects both the plugin-plus-manual-copy double install and a lock-mode/state mismatch. | `src/binding/frameworks/ecc.ts`, `src/binding/frameworks/binding-doctor.ts`, `src/doctor.ts`. | `tests/binding/frameworks/ecc.test.ts` (`full provision fails closed over a lean lock (no CLI, lock stays lean)`, `lean provision fails closed over a full lock (no installer, lock stays full)`, `flags a continuous-learning skill + a learned-skills write; strict:false, shared writes enumerated`, `excludedSurfaces covering all findings -> strict:true, exclusions removed`), `tests/binding/frameworks/binding-doctor.test.ts` (`fails when both the plugin and a project-scope manual copy are present`, `tolerates malformed settings.json — treated as no plugin signal, never throws`). |
| <a id="CM-35"></a>CM-35 | The change-profile preflight accepts exactly one deliberately named, bounded regular JSON file through the canonical read-only command adapter and emits one standard digest. Missing, empty, non-regular, oversized, changing, invalid-UTF-8, malformed, unknown, contradictory, and classifier-invalid input fails closed under one stable code with sanitized, sorted, capped issue records. Current and previous change paths containing C0/C1 or bidirectional-control characters are rejected before a successful human or JSON result can echo them. The separate classifier emits a versioned, byte-stable capability profile with reason-carrying ambiguity and unavailable-content records and has no filesystem, Git, process, network, clock, randomness, environment, or write dependency. | `src/profile/change-profile-command.ts`, `src/profile/change-profile.ts`, `src/profile/change-profile-rules.ts`, `src/capability/id.ts`, and `src/errors.ts`. | `tests/profile/change-profile-command.test.ts` (`emits one canonical digest with the profile at digests[0].data`, `fails closed with input.changed when the file changes after opening`, `rejects control and bidi characters before a successful command can echo change paths`, `sanitizes, stable-sorts, and caps rendered issue records at 20 while retaining the total`), `tests/profile/change-profile-validation.test.ts` (`rejects C0, C1, and bidi controls in current and previous paths`, `is canonical, deterministic, byte-sensitive, and never leaks content or revisions`, `requires denied paths to contain only policy-denied unreadable facts`), `tests/profile/change-profile-rules.test.ts` (`keeps classifier imports inside the pure static boundary`, `orders output stably and is independent of clock, randomness, and environment`, `handles all statuses/scopes and unavailable content without arbitrary token scanning`). |
| <a id="CM-36"></a>CM-36 | `aih live` is progress-only for exactly one explicitly selected human-invoked local CLI and uses the canonical target root. Codex and Claude run in pinned explicit core read-only modes and every progress/final view labels them `read_only`; that label is not a clean-room attestation for locally configured vendor extensions. Claude disables skills but does not use `--safe-mode`, Codex does not ignore all user configuration, and native project/user instructions, plugins, hooks, MCP configuration, or other customization surfaces may still initialize under the vendor CLI without an aih read-only attestation. Aih itself does not select workers, schedule tasks, retain aih agent memory, or run a council. Kimi 0.29.2 has no verified equivalent mode, is labeled `non_read_only` in progress plus human/JSON success and error views, requires consent before prompt read or launch, may use native tools and change the selected worktree, and receives its prompt only as one direct argv element to a native executable (never a Windows command shim). Aih performs no worktree-safety or dirty-tree preflight for the Kimi path. Aih never prints Kimi's argv, but its prompt argument can be visible to Task Manager, WMIC, and other local process-inspection tools. Prompts are strict-UTF-8 bounded regular files; Codex/Claude receive exact decoded text on stdin. Native streams become capped generic versioned events on stderr, while one redacted bounded terminal digest uses stdout. Aih does not claim OS containment. | `src/live/index.ts`, `src/live/runner.ts`, `src/commands/index.ts`, and `src/errors.ts`. | `tests/live/command.test.ts` (`requires exactly one explicit CLI selection and never falls through to process launch`, `requires Kimi consent before prompt read, resolution, or spawn`, `labels Kimi non_read_only in the human success view`, `labels Kimi %s failures non_read_only in human and JSON final views`, `rejects denied prompt files before read or spawn`, `accepts timeout bounds 5 and 3600 and rejects 4 and 3601 before spawn`, `fails closed when the prompt file changes after opening`), `tests/live/runner.test.ts` (`pins core read-only Codex and Claude invocations without claiming clean-room isolation`, `executes a temporary fixed cmd fixture through absolute System32 cmd.exe`, `skips a non-executable POSIX decoy and selects the later executable`, `treats a terminal Claude error result as failure even after a prior result candidate`, `settles the original bounded failure when cleanup rejects`, `uses bounded absolute System32 taskkill tree cleanup on timeout`). |

| <a id="CM-37"></a>CM-37 | The internal ECC profile pipeline authenticates its pinned manifests, review receipt, and complete projected-source closure before deterministically rendering native Claude and Codex project artifacts. Ownership remains distinct from per-client transport (`native`, reviewed `normalized`, or `unavailable` with an actionable reason/fallback); source links, path escapes, ambiguous identities, byte/type/mode drift, races, and size-limit violations fail closed. Snapshot inventory totals are versioned receipt evidence, not implementation targets. | `src/ecc-profile/index.ts`, `src/ecc-profile/source-closure.ts`, `src/ecc-profile/projection-policy.ts`, and `src/ecc-profile/render.ts`. | `tests/ecc-profile/resolver.test.ts` (`derives the exact module closure and complete pinned path sets`, `adapts only the four real ownership-sensitive commands`, `rejects an invented active leaf absent from pinned skill inventory`, `rejects altered manifest bytes under a supplied source root`, `rejects ambiguous derived identity: %s`), `tests/ecc-profile/render.test.ts` (`renders exact Claude and Codex parity from the verified resolver`, `classifies and context-normalizes every workflow transport without changing ownership`, `classifies every selected skill and prevents inactive or client-specific behavior from masquerading as native`), `tests/ecc-profile/render-race.test.ts` (`rejects a same-byte linked file substituted after inventory enumeration`), `tests/ecc-profile/actual-source.test.ts` (`reproduces the authenticated input closure and stable native projection`), and `tests/ecc-profile/native-smoke.test.ts` (`checks root config and doctor parsing with materialized canaries`). |
| <a id="CM-38"></a>CM-38 | The internal ECC hook core normalizes the reviewed Claude and Codex event intersection into a bounded canonical envelope and sequences matching handlers inside one deterministic composite dispatcher. Handler order, enablement, timeout, failure behavior, redaction, and storage are explicit; ambiguous configuration, hostile paths or JSON, native/logical identity conflicts, false fail-closed guarantees, and unredacted persistence fail closed without leaking handler errors. | `src/ecc-profile/hook-core.ts` and `tests/fixtures/ecc-profile/hook-vectors.json`. | `tests/ecc-profile/hook-core.test.ts` (`normalizes paired Claude and Codex shared vectors`, `normalizes reviewed Claude-only failure and notification events`, `rejects a fabricated normalized event whose native identity conflicts`, `runs matching handlers in explicit order and returns byte-stable receipts`, `fails open or closed according to each handler's reviewed policy`, `bounds a handler timeout and aborts its signal`, `rejects fail-closed policy on a native informational event`, and `rejects persisted handler data without reviewed redaction`). |
| <a id="CM-39"></a>CM-39 | The internal ECC default handlers block Git verification bypass and hook-path overrides without issuing Git writes, retain redacted continuity checkpoints only in an exact repository/worktree/harness scope outside the worktree, and probe only explicitly selected MCP servers with bounded caching, backoff, opt-in reconnect, and deterministic fallbacks. Repository protection fails closed on ambiguous shell input; continuity and MCP health fail open at dispatch. Continuity state and injected context are bounded, retained for 30 days, reject links or malformed/cross-scope data, and omit raw prompts, tool input/output, credentials, and authentication headers. | `src/ecc-profile/default-hooks.ts`. | `tests/ecc-profile/default-hooks.test.ts` (`blocks verification bypass without performing a Git write: %s`, `writes a redacted durable pre-compaction checkpoint and resumes the exact worktree`, `records activity metadata without raw prompt, tool input, output, or auth headers`, `rejects foreign worktrees and cannot resume another harness or repository`, `persists deterministic scoped records outside the worktree`, `rejects in-worktree state, linked state files, malformed state, and byte overflow`, `probes only selected servers, caches health, and returns deterministic fallbacks`, and `backs off failures and reconnects only when explicitly configured`). |
| <a id="CM-40"></a>CM-40 | The internal ECC learning and personal-observability handlers remain disabled until explicitly enabled. Learning stores only bounded redacted observations and non-discoverable candidates; its handler cannot promote a candidate, and a separate approval record requires human intent plus digest-bound observation/candidate provenance. Personal observability stores bounded event metrics without raw prompts, session identifiers, tool input/output, or error bodies. Both handlers fail open at dispatch and use exact repository/worktree/harness-scoped state outside the worktree with deterministic record caps, retention pruning, atomic replacement, and malformed/link/cross-scope rejection. This internal slice does not register client hooks or materialize learned skills. | `src/ecc-profile/opt-in-hooks.ts`. | `tests/ecc-profile/opt-in-hooks.test.ts` (`stays disabled until the caller records explicit opt-in`, `records only redacted observations and non-discoverable candidates`, `requires recorded human intent and candidate provenance before approval`, `stores sanitized event metrics without prompts, tool data, or raw session identity`, `keeps learning state outside Git scope and rotates deterministically`, `prunes observability state by retention and record cap`, and `rejects in-worktree, linked, malformed, and cross-scope state`). |

| <a id="CM-41"></a>CM-41 | The internal ECC MCP profile prepares, but does not register, the reviewed Claude and Codex native projections for the selected server set. Serena is bound to the exact package/source/wheel/metadata identity plus acquisition-supplied dependency-lock evidence, uses isolated state and telemetry-off launch settings, and exposes only the fixed reviewed symbol/refactor surface through a protocol-level guard. Context7 requires an endpoint-shape attestation; disabled defaults are not silently reintroduced; unrelated operator servers survive merge while selected-name ownership conflicts fail closed. Ordinary startup health remains advisory, while setup acceptance fails closed until every selected server initializes. | `src/ecc-profile/mcp-profile.ts`. | `tests/ecc-profile/mcp-profile.test.ts` (`projects exactly the selected profile identities and leaves defaults disabled`, `renders deterministic native Claude JSON and Codex TOML without registering it`, `binds Serena to the reviewed package, source, wheel, dependency lock, and isolated roots`, `fails closed on malformed trust evidence or unsafe/ambiguous roots`, `enforces the Serena tool boundary even when a client filter is absent`, `enforces the hard policy on JSON-RPC calls and returned discovery`, `merge-preserves unrelated operator servers and rejects selected-name conflicts`, and `keeps ordinary startup advisory while setup acceptance fails closed`) plus opt-in `tests/ecc-profile/serena-live.integration.test.ts` (`initializes the bounded symbolic surface and serves a representative symbol request in both clients`). |

| <a id="CM-42"></a>CM-42 | The internal ECC Token Savior boundary binds the reviewed package, source, and wheel identity; invokes only the pure compactor through an isolated, credential-scrubbed process; retains bounded full originals outside the worktree before model-visible substitution; and fails open without changing native output on timeout, malformed output, or retention failure. Claude uses its native successful-output replacement. Codex replacement remains explicitly experimental because its qualified native hook reports blocked-result feedback. The optional Claude audit projection requires explicit digest-bound consent, remains prepared but unregistered, keeps state and transcripts outside the worktree, exposes only `ts_discover`, and reports no resources. | `src/ecc-profile/token-savior.ts`. | `tests/ecc-profile/token-savior.test.ts` (`binds the exact reviewed package and source identity`, `retains the full original before returning the reviewed %s pre-model transport`, `matches a worktree reached through a canonical parent-directory alias`, `fails open to the original output on timeout, runner failure, malformed results, or no match`, `fails closed on unsafe retention scope and malformed persisted evidence`, `is Claude-only, consent-bound, sanitized, unregistered, and hard-limited to ts_discover`) plus opt-in `tests/ecc-profile/token-savior-live.integration.test.ts` and `tests/ecc-profile/token-savior-native.integration.test.ts`. |
| <a id="CM-43"></a>CM-43 | The internal ECC Plan Canvas adapter authenticates and deterministically hardens its pinned runtime before use, copies only reviewed Markdown or HTML artifacts into revision-bound managed state, listens only through the upstream loopback guard, and enforces managed artifact-root containment again inside the local HTTP server. Browser rendering has no network egress by default; only the reviewed exact Mermaid URL can be enabled. Runtime commands use bounded I/O, fixed idle cleanup, and a credential-scrubbed environment, while feedback and verdicts are rejected if snapshot bytes change. | `src/ecc-profile/plan-canvas.ts`. | `tests/ecc-profile/plan-canvas.test.ts` (`binds the independently published artifact and complete runtime closure`, `creates a deterministic immutable revision snapshot under managed state`, `rejects traversal, symlinks, unsupported types, and oversized artifacts`, `uses only the pinned entrypoint, fixed loopback defaults, bounded I/O, and scrubbed env`, `binds feedback and verdicts to immutable snapshot bytes`) plus opt-in `tests/ecc-profile/plan-canvas-live.integration.test.ts` (`materializes only the complete authenticated source closure`, `enforces loopback request guards and returns a revision-bound verdict`). |

| <a id="CM-44"></a>CM-44 | The internal ECC profile lifecycle converts an authenticated native projection into deterministic preview-first install, exact-pin update, repair, rollback, and uninstall plans. Ownership is recorded per destination and bound to the canonical project root, source closure, projection digest, source paths, normalized and installed hashes, merge strategy, previous state, and capability owner. Existing unowned files, foreign or malformed receipts, path escapes, links, case ambiguity, and modified managed bytes fail closed. Operator TOML outside the AIH-managed block is preserved. Apply-time pins cover writes, removals, and authorizing ownership state, so drift rolls the complete filesystem transaction back. This seam is internal and does not expose a user command by itself. | `src/ecc-profile/lifecycle.ts`, `src/internals/plan.ts`, `src/internals/execute.ts`, and `src/internals/fsxn.ts`. | `tests/ecc-profile/lifecycle.test.ts` (`previews without writes, then installs transactionally and deterministically`, `updates only owned bytes and rolls back to the prior projection`, `repairs a missing owned file but refuses to overwrite modified owned bytes`, `uninstalls matching owned bytes while preserving unrelated operator TOML`, `binds uninstall to the exact authenticated projection`, `pins the authorizing ownership receipt during repair`, `pins writes and rolls the whole transaction back on apply-time drift`, `pins removals and rolls the whole transaction back on apply-time drift`), `tests/ecc-profile/lifecycle-integration.test.ts` (`installs, repeats, and uninstalls the complete derived projection`), and `tests/internals/fsxn.test.ts` (`restores a removal target when its apply-time content pin no longer matches`, `rolls back staged writes when an asserted authority file changed`). |
| <a id="CM-45"></a>CM-45 | The supported ECC lifecycle installs field-owned Claude/Codex native hook and MCP registration alongside the authenticated projection. Runtime launchers and fragments are hash-bound in a project receipt; shared JSON/TOML preserves unrelated operator entries; conflicts, unsafe roots, links, and managed drift fail closed. The hidden launcher normalizes bounded native hook input, dispatches repository protection, MCP-health, and continuity handlers, and returns only client-native output. Serena is constrained to its exact pin, canonical project, isolated state, offline launch, credential-scrubbed environment, hardened configuration, and protocol-level tool allowlist. Projection and native-registration install/update actions are composed and preflighted under one filesystem transaction, so a registration preflight failure produces no completed projection; the compound lifecycle uses receipt-bound per-file ownership and content pins instead of treating managed output as generic worktree dirt, while repair and rollback likewise preflight projection and registration before applying either. A path-independent parity receipt accounts every authenticated Claude/Codex skill, role, and workflow mapping plus the native hook/MCP policy. | `src/ecc-profile/native-registration.ts`, `src/ecc-profile/native-runtime.ts`, `src/ecc-profile/native-runtime-cli.ts`, `src/ecc-profile/command.ts`, and `src/ecc-profile/parity-receipt.ts`. | `tests/ecc-profile/native-registration.test.ts`, `tests/ecc-profile/native-runtime.test.ts`, `tests/ecc-profile/parity-receipt.test.ts`, `tests/ecc-profile/actual-source.test.ts`, `tests/ecc-profile/native-smoke.test.ts`, and `tests/ecc-profile/command.test.ts` (`uses receipt-bound ownership for the compound lifecycle in a real Git worktree`, `rolls back the compound install when native registration preflight fails`, `preflights projection and native %s recovery before applying either plan`). |

| <a id="CM-46"></a>CM-46 | `aih policy evaluate <root> --no-log --json` resolves each requested governed candidate against immutable MCP/hook identity, externally verified evidence or approval, unwaivable danger findings, actual target coverage, ownership, and an available AIH projector. Custom proof comes only from the fixed receipt after `gh attestation verify` against an out-of-band organization repository/workflow registry; the governed checkout and policy JSON cannot establish authority. Custom stdio identities remain curated but hard-blocked. A remote identity records an exact HTTPS origin, administrator, authentication mode, permitted data classes, administrator-managed `approved`/`revoked` status, and an explicit no-content-scan marker; legacy schema-v2 digest/verdict records remain readable, but `drifted` is not a live check. Remote candidates remain hard-blocked with `missing-projector`: AIH never contacts or launches them. Governance exclusively owns AIH MCP and usage-hook projection; generic MCP/usage/workspace writers are suppressed or fail closed, governed ECC materialization strips MCP and host-hook/runtime operations across core, platform, and full scope while retaining eligible agents, skills, and commands, and `governance.supportedClis` is an organization sanction gate: Enterprise requires a non-empty explicit registry list, Vibe permits absence, and a present list refuses unsanctioned selected, detected, or marker-derived CLI targets by name at either posture. Materialization is a separate capability gate, so a sanctioned but incapable target names that gate rather than being silently dropped. Hook teardown removes only receipt-proven unchanged entries. A configuration entry alone is not active; framework intents are report-only and hard-blocked rather than creating ECC/Superpowers agents, skills, commands, or bindings. | `src/org-policy/authority.ts`, `src/org-policy/schema.ts`, `src/org-policy/effective.ts`, `src/org-policy/runtime.ts`, `src/org-policy/evaluate.ts`, `src/org-policy/project.ts`, `src/internals/cli-detect.ts`, `src/doctor.ts`, and `src/report/local.ts`. | `tests/org-policy/effective.test.ts` (`binds the complete approval subject while excluding the post-signing transport locator`), `tests/org-policy/governance-projection.test.ts` (`rejects a receipt attested by the governed target repository even if gh reports the digest valid`, `keeps an externally evidenced custom stdio MCP authorable but blocked without an integrity-enforcing projector`, `removes an unchanged AIH-owned hook round trip using its receipt-proven state`), and `tests/org-policy/validate.test.ts` (`fails closed and reports the precise blocked candidate instead of projecting it`), `tests/internals/cli-detect.test.ts` (`allows a selected CLI that the org policy sanctions`, `refuses an explicit selected CLI that the present %s allow-list does not sanction`, `refuses --all-tools when any selected CLI is not sanctioned`, `refuses a detected runnable CLI that is not sanctioned`, `refuses a marker-derived CLI that is not sanctioned`), and `tests/org-policy/org-policy.test.ts` (`accepts a declarative remote record without a tool-surface digest`, `keeps legacy remote tool-surface records parse-compatible`, `accepts an org supported-CLI allow-list from the registry`, `rejects unknown or duplicate org supported-CLI entries`). |

| <a id="CM-47"></a>CM-47 | `aih policy generate --apply` writes a portable Policy Workbench without scanning a target repository. The artifact authors the actual org-policy schema, distinguishes authored intent from target-repository evaluated state, authors sanctioned CLIs independently of activation targets; Enterprise authoring writes an explicit non-empty list while Vibe may omit it; and keeps custom MCP blocked without an activation affordance, with separate pinned-stdio and fenced-remote authoring forms; new remote records state their exact HTTPS origin, approval metadata, administrator-managed `approved`/`revoked` status, and explicit no-content-scan marker; imported legacy digest/verdict records are preserved read-only. The authoring model carries ECC's exact pinned 35-entry MCP source as a separately validated inventory and renders every entry: the 31 ECC-owned entries route to approval authoring, while the four AIH-owned declarations remain read-only availability and do not imply a matching Core control. It separately locks and renders all 286 canonical ECC `skills/*/SKILL.md` identities. Every exact Skill identity is an independently scannable baseline subject and can author source-bound requested selection, but no row thereby claims installation, materialization, evidence, or support; the governed lifecycle remains held until exact evidence clears. Left-rail ECC language, framework, capability, and module choices provide aggregate suggestions; the center inventory remains final authority for each individual Agent and Skill, including deselection, while search-opened detail stays read-only and informative. The flat Ledger paper-and-ink presentation reserves colour for evidence state in light and dark themes; the inspector has one routed next action and no forms or mutation controls, authoring lives in a separate sidebar, and the canonical rail remains available on compact screens. A separate Add MCP sidebar authors exact source-digest-bound `governance.eccMcpApprovals` records only; HTTPS-configurable entries still require an explicit later one-client CLI action, manual entries remain permission-only, and the browser makes no endpoint or tool-surface claim. A distinct ECC hook-controls panel is bound to the pinned 42-gated-hook inventory plus one non-disableable wrapper; it authors reversible Minimal/Standard/Strict profile intent and eligible per-hook disables without claiming AIH executes the hooks. A separate inert adoption panel presents the first bounded, source-validated five-role routing recipe with prerequisites, overlap boundaries, supported next routes, and truthful local usage-attribution limits; it adds no inventory row, authoring control, exported policy state, authority, effect, or generic recipe-distribution plane. ECC/Superpowers Agent and Skill center selections are stored as exact external selections; explicitly curated Agent, Skill, and command intent remains report-only external curation. Imported authority data is preserved for preflight; the workbench does not verify receipts or create effective approval. | `src/org-policy/catalog.ts`, `src/org-policy/ecc-mcp-catalog.ts`, `src/org-policy/ecc-mcp-catalog.snapshot.json`, `src/org-policy/ecc-skill-catalog.ts`, `src/org-policy/ecc-skill-catalog.snapshot.json`, `src/org-policy/ecc-hook-controls.ts`, `src/org-policy/adoption-recipe.ts`, `src/org-policy/generate.ts`, `src/org-policy/studio-model.ts`, `src/org-policy/studio-template.ts`, `src/org-policy/schema.ts`, and `src/org-policy/effective.ts`. | `tests/org-policy/generate.test.ts` (`creates a portable workbench in a temporary fixture without inspecting a repository`, `uses the actual policy grammar for exact semantic import/export`, `rejects stale managed MCP authority even when its synchronized gate is present`, `round-trips administrator-managed remote status without inventing legacy drift fields`, `has semantic controls and a usable accessible help interaction in the generated DOM`), `tests/org-policy/ecc-mcp-catalog.test.ts` (locks the upstream pin, path, SHA-256, ordered ids, transport split, AIH-owned exclusions, and fail-closed placeholder classifications), `tests/org-policy/studio-catalog-inventory.test.ts` (`renders every source-locked ECC skill and external MCP availability row`, `keeps every ECC Skill under center-panel authority`, `binds every selectable ECC Skill to the authoritative lifecycle catalog`), `tests/org-policy/studio-adoption-recipe.test.ts` (`assigns exactly one bounded owner and route to every adoption question`, `fails closed when the selected profile or real core catalog drifts`, `fails closed when routed profile identities overlap selected and disabled sources`, `renders a separate escaped, inert panel without altering authored policy or ticker counts`), `tests/org-policy/studio-navigation-mcp-sidebar.test.ts` (`maps every left-rail ECC selection to the same canonical row in the main inventory`, `opens rail-owned detail through search without offering another mutation path`, `authors and removes a pinned ECC MCP approval without an installation claim`), `tests/org-policy/studio-ecc-hook-profile.test.ts` (`records the pinned profile and reversibly disables only eligible pinned hooks`), `tests/org-policy/studio-ledger-identity.test.ts` (`uses paper, ink, flat rules, evidence colours, and bounded accessible motion`, `keeps the inspector pure and retains the canonical selection rail on compact screens`, `narrates state in the mutation-free inspector and routes its one next action elsewhere`), `tests/org-policy/effective.test.ts` (`binds the complete approval subject while excluding the post-signing transport locator`), `tests/config/json-schema.test.ts` (`preserves signed approval clarifications and report-only external curation intent`), and `tests/org-policy/studio-relations.test.ts` (`authors sanctioned CLIs independently of policy activation targets`). |

| <a id="CM-48"></a>CM-48 | A governed policy may declare `governance.hookRegistrations`, and AIH emits every declared entry into the Claude hook configuration it owns while third-party runtimes stay the executors. It may separately declare source-locked `governance.eccHookControls`; AIH merges only receipt-owned `ECC_HOOK_PROFILE` and `ECC_DISABLED_HOOKS` values into Claude `settings.json.env`, while ECC executes and enforces them after process spawn. A third-party command is transported byte-for-byte: nothing parses, wraps, or re-emits it, and the only value AIH derives from it is the hash that proves it did not change. A launcher whose hash no longer matches its pin, a duplicated registration id, and registrations drawn from two harnesses at once are all refused when the policy is parsed. Provenance is administrator-declared rather than inferred, so an unattributable launcher stays `owner: unknown` with the captured bytes still hash-pinned; adoption reads those bytes from the destination and never from the declaration. Projecting onto a destination carrying entries AIH did not emit is refused with each named by owner and event, on every projection rather than only the first. Uninstall subtracts the receipt-owned `hooks` key, including third-party entries whose own source ships no removal path, preserves every other key, and never replays recorded prior bytes. A policy whose usage-hook selection conflicts with hook registrations or ECC controls at the same destination is refused instead of silently collapsing to one writer. Hook controls and the registrar share one guarded destination snapshot, preserve operator siblings, refuse unreceipted collisions or receipt drift, and record ownership after the settings write. The registrar panel remains a read-only projection view; the separate hook-controls panel authors reversible profile and disable intent, and both state that a source-disabled hook still spawns a process. | `src/org-policy/schema.ts`, `src/org-policy/ecc-hook-controls.ts`, `src/org-policy/ecc-hook-controls-projection.ts`, `src/org-policy/ecc-hook-controls-receipt.ts`, `src/org-policy/hook-registrar.ts`, `src/org-policy/project.ts`, `src/org-policy/effective.ts`, `src/org-policy/catalog.ts`, `src/org-policy/studio-template.ts`, and `src/uninstall/index.ts`. | `tests/org-policy/hook-registration-grammar.test.ts` (`accepts hook registrations as a governance sibling field, launchers byte-for-byte`, `refuses a launcher whose hash no longer matches its pin at parse time`, `refuses hook registrations drawn from both harnesses at once`), `tests/org-policy/hook-registrar.test.ts` (`projects a third-party launcher byte-for-byte`, `never parses, wraps or re-emits a third-party command`), `tests/org-policy/hook-adoption.test.ts` (`captures the launcher byte-for-byte and hashes those exact bytes`, `leaves an unattributable entry owner unknown, hash still pinned`, `names each unowned entry by owner and event when projection refuses`), `tests/org-policy/governed-hook-wiring.test.ts` (`emits the registrar's actions for a policy that declares registrations`, `emits revocation actions for a policy that declares none`, `refuses two hook writers into the client destination rather than dropping one`, `refuses usage-hook and controls-only writes into the same destination`), `tests/org-policy/ecc-hook-controls.test.ts` (`binds all reviewed source files and the exact 43-row, 42-gated active-pin inventory`), `tests/org-policy/ecc-hook-controls-projection.test.ts` (`passes the controls-validated snapshot into the registrar write`), `tests/uninstall/hook-registrar-revocation.test.ts` (`removes third-party entries the third party cannot remove, operator content intact`), `tests/org-policy/studio-hook-registrar.test.ts` (`keeps the registrar inventory read-only while ECC controls remain a distinct surface`, `pins the ticker's per-owner totals to the DOM rows it counts`, `never describes hook usage metering as cost`), and `tests/org-policy/acceptance-hook-registrar.test.ts` (`author → project → panel → mutate → drift by owner and event → uninstall`). |

| <a id="CM-49"></a>CM-49 | `governance.eccMcpApprovals` records an administrator's approved or revoked decision for one exact external entry from the pinned ECC MCP catalog. Every record binds the raw catalog source digest, administrator, authentication mode, and permitted data classes; AIH-owned, unknown, duplicate, malformed, or source-mismatched records fail closed. The resolver returns only declarative seat-Add authority state. It creates no candidate, effective activation, projector action, client configuration, endpoint contact, scan, installation, or tool-surface observation. | `src/org-policy/ecc-mcp-approval.ts`, `src/org-policy/ecc-mcp-catalog.ts`, `src/org-policy/schema.ts`, and `src/org-policy/effective.ts`. | `tests/org-policy/ecc-mcp-approval.test.ts` (`accepts an exact approval record without creating an activation or projector`, `rejects duplicate IDs and resolves malformed records fail-closed`) and `tests/config/json-schema.test.ts` (`models source-locked ECC external MCP approvals as declarative records`). |

| <a id="CM-50"></a>CM-50 | `aih ecc mcp add/remove` turns one declarative ECC MCP approval into an explicit receipt-owned write only when the operator selects exactly one CLI. Add reads the target-root policy, requires an approved HTTPS-configurable ECC catalog id at the pinned raw source digest, enforces a present `governance.supportedClis` sanction, preserves unrelated config, writes the client entry before the ownership receipt, and pins both writes against plan-to-apply races. Remove is receipt-bound cleanup: malformed, missing, unsafe, absent, altered, or revoked state is report-only, and only an unchanged receipt-owned entry is subtracted. Supported native configs are project-local JSON for Claude, Cursor, Copilot, Kimi, and Kiro; execution-time trusted-HOME containment and no-symlink-parent guards protect global JSON for Antigravity, Gemini, Windsurf, OpenCode, and Zed plus Codex TOML. Doctor reports clean, absent, altered, revoked, malformed, and unsafe local receipt/config state only. The lifecycle does not contact endpoints, scan remote tool lists, attest behavior, install all approved MCPs, or infer approval from config presence. | `src/ecc/index.ts`, `src/ecc/mcp-explicit-add.ts`, `src/ecc/mcp-explicit-add-receipt.ts`, `src/doctor.ts`, `src/org-policy/ecc-mcp-approval.ts`, `src/org-policy/ecc-mcp-catalog.ts`, `src/mcp/render.ts`, `src/internals/plan.ts`, `src/internals/execute.ts`, and `src/internals/fsxn.ts`. | `tests/ecc/mcp-explicit-add.test.ts` (`adds and removes %s under an explicit guarded HOME`, `adds and removes only an exact receipt-owned Codex table`, `keeps a selected Codex table exact among direct siblings and nested subtables`, `refuses global %s target and parent symlink swaps at apply`, `reports every explicit receipt state without contacting a server`) and `tests/ecc/mcp-explicit-add-command.test.ts` (`uses the explicit temporary HOME for Gemini config without touching project MCP settings`) and `tests/doctor-explicit-ecc-mcp.test.ts` (`uses canonical USERPROFILE before HOME for global receipt-owned configs`, `reports local clean, absent, altered, revoked, malformed, and unsafe-path receipt states per target`). |

| <a id="CM-51"></a>CM-51 | Package Graph Schema v1 is a public, strict TypeScript contract for human-readable namespaced surface and package ids, provider-neutral repository identity, separate immutable source digests, direct surface-only composition, and independent declared and observed risk. Observed evidence binds its own SHA-256 and the exact subject digest; duplicate identities, unsafe rendered identity text, nested packages, and unresolved members fail closed. The shipped JSON Schema covers the portable editor structure and explicitly directs consumers to the TypeScript parser for cross-record semantic invariants; the graph is metadata, never approval, evidence, or a replacement authority. | `src/capability/package-graph/schema.ts`, `src/capability/package-graph/index.ts`, `src/config/json-schema.ts`, `src/index.ts`, and `schemas/aih-package-graph.schema.json`. | `tests/capability/package-graph-schema.test.ts` (`parses human-readable surfaces, shallow packages, and separate risk records`, `rejects package-to-package composition`, `rejects unresolved direct surface references`, `rejects observed evidence reused across source digests`, `rejects unsafe detector version identity %#`, `keeps every validation issue message free of controls and directional formatting`, `rejects duplicate graph, member, and risk identities`, `rejects unknown fields at every object boundary`), `tests/config/json-schema.test.ts` (`emits editor schemas for config, governed policy, authority receipt, decision, observation, qualification evidence, and package graph`, `publishes strict Package Graph v1 editor validation`), and `tests/config/package-graph-package.test.ts` (`ships and exports the committed Package Graph JSON Schema`). |

| <a id="CM-52"></a>CM-52 | The Package Graph additive index retains every catalog, lock, or receipt claim, including identical claims, and reports only divergent definitions without selecting a winner. Each authority keeps a caller-asserted source-store SHA-256, whose exact-byte computation belongs to an authority adapter, and a separate builder-owned, domain-separated digest of its normalized graph projection; the pure builder never treats either as approval or evidence. Authority references, claim identities and digests, projection digests, canonical ordering, and the exact conflict set are revalidated before serialization. Equal inputs emit byte-identical JSON, built indexes are recursively frozen, and non-JSON or executable object behavior is rejected at the canonical hashing boundary. | `src/capability/package-graph/build.ts`, `src/capability/package-graph/canonical.ts`, and `src/capability/package-graph/index.ts`. | `tests/capability/package-graph-index.test.ts` (`keeps asserted source digests separate from builder projection digests`, `retains empty authorities in the canonical top-level list`, `deep-freezes output and isolates it from caller input mutation`, `retains and orders every authority claim, including identical claims`, `reports only divergent canonical claims and never selects a winner`, `emits byte-identical output for shuffled inputs, arrays, and object insertion order`, `rejects forged or stale index integrity before serialization`, `rejects non-JSON canonical input: %s`, `never executes an enumerable hostile toJSON function`, `rejects enumerable accessors before invoking them`). |

| <a id="CM-53"></a>CM-53 | Package Graph authority adapters hash and strict-parse the same exact bytes: shipped baseline rows join the evidence lock by source, pin, component, and path set; granular ECC agent/rule packages remain lock projections; ECC materialization rows join that baseline by repository, pin, evidence, and component tree; pinned HTTPS ECC MCP packages remain catalog claims until an exact explicit-add receipt supplies the separate receipt authority; and skill lock/pack rows accept only exact GitHub `owner/repo@<lower-40-sha>` identity. Fatal input suppresses that authority document rather than emitting a partial claim. Conflicts have no winner, and the adapters perform no writes, installation, approval, network contact, or support inference. | `src/capability/package-graph/adapters/baseline.ts`, `src/capability/package-graph/adapters/ecc-domains.ts`, `src/capability/package-graph/adapters/ecc-materialization.ts`, `src/capability/package-graph/adapters/github.ts`, `src/capability/package-graph/adapters/skills.ts`, `src/capability/package-graph/adapters/residue.ts`, `src/baseline-evidence/vendor.ts`, `src/ecc/materialization-receipt.ts`, and `src/capability/package-graph/index.ts`. | `tests/capability/package-graph-baseline-adapters.test.ts` (`projects every shipped catalog from the exact committed vendor-lock bytes`, `makes exact lock formatting part of authority identity, not semantic projection`, `rejects a jointly forged authorization and provenance commit`), `tests/capability/package-graph-ecc-domain-adapter.test.ts` (`derives granular agent, rule, and MCP packages from the exact baseline lock`, `projects only HTTPS-configurable MCP packages from the pinned ECC MCP catalog`, `projects exact MCP receipt claims without creating packages`), `tests/capability/package-graph-skill-adapters.test.ts` (`hashes exact authority bytes while stable semantic projections ignore JSON formatting`, `rejects malformed lock input at document granularity with no partial lock claim`, `retains a catalog-only ref as a catalog claim with an explicit diagnostic`), and `tests/capability/package-graph-residue.test.ts` (`registers only exact immutable lock or receipt matches`, `classifies every mixed exact and divergent authority set as divergent without a winner`, `keeps mutable local, remote MCP, and identity-free observations unsupported without leaks`, `emits byte-equivalent deterministic output for input permutations`). |
| <a id="CM-54"></a>CM-54 | Strix is modeled through a separate external security-detector contract, never as a static `TrustDetector`. The contract pins the verified Strix v1.5.2 source, immutable sandbox image index, and exact Linux platform manifests; preflight accepts only the exact installed CLI version and an already-present image whose index, platform, and runnable manifest all match. Bounded fatal-UTF-8 parsing rejects unknown or hostile output as a whole, maps headless exits 0/2/1 to no-findings/findings/indeterminate without claiming safety, redacts PoC-bearing fields, and emits deterministic control-free typed findings. The strict org-policy block permits only local fixtures, forces telemetry off, denies live targets and mounts, and shares hard ceilings of $10, 20 turns, and five minutes with the evidence grammar. Evidence build packages separately produced records from `.aih/security/strix` as their own versioned kind only after bounded, fatal-UTF-8, strict whole-document validation; publication does not claim AIH executed Strix. This foundational layer performs no scan, pull, install, posture enforcement, or trust/baseline/vet registration. | `src/security/detectors/types.ts`, `src/security/detectors/strix.ts`, `src/security/detectors/strix-image.ts`, `src/security/index.ts`, `src/org-policy/schema.ts`, `src/org-policy/effective.ts`, `src/evidence/manifest.ts`, `src/evidence/build.ts`, `src/index.ts`, and `schemas/aih-org-policy.schema.json`. | `tests/security/detectors/strix-preflight.test.ts` (`proves the exact installed CLI, index, runnable manifest, and platform`, `fails closed for %s`, `does not inspect Docker after a failed, noisy, or ambiguous version probe`), `tests/security/detectors/strix-contract.test.ts` (`normalizes strict findings deterministically and redacts PoC and private payloads`, `maps source exit semantics and enforces cross-field consistency`, `rejects Unicode format and line-separator controls while allowing prose line breaks`, `does not execute inherited toJSON while computing finding fingerprints`, `keeps typed evidence strict and cross-field safe`), `tests/org-policy/strix-grammar.test.ts` (`preserves old policies and parses a bounded local-only Strix declaration`, `rejects %s`, `keeps required declarative because no posture enforcement consumer exists`), `tests/config/json-schema.test.ts` (`publishes strict bounded local-only Strix policy inputs`), `tests/security/strix-public-export.test.ts` (`exports the separate security detector contract from the library root`), and `tests/evidence/build.test.ts` (`publishes Strix as its own versioned kind only after strict typed validation`, `fails closed instead of publishing malformed or payload-bearing Strix evidence`, `fails closed on non-UTF-8 Strix evidence bytes`, `rejects hostile Strix entry names without echoing control or format characters`, `bounds Strix evidence reads and fails closed when a discovered file is unreadable`). |
| <a id="CM-55"></a>CM-55 | Governed ECC materialization accepts the single `kiro` target only for evidence-passed selected agents with exact pinned Kiro mappings, skills, and `baseline:rules`. The verified-source adapter re-hashes the exact selected catalog paths and pinned Kiro runtime tree, requires current unheld selected and `runtime:ecc-kiro` content authorization, and emits only direct `.kiro/agents/<name>.md`, `.kiro/agents/<name>.json`, `.kiro/skills/<name>/SKILL.md`, and top-level `.kiro/steering/*.md` bytes. Kiro's custom-agent documentation, re-verified 2026-08-13, records both agent formats as workspace definitions available to IDE 1.x and CLI 3.x; AIH copies the exact selected source Markdown for the IDE representation and the exact curated JSON for CLI configuration under one component receipt, without synthesizing either or claiming a live-host probe. The curated JSON must identify the selected agent, contain no duplicate keys, and contain no non-empty MCP or hook configuration; an unmapped agent is refused by name without vetoing mapped components. The planner admits Kiro paths only through an unforgeable in-process proof whose private snapshot binds the exact bytes, selected authorization, runtime authorization, and provenance; it incrementally bounds collision inventory and refuses pre-existing same-name definitions and case- or Unicode-folded spellings before mutation. The receipt records the applicable evidence for both representations, deterministic reapply is byte-stable, and narrowing or uninstall subtracts only unchanged receipt-owned files. Kiro identity variants, arbitrary caller-supplied `.kiro` files, nested or non-regular source entries, ECC hooks/settings/scripts, and the native installer remain outside this capability. | `src/ecc/materialization-target-kiro.ts`, `src/ecc/materialization-target.ts`, `src/ecc/governed-lifecycle.ts`, `src/ecc/materialization-plan.ts`, `src/ecc/materialization-receipt.ts`, and `src/ecc/materialization-types.ts`. | `tests/ecc/materialization-target-kiro.test.ts` (`projects the exact IDE Markdown and CLI JSON agent mappings under one Kiro target`, `refuses Kiro agents whose curated JSON identity or excluded runtime fields are unsafe`, `projects only the selected exact Kiro skill and direct steering bytes`, `joins every selected component to one exact current unheld authorization`, `requires exactly one unheld runtime:ecc-kiro authorization`, `revalidates selected and runtime tree hashes before projecting bytes`), `tests/ecc/materialization-target.test.ts` (`projects selected agents, skills, and steering through the verified Kiro adapter`, `requires current runtime evidence for an agent-only Kiro selection`, `refuses an unmapped agent by name without vetoing a mapped Kiro agent`, `refuses Kiro variants and every CLI outside the single governed Kiro identity`), `tests/ecc/materialization.test.ts` (`bounds Kiro agent collision inventory before accepting an oversized directory`, `refuses forged or mutated adapter proof objects`, `never invokes branded file accessors or rereads enclosing component accessors`), `tests/ecc/governed-lifecycle-command.test.ts` (`applies selected agents, skills, and steering with dual evidence`, `is deterministic when the selected Kiro agent is reapplied`, `refuses a same-name operator Markdown agent without changing either representation`, `refuses a case-folded operator JSON agent on case-sensitive filesystems`, `uninstalls only the unchanged receipt-owned Kiro agent variants`, `subtracts a genuinely deselected Kiro surface`, `preserves prior Kiro ownership when runtime evidence is absent or held`), and `tests/ecc/acceptance-governed-lifecycle.test.ts` (`Kiro IDE and CLI variants share one governed target`). |

| <a id="CM-56"></a>CM-56 | The internal verified-promotion handoff accepts only the exact cleared GitHub acquisition gate registered by the workspace trust flow. Issuance replays the current scan between two exact source snapshots, joins every selected skill to the committed approval repository, pin, and optional curated source scope, and re-pins strict bounded trust-lock and approval-lock bytes before returning an opaque handle. Reads from that handle return fresh copies of source-derived buffers and preimages; copied, spread, forged, or cross-channel handles do not confer authority. This handoff performs no writes and does not claim installation, adoption, custody, or package-manager execution. | `src/workspace/acquire.ts`, `src/workspace/verified-promotion.ts`, `src/workspace/promotion-snapshot.ts`, `src/skill/lockfile.ts`, and `src/trust/lock.ts`. | `tests/workspace/verified-promotion.test.ts` (`does not accept a handle forged outside its issuing channel`, `validates hashes and returns fresh deeply isolated snapshots`, `rejects accessors and proxies without invoking hostile input`, `issues only from a genuine cleared GitHub gate and uses authoritative scan findings`), `tests/skill/lockfile.test.ts` (`reads strict exact committed bytes without accepting unknown entry fields`, `rejects folded duplicate approvals and hard-linked authority files`, `rejects a committed approval replaced between inspect and open`), and `tests/trust/lock.test.ts` (`returns copied exact bytes and a digest for strict valid state`, `rejects invalid UTF-8, folded duplicate identities, artifacts, and oversized bytes`, `rejects a trust-lock replaced between inspect and open`). |
| <a id="CM-57"></a>CM-57 | `aih capability package list/show/status/doctor` and add/update/remove preview resolve one typed local view from exact policy, approval, evidence, catalog, Package Graph, intent, ownership, custody, and domain-receipt bytes without writes, processes, network, acquisition, or component loading. Explicit `--apply` reconciles policy-selected GitHub skill packs already present in the promotion store, existing receipt-owned ECC agent/rule materialization, and explicitly added HTTPS ECC MCP configuration. Add/update assert current member bytes and publish intent, one content-addressed custody record per domain, then ownership. Removal requires policy deselection and exact current custody, subtracts only unchanged last-owned files, preserves shared members and unrelated state, and issues successor custody for retained packages. Authorities and final local state are re-read inside the ordered transaction; failure uses compensating rollback and never claims crash atomicity, filesystem isolation, component execution, or external acquisition. | `src/capability/package-manager/commands.ts`, `src/capability/package-manager/live-context.ts`, `src/capability/package-manager/domains/skill-pack-coordinator.ts`, `src/capability/package-manager/domains/mixed-coordinator.ts`, `src/capability/package-manager/owned-files.ts`, `src/capability/package-manager/custody-receipt.ts`, `src/capability/package-manager/domains/skill-pack-installed.ts`, `src/internals/owned-file-transaction.ts`, and `src/commands/index.ts`. | `tests/capability/package-manager-commands.test.ts` (`uses one typed report for list/show/status/doctor and emits only local digests`, `dispatches --apply through the registered command without acquisition or run-ledger writes`), `tests/capability/package-manager-command-dispatch.test.ts` (`routes add/update through the closure-wide coordinator`, `routes a single-domain ECC removal through the closure coordinator`, `routes a single-domain ECC MCP removal through the closure coordinator`), `tests/capability/package-manager-skill-pack-coordinator.test.ts` (`publishes exact ownership and custody from the existing promotion receipt`, `preserves drifted member bytes and package ownership on removal`), `tests/capability/package-manager-mixed-coordinator.test.ts` (`publishes one ownership receipt and both domain custodies in one transaction`, `reconciles a sole ECC package through add and final removal`, `reconciles a sole MCP package through add and final removal`, `issues successor ECC custody when removing one agent while retaining another`, `issues successor skill custody when removing one skill pack while retaining another`, `issues successor MCP custody when removing one MCP while retaining another`, `refuses a skill custody receipt with an extra deletion path`, `refuses an ECC custody receipt with an extra member file`, `refuses an MCP custody receipt with an extra member file`), and `tests/internals/owned-file-transaction.test.ts` (`snapshots commit guards and rolls back when afterEffects refuses`, `supports explicit assertions without effects and enforces optional modes`). |
| <a id="CM-58"></a>CM-58 | Kiro has one public target id, `kiro`, while runnable CLI detection is bound to the documented `kiro-cli` executable rather than the optional `kiro` IDE/CLI router; `KIRO_HOME` overrides the default global configuration root for detection and reporting. Standalone Kiro v1 `.kiro/hooks/*.json` projection is gated by the explicit, persisted `--kiro-hook-runtime ide1-cli3` capability because binary detection cannot distinguish CLI 2 from CLI 3 mode. Unknown runtimes and CLI 2 stay advisory, embedded custom-agent hooks are never mutated, reserved hook-name collisions refuse, and prune/uninstall treat current and legacy hook names as manual advisories without per-file ownership evidence. Adoption inventories Kiro steering, hooks, custom-agent definitions, skills, prompts, settings, and specs. Every `.kiro/agents/**` definition, including Markdown, and settings remain runtime configuration and are never auto-migrated. Policy authoring can target Kiro for AIH-reviewed stdio MCP controls; unsupported remote candidates fail closed instead of disappearing during projection. Projection merges only selected server names into `.kiro/settings/mcp.json` under a separate `kiroMcpProjection` receipt, preserves unrelated servers, comments, formatting, and top-level keys through nested JSONC edits, refuses duplicate keys and unreceipted collisions, keeps Claude and Kiro receipts through coalesced marker writes, converges idempotently, and subtracts only exact unchanged owned entries on deselection, prune, or uninstall; malformed, drifted, non-file, or unsafe paths revoke the claim without mutating the workspace config. This is workspace distribution, not managed enforcement, because a custom agent can override or decline workspace MCP inheritance. It does not reuse Claude managed settings, project unsupported governed hooks, or expand governed ECC Kiro materialization beyond CM-55's verified agents, rules, and skills boundary. | `src/internals/cli-registry.ts`, `src/internals/cli-detect.ts`, `src/kiro/content.ts`, `src/kiro/runtime.ts`, `src/kiro/hook-projection.ts`, `src/bootstrap-ai/index.ts`, `src/usage/hooks.ts`, `src/usage/hook-health.ts`, `src/adopt/cli-footprint.ts`, `src/adopt/migrate-cli.ts`, `src/config/marker.ts`, `src/mcp/kiro-managed-projection.ts`, `src/mcp/projection-marker.ts`, `src/org-policy/schema.ts`, `src/org-policy/catalog.ts`, `src/org-policy/effective.ts`, `src/org-policy/project.ts`, `src/org-policy/evaluate.ts`, `src/prune/detect.ts`, `src/prune/index.ts`, `src/uninstall/index.ts`, and `src/report/cli-coverage.ts`. | `tests/internals/cli-detect.test.ts` (`detects the public Kiro target through the documented kiro-cli executable`), `tests/kiro/content.test.ts` (`uses current standalone JSON paths and PascalCase file triggers`), `tests/bootstrap-ai/bootstrap-ai.test.ts` (`--cli kiro generates current standalone v1 JSON hooks only for an explicit IDE1/CLI3 runtime`, `refuses to overwrite an operator hook that uses an AIH-reserved Kiro filename`), `tests/adopt/cli-footprint.test.ts` (`keeps every Kiro custom-agent definition as operator-owned runtime configuration`), `tests/adopt/migrate-cli.test.ts` (`never migrates Kiro custom-agent definitions, including Markdown`), `tests/org-policy/governance-projection.test.ts` (`projects a Kiro-only reviewed MCP through its separate workspace receipt`, `keeps both Claude and Kiro ownership receipts through one activation and deactivation`), `tests/mcp/kiro-managed-projection.test.ts` (`refuses a newly selected server collision while updating an existing receipt`, `preserves unrelated JSONC server bytes while changing an owned entry`, `refuses duplicate Kiro MCP keys instead of rewriting an ambiguous JSONC map`, `refuses duplicate keys inside a Kiro-owned server entry`, `removes only unchanged owned entries when deselected and preserves the workspace file`, `revokes ownership without mutating altered or non-file workspace settings`, `prune and uninstall subtract only unchanged receipt-owned Kiro server names`), `tests/prune/detect.test.ts` (`flags only exact current or legacy aih Kiro hooks, never user/team hooks`), `tests/uninstall/uninstall.test.ts` (`keeps exact current and legacy Kiro hook names advisory without touching lookalikes`), and `tests/report/cli-coverage.test.ts` (`Kiro MCP wiring reports only project configuration, not custom-agent enforcement`). |
| <a id="CM-59"></a>CM-59 | Dormant internal V1 catalog-binding, resolution, admin-distribution, and admin-catalog-fetch contracts strictly parse bounded canonical `CatalogSnapshotV1`, artifact, and cache-record bytes and admit selected material only through the signed head/snapshot resolution path. The pure admin composer derives an immutable resolved binding from that hidden verified material, requires distinct catalog-head and admin signer roots, calls independent catalog and admin verifier seams, and emits a canonical `AdminSeatDistributionV1` parser/byte form with a standard DSSE/in-toto envelope whose subject, administrator identity, signature, and root cross-check the binding. The internal exact-pin fetch/cache orchestrator derives its catalog-side authority key from trusted catalog fields, uses injected acquisition, cache, attestation, verifier, and signer seams, recomputes and reattests fetched or cached artifacts, revalidates the signed head, and commits fresh verified-cache bytes before admin signing. It falls through fresh → cached-verified → packaged only on exact `unavailable`; malformed, trust, replay, cache, package, or commit failures are fatal, while unknown compatible versions remain visible as non-materializable `compatibility-required`. These modules stay capability-free themselves: they contain no filesystem, network, provider client, scanner, process, or runtime route and no signing implementation beyond the injected signer seam, and they gain no public export, verdict, acknowledgement, or runtime cutover. Their consumers are the administrator route in CM-60, which owns every effect, and the internal developer-seat consumption foundation documented in CM-61. | `src/org-policy/admin-catalog-fetch-v1.ts`, `src/org-policy/admin-distribution-v1.ts`, `src/org-policy/catalog-binding-v1.ts`, `src/org-policy/catalog-resolution-v1.ts`, and `src/contract/strict-json-v1.ts`. | `tests/org-policy/admin-catalog-fetch-v1.test.ts` (`recomputes the fetched digest, verifies exact authority/cache binding, commits before signing, and returns closed fresh provenance`, `uses only literal unavailable for the verified-cache then packaged fallthrough`, `treats cache mutation, revalidation failure, future age, and trusted-input accessors as fatal without callback leakage`, `remains an internal capability-free foundation with no transport locator or seat/runtime route`), `tests/org-policy/admin-distribution-v1.test.ts` (`derives every binding fact from verified selected head and snapshot material before signing`, `never accepts caller member or digest projections and signs only a fully verified material selection`, `fails closed when an injected signer or independent verifier returns malformed, mismatched, or rejected evidence`, `remains an internal pure composer with no filesystem, Plan, CLI, Workbench, provider, process, or runtime route`), `tests/org-policy/catalog-binding-v1.test.ts` (`binds exact signed catalog material into an immutable, canonically parseable Core-only binding`, `requires a distinct admin-signed DSSE envelope and excludes acknowledgement authority`), and `tests/org-policy/catalog-resolution-v1.test.ts` (`parses only canonical bytes into an immutable, branded, component-sorted snapshot`, `admits selected material only after the existing signed resolution path and never exposes caller-forged state`, `downgrades only explicit unavailable freshness tiers and preserves verified last-good`, `reports compatibility-required without materializing and treats corrupt cache or invalid higher tiers as terminal`). |
| <a id="CM-60"></a>CM-60 | `aih policy generate <admin-root> --apply` resolves the signed supported catalog before rendering; omission preserves the rootless portable artifact, and a non-applying admin-root invocation fails before effects. Enterprise uses only the fixed OS-managed bootstrap; vibe uses the canonical file under the supplied admin root. The strict canonical bootstrap pins catalog and administrator identities, distinct roots, HTTPS locators, exact catalog/promotion/package facts, separate catalog and administrator workflow/bundle identities, and bounded cache policy. The authority-controlled catalog path segment must be real directories, never a symlink or junction, before the bootstrap is read; malformed, noncanonical, traversal-unsafe, credentialed, ported, queried, or non-HTTPS input fails closed. The catalog artifact and pre-signed distribution each complete an exact `gh attestation verify` against their separately pinned workflow and bundle, with derived repo, issuer, ref, and predicate; `gh` is an absolute executable outside the posture-resolved authority root. Failure or cleanup error is unverified and fatal. The workstation never signs; exact outer-attested plus inner DSSE PAE/signature pairs, and exact pre-signed distribution bytes, are required. The contained owner-only cache has no authority by itself. The visible Workbench provenance line renders only verified tier, source, channel, resolved time, download age, and bootstrap provenance. Its embedded safe model additionally carries sequence, digests, posture, and verification time, but no locator, path, token, signature, attestation, signer identity, root digest, or machine detail. No developer-seat, public-export, runtime, or scanner cutover is introduced. | `src/org-policy/admin-catalog-bootstrap-v1.ts`, `src/org-policy/admin-catalog-operations-v1.ts`, `src/org-policy/generate.ts`, `src/org-policy/studio-model.ts`, `src/org-policy/studio-template.ts`, and `src/commands/index.ts`. | `tests/org-policy/admin-catalog-bootstrap-v1.test.ts` (`refuses a symlinked or junctioned catalog root and parent before reading the bootstrap`), `tests/org-policy/admin-catalog-operations-v1.test.ts` (`excludes a gh executable under the posture-resolved enterprise authority root`), and `tests/org-policy/admin-catalog-cli-route.test.ts` (`rejects whitespace-padded admin-root authority text before any effect`). |
| <a id="CM-61"></a>CM-61 | The internal `DeveloperSeatCatalogConsumptionV1` foundation accepts only bounded canonical, pre-transported `AdminSeatDistributionV1` bytes — a fixed 96 KiB transport ceiling is enforced before the verifier ever runs — and independently parses and verifies the current material and any signed prior material against exact trusted, distinct catalog-head and admin-signer roots and admin signer identity, in a deterministic current-then-prior order. A present-but-broken current, whether malformed bytes or a failing signature, is terminal and never falls back to a valid prior. An explicit, exact `{ kind: "unavailable" }` current may select a fully verified prior as last-good; both material sources being absent is fatal. Across a verified current and prior, a higher current sequence advances, a lower current sequence rolls back and fails closed, and an equal sequence requires an identical resolved-catalog-binding digest regardless of any difference in the enclosing distribution envelope. Selected material must not be future-dated relative to the caller-supplied `now` and must fall within a caller-supplied, integer-bounded `maxAgeSeconds` window, both checked at their exact boundary. An unknown compatible schema or effect version returns a closed, frozen `compatibility-required` result that exposes no binding, member, or distribution material, taking precedence for the current material even when a compatible prior exists. This module remains an internal, capability-free foundation with no filesystem, network, provider, process, scanner, or signer route beyond an injected verifier callback, no CLI, Workbench, or public export surface, and no operational seat transport, storage, runtime, verdict, acknowledgement, or cutover behavior. | `src/org-policy/developer-seat-catalog-consumption-v1.ts` and `src/org-policy/catalog-binding-v1.ts`. | `tests/org-policy/developer-seat-catalog-consumption-v1.test.ts` (`verifies a transported distribution and returns a minimal, deeply immutable resolved fact`, `advances on a higher sequence, accepts idempotent equal-sequence replay, and rejects rollback or digest conflict`, `falls back to a fully verified prior only when current is explicitly unavailable, and never when both are absent`, `passes fixed trusted roots and identity through to independent verification in a deterministic current-then-prior order`, `enforces strict UTC-second time bounds: rejects a future resolvedAt and expiry beyond the bounded max age, accepting the exact boundary`, `accepts maxAgeSeconds only as an integer within [60, 31536000], with both exact age boundaries resolving`, `enforces an exact 96*1024-byte transport ceiling for both current and lastGood, rejecting one byte over before the verifier ever runs`, `returns closed compatibility-required for unknown schema/effect versions without exposing binding or member material, taking precedence over a compatible prior`, `treats a present-but-broken current as terminal — malformed bytes or a failing signature — even with a fully valid prior available, and never silently falls back to it`, `keeps the compatibility-required output closed and non-materializing: frozen, no method surface, and immune to post-hoc mutation`, and `remains an internal pure consumer with no filesystem, network, provider, process, signer, CLI, Workbench, or runtime route, and introduces no cutover`). |

| <a id="CM-62"></a>CM-62 | The internal Core-only `resolveOperationalDeveloperSeatCatalogV1` custody adapter reads only fixed `current.json` and `last-good.json` slots below a caller-supplied absolute, normalized authority root. It admits only bounded, regular, single-link exact bytes; the canonical distribution parser rejects alternate leading-BOM transport bytes. The supplied authority-root entry itself cannot be a link, junction, or reparse point; this control does not claim that parent aliases above that boundary are link-free. The adapter passes both snapshots to independent pure verification, then rechecks both slots and the root for every result before it returns or promotes. Only a verified current result is promoted: an exclusive lock and owner-only temporary receive a full write and file fsync, then exact identity and byte revalidation before atomic rename and POSIX directory fsync. Concurrent changes, held locks, filesystem or durability failures, and malformed custody state fail through fixed diagnostics that do not expose a filesystem path, root, or slot name. This is not a transport or runtime route: it adds no network, provider, fetch, scanner, signer, CLI, Workbench, public export, materialization route, or cutover. | `src/org-policy/developer-seat-catalog-operations-v1.ts`, `src/org-policy/developer-seat-catalog-consumption-v1.ts`, and `src/org-policy/catalog-binding-v1.ts`. | `tests/org-policy/developer-seat-catalog-operations-v1.test.ts` (`reads exact bytes with no UTF-8, BOM, or newline normalization`, `fails closed when verification races non-promoting last-good or compatibility results`, `does not promote when current is replaced with different bytes while the verifier runs`, `fails promotion closed against a held lock and never breaks or steals it`, `leaves no scratch file or lock behind on successful promotion and fails closed on custody failure`, `never leaks a filesystem path, root, or slot name into an adapter diagnostic`, `stays an internal custody adapter: no network, process, provider, scanner, signer, CLI, Workbench, or public export`), `tests/org-policy/catalog-binding-v1.test.ts` (`parses only an exact canonical distribution whose DSSE statement binds its branded binding`), and `tests/org-policy/developer-seat-catalog-consumption-v1.test.ts` (`rejects BOM-prefixed transport bytes rather than treating them as canonical distributions`, `passes fixed trusted roots and identity through to independent verification in a deterministic current-then-prior order`). |
| <a id="CM-63"></a>CM-63 | The internal Governance Doctor Audit/Guide foundation consumes capability-free, precomputed diagnostic data only. It bounds and canonicalizes Profile, Audit, and Guide transport bytes; assigns domain-separated identities; maps its closed diagnostic IDs to existing read-only command specs; preserves distinct audit and per-diagnostic refusal states; and renders prose quoted, source-attributed, and non-authoritative. It has no Repair, mutation, consent, command-synthesis, or runtime diagnostic invocation path, and introduces no CLI, Workbench, or public export. The source-only `governance-quality` pack scaffolds its first-party skill, license, and canonical profile with source-asset byte fidelity, but creates no card, lock, report, or approval; destination lifecycle vetting and approval remain required. This exact-text behavior is limited to first-party source assets, not a general binary or public feature. | `src/governance-doctor/capability-v1.ts`, `src/governance-doctor/profile-v1.ts`, `src/governance-doctor/audit-guide-v1.ts`, `src/pack/scaffold.ts`, `src/internals/plan.ts`, and `src/internals/execute.ts`. | `tests/governance-doctor/profile-v1.test.ts` (`round-trips exact canonical UTF-8 bytes`, `rejects oversized and deeply nested transport before parsing unknown content`), `tests/governance-doctor/audit-guide-v1.test.ts` (`does not retain an arbitrary caller callback behind a read-only label`, `round-trips audited and refused records only through their exact canonical bytes`, `binds every audited and refused semantic field into the audit identity`, `binds every available and withheld semantic field into the guide identity`), `tests/pack/scaffold.test.ts` (`ships the governance Audit/Guide source and profile without inventing lifecycle evidence`), and `tests/internals/execute.test.ts` (`apply preserves exact trusted text only through the closed exact builder`). |

| <a id="CM-64"></a>CM-64 | The internal Governance Doctor operational adapter has two code-owned executable diagnostics: it invokes the hard-pinned read-only `aih doctor` and `aih policy evaluate` commands through a frozen allowlisted context, then converts only fixed safe outcomes into the unchanged precomputed Audit/Guide registry. It forwards no caller options, target selection, prompt, progress, or cleanup seams. Records bind root separately and carry a domain-separated safe evaluation-context digest over context directory, posture/source, platform, and host verification state; they do not hash environment or runner values. The adapter accepts only bounded ordinary, `runMany`, structured-legacy, and structured `probe` variants in executor precedence; checks are closed-schema canonical projections with per-check and total bounds, and diagnostic detail is not copied into canonical records. Other frozen diagnostic IDs remain the foundation-derived `missing-adapter` result. The frozen surface table binds each ID to a command spec independently recorded as `readOnly: true`, including that semantic in the surface revision. Policy denial and profile incompatibility still read the supplied policy/profile/context needed to form the refusal, but plan and run no diagnostic. Hostile plans, non-probe actions, rejected probes, malformed output, and oversized action/check outputs become fixed `evidence-gap` results. This outer adapter adds no Repair, mutation, consent, command synthesis, CLI, Workbench, or public export. | `src/governance-doctor/operational-v1.ts`, `src/governance-doctor/audit-guide-v1.ts`, `src/governance-doctor/capability-v1.ts`, `src/governance-doctor/profile-v1.ts`, `src/doctor.ts`, and `src/org-policy/validate.ts`. | `tests/governance-doctor/operational-v1.test.ts` (`snapshots an allowlisted context before branding and passes that exact sanitized snapshot to planning and probes`, `rejects a proxied operational context before any getter runs`, `accepts the closed runMany, structured-legacy, and structured probe variants in executor order`, `runs the unmocked Doctor plan shape without silently downgrading its code-owned variants`, and `binds a safe evaluation-context digest while discarding options and targets`). |

| <a id="CM-65"></a>CM-65 | `aih governance-doctor` is a top-level `readOnly: true`, `zeroWrite: true` route that presents the Governance Doctor Audit and Guide. It is not reachable through, and does not re-enter, `aih doctor`; the internal operational adapter plans and probes that command and `aih policy evaluate` through fixed code-owned seams. It reads exactly one artifact — the canonical profile shipped at `packs/governance-quality/aih-gov-doctor/profile.json` — through a bounded no-follow file boundary with no caller-selectable location, bytes, registry entry, flag, option, environment variable, or positional value. The policy decision and revision are derived in code from the shared-schema-validated org policy and resolved posture; a caller-authored decision, callback, or opaque revision has no input channel, and the presentation boundary accepts only an AIH-minted policy state whose revision and decision bind the operation record. It fails closed on an unreadable org policy and on a resolved posture below the policy `minimumPosture` floor; this posture-scoped read-only verifier honors an explicit valid `--posture` while the organization floor still applies. The adapter is invoked once per run and its guided next action stays `executable: false`: no next action, `aih status`, or Repair is executed. The nested presentation report has one fixed key set across `completed`, `partial`, `evidence-gap`, `refused`, and `unavailable` outcomes; only completed exits zero. The closed report carries identities, states, ids, and quoted source-attributed prose — never raw diagnostic check text, argv, environment values, filesystem locations, process output, support tickets, or run-ledger rows. The route exposes only shared zero-write flags, writes no repository or workstation file, and adds no Repair, Workbench, public library export, or runtime/seat cutover. | `src/governance-doctor/command-v1.ts`, `src/commands/index.ts`, `src/governance-doctor/operational-v1.ts`, `src/internals/fsxn.ts`, and `src/org-policy/schema.ts`. | `tests/governance-doctor/command-v1.test.ts` (`registers exactly one non-recursive top-level read-only zero-write route`, `loads the shipped profile through the fd-guarded bounded file boundary`, `denies a run below the org policy posture floor and never reads a caller decision`, `honors --posture for this posture-scoped read-only route`, `invokes the operational adapter exactly once and never re-enters a Doctor route`, `never plans, runs, or repairs the guided next action`, `refuses proxied, accessor-bearing, and forged inputs without invoking a getter`, `does not present hostile raw diagnostic text from passing or failing probes`) and `tests/contract/command-surface.test.ts` (`keeps the zero-write governance-doctor route free of mutating and artifact flags`). |
| <a id="CM-66"></a>CM-66 | Dormant internal Governance Doctor Repair V1 contracts admit a repair record only through a code-owned mechanical registry and exact branded Audit/Guide/operation/profile joins. Canonical broker, plan, consent, receipt, and verification transports are bounded and reject bytes that do not reproduce their trusted context; a plan requires explicit out-of-band consent, an execution context binds the receipt, and a verification context binds the covering verification. The pure resolver preserves an applied fact only for a coherent receipt and distinguishes `applied-unverified`, verified, expired, failed, mismatched, and replayed states. These capability-free contracts do not execute or mutate: they add no broker implementation, filesystem, process, network, provider, scanner, signer, CLI, Workbench, public export, runtime, seat, persistence, or lifecycle-approval cutover. | `src/governance-doctor/operation-record-v1.ts`, `src/governance-doctor/repair-broker-v1.ts`, `src/governance-doctor/repair-plan-v1.ts`, `src/governance-doctor/repair-consent-v1.ts`, and `src/governance-doctor/repair-outcome-v1.ts`. | `tests/governance-doctor/repair-broker-v1.test.ts` (`parses only bounded canonical registry bytes under the exact trusted registry`), `tests/governance-doctor/repair-plan-v1.test.ts` (`never rehydrates an authorization from plan bytes without matching trusted joins`, `rejects every independently rehashed trusted-join swap`), `tests/governance-doctor/repair-consent-v1.test.ts` (`refuses consent outside the plan validity window`), and `tests/governance-doctor/repair-outcome-v1.test.ts` (`reconstructs receipt bytes only under their exact trusted plan and consent`, `reconstructs verification bytes only under their exact trusted plan and receipt`, `treats expiry as exclusive and preserves applied truth on replay`). |
| <a id="CM-67"></a>CM-67 | The internal Governance Doctor Repair executor applies exactly the four frozen V1 effect kinds through plan-bound custody, and an independent verifier takes a two-part verdict: each effect's plan-declared goal must hold over the live tree and the attempt's own recorded evidence must match those live bytes, so neither the executor's success carried forward nor fabricated evidence that merely echoes an unrepaired tree can manufacture a verified outcome. A plan declares each managed path at most once, so no effect's per-attempt evidence can be made stale by a later effect in the same plan. Durable single use is a per-machine claim store under the OS account's own home, read from the account record rather than from `HOME` or `USERPROFILE`: one granted plan is spent once per canonical checkout root under a fixed digest-derived name, the record is committed before the first effect, caller-supplied replay state is refused, no record is ever removed at any interval, and the store fails closed past a bounded record ceiling rather than making room. Custody creates only the declared leaf, never creates or recreates a parent, refuses an occupied path, and publishes by hard link through a write, flush, displace, link, and retire sequence that replaces nothing it did not create and restores what it displaced, re-proving the bound parent's identity the moment the scratch exists and again immediately before the publish so that a directory swapped in behind a path-based syscall captures at most a stray private scratch and never a publication; attempt evidence is recorded only for effects the receipt reports applied, and an effect the receipt did not apply verifies as failed. Recording that evidence requires the durable claim the run spent, passed by the executor and joined to the receipt's own plan and consent, so local evidence — and therefore any Verification resting on it — cannot exist for a plan no record was ever committed for; a claim that is merely well formed is refused, and exactly one module may mark one spent. That join now covers all three of a claim's bindings. Plan and consent cannot supply the third on their own -- a claim's scope digests the resolved canonical checkout path while a plan's root identity digests the declared strings, so no comparison between them exists -- so the executor states the resolved root it applied the effects in, and the recorder digests it through the claim module's own pure scope rule and refuses a claim spent for any other checkout. The path is trusted only as the caller's statement of where it acted, never as a fact about the filesystem; the digest comparison is what makes a false statement useless rather than merely unverified, so a caller that misstates its own root can only refuse itself. The authority window bounds the attempt rather than only its writes: it is checked before every effect and again by every mutation grant, so a plan expiring between effects halts before the next one whether or not that effect would have written anything. An effect whose goal already holds takes no grant, and before this it therefore read no clock and reported `applied` on a plan whose authority had closed -- zero work, but a Receipt that said otherwise. The whole trusted join is also re-taken in the last instant before the claim is spent, together with the live window, because the claim is irreversible by design: a plan whose authority closes between the preflight and the spend refuses with nothing spent and nothing written, leaving a plan that can be consented again rather than one that is finished. Acquiring a claim is itself many syscalls -- resolving the account home, proving the naming ancestry, enumerating the store against its ceiling -- so the store re-reads the same window from the plan's own bounds and the consent instant immediately before the exclusive create, beside its own last store-identity proof, and refuses before the name is taken. That is bounded rather than atomic like every other proof here: a window closing between that check and the create returning is not observable through these APIs, and what the check removes is the whole-acquisition gap rather than the last syscall. On POSIX the reported home must be a real, canonical, non-link directory this account alone may mutate, and its whole lexical naming ancestry is proved to the filesystem root under a bounded depth ceiling — each ancestor owned by root or by this account and not group- or world-writable, or sticky and naming an entry owned by root or this account, which admits a root-owned `0755` `/home` and a root-owned `01777` `/tmp` — with the home-and-ancestry identity re-proved at each controlled segment boundary, immediately before the exclusive claim create, and again after the read-back. Windows asserts no POSIX mode or uid bound and no ACL substitute for one, defines no `O_NOFOLLOW` so an open follows a reparse point rather than refusing it, and exposes no directory handle to flush; those are recorded limitations, not claims. Every path proof is bounded rather than atomic: a substitution spanning a proof boundary is refused, and one that lands and reverts inside a single syscall is not observable through these APIs. Refusals are fixed labels that interpolate no path, byte, identity, or OS diagnostic. The separate `aih repair` command reaches these modules only through staged live attempt, local TTY consent, and independent verification; there is still no Workbench, public-library export, broker selection UI, network, provider, scanner, signer, process, package, runtime, seat, or lifecycle-approval cutover. | `src/governance-doctor/repair-executor-v1.ts`, `src/governance-doctor/repair-custody-v1.ts`, `src/governance-doctor/repair-verifier-v1.ts`, `src/governance-doctor/repair-claim-store-v1.ts`, `src/governance-doctor/repair-claim-v1.ts`, and `src/governance-doctor/repair-content-v1.ts`. | `tests/governance-doctor/repair-content-v1.test.ts` (`refuses a claim this process never durably spent`, `refuses a spent claim taken for a different plan or consent`, `refuses a claim spent for a different resolved root`), `tests/governance-doctor/repair-capability-boundary-v1.test.ts` (`lets exactly one module mark a claim spent`), `tests/governance-doctor/repair-executor-v1.test.ts` (`halts when the authority window closes before the next mutation`, `halts on an already-satisfied effect once the authority window has closed`, `refuses without spending the claim when authority closes before the spend`, `never widens the frozen V1 effect allowlist`, `applies all four frozen effect kinds and returns a bound applied-unverified receipt`, `refuses to spend one granted consent more than once, before any effect`, `binds the claim to this canonical root, so another checkout's record does not block it`, `rejects caller-supplied replay state instead of treating it as authority`, `preserves every unrelated byte and deletes nothing`), `tests/governance-doctor/repair-custody-v1.test.ts` (`never clobbers a target raced into the name after the read`, `refuses to replace an occupied path`, `never creates an absent parent and never recreates one after a race`), `tests/governance-doctor/repair-custody-interposition-v1.test.ts` (`never unlinks a foreign replacement installed immediately after publication`, `restores the exact prior when validation fails after publication`, `publishes nothing through a parent replaced with a fresh directory before the scratch exists`), `tests/governance-doctor/repair-verifier-v1.test.ts` (`is independent of the executor: a state undone after the attempt reads as failed`, `never verifies fabricated evidence that echoes an unrepaired tree`, `reports exactly failed for every effect the receipt did not apply`, `reports unavailable for a transported receipt that carries no local evidence`), `tests/governance-doctor/repair-claim-store-v1.test.ts` (`spends the plan exactly once in one process, and never steals the held name`, `refuses an unbounded store rather than making room by deleting evidence`, `never echoes the home, the store path, or the checkout in a refusal`), `tests/governance-doctor/repair-claim-store-account-home-v1.test.ts` (`resolves one store from the OS account, whatever the environment says`, `refuses a reported account home that is a symlink or reparse point`), `tests/governance-doctor/repair-claim-store-permissions-v1.test.ts` (`accepts a root-owned 0755 ancestry`, `accepts a root-owned sticky ancestor naming an entry this account owns`, `refuses a root-owned world-writable ancestor that is not sticky`, `refuses an account home whose ancestry is deeper than the bounded ceiling`), `tests/governance-doctor/repair-claim-store-ancestry-v1.test.ts` (`refuses a naming ancestor substituted at the pre-create store boundary`, `reports the post-publication label when an ancestor fails after the read-back`), and `tests/governance-doctor/repair-capability-boundary-v1.test.ts` (`keeps the executor and verifier out of every public and command surface`, `lets exactly one module reach the durable claim store`, `has no route into a process, network, provider, scanner, signer, or command area`). |
| <a id="CM-68"></a>CM-68 | `aih governance-doctor --repair-plan` appends a preview-only mechanical Repair plan derivation to the read-only presentation. The preview consumes only the run's own branded Audit, the shipped profile, and one code-owned broker mapping; no flag, option, or positional value can supply a broker, recipe, effect, path, or content, and without the flag the output is unchanged. Its closed payload has one fixed key set across the four outcomes (`no-mechanical-repair`, `plan`, `posture-unavailable`, `unavailable`), reports `executable: false` always, presents only plan/summary digests, the recipe name, and bounded effect summaries over managed-relative arguments, and collapses every malformed or refused input into a fixed no-plan outcome that echoes no path, byte, or OS diagnostic. Minting a preview captures no consent, spends no claim, and runs no executor, custody, or verifier code: the command route's import closure gains exactly the preview, broker, and plan modules plus their shared repair-capability primitives, and the executor, verifier, claim, claim store, content, and custody modules keep zero importers outside the foundation. The findings-to-effects table is a code-owned constant whose widening is a reviewed edit; it holds exactly one entry, described in [CM-69](#CM-69). | `src/governance-doctor/repair-plan-preview-v1.ts` and `src/governance-doctor/command-v1.ts`. | `tests/governance-doctor/repair-plan-preview-v1.test.ts` (`reports no-mechanical-repair for a completed healthy audit, minting nothing`, `reports unavailable for a refused audit and for a run that produced none`, `reports posture-unavailable when the profile does not declare guided-only repair`, `collapses malformed, missing-key, and extra-field input into unavailable, throwing nothing`, `mints and presents a bounded plan from code-derived effects only`, `refuses derived effects the frozen recipe cannot join rather than presenting them`), `tests/governance-doctor/command-v1.test.ts` (`presents no repair plan preview unless the flag asks for one`, `appends the preview-only repair plan section under --repair-plan`, `emits the closed preview JSON shape under --repair-plan and carries no root path`, `keeps the preview a no-plan outcome when a diagnostic yields an evidence gap`), and `tests/governance-doctor/repair-broker-v1.test.ts` (`keeps only the reviewed command registration, Workbench, and public exports`, `keeps its importer set closed to the foundation and its internal executor`). |
| <a id="CM-69"></a>CM-69 | Exactly one `aih doctor` outcome survives as an audited finding instead of collapsing its diagnostic into an evidence gap: the code-owned four-part tuple of diagnostic `aih.doctor.root`, check `context-dir`, verdict `skip`, and code `canon.context-dir-missing`. That code is compared and then discarded; the emitted finding carries the AIH-owned code `AIH_CANON_CONTEXT_DIR_MISSING`, `low` severity, and AIH-authored prose, so no string a probe authors reaches an Audit, Guide, or plan as data. The table is not a passthrough and not configurable, and it is all-or-nothing per diagnostic: any non-pass verdict it does not cover -- a different code, check, verdict, or diagnostic, or an unmapped verdict accompanying a mapped one -- keeps the pre-existing `evidence-gap` refusal. Because the mapping is all-or-nothing, it is not reachable from a real run: Doctor's canon markdown lint check skips on exactly the same condition as this one -- both test whether the context directory exists -- so the mapped tuple never occurs alone and this finding is never emitted, and a fresh fixture root in fact reports 24 non-pass checks. The table and the eligibility boundary are proved by tests, including a pin that runs the real Doctor planner against a fixture root. The emitted prose says the *configured* context directory, never the canonical one, because Doctor reports the identical tuple for a marker naming a custom directory and this finding must not assert a distinction the diagnostic never made. Under `--repair-plan` that finding derives one `create-managed-directory` effect whose path is the literal module constant `ai-coding` -- never the marker, a `--context-dir` override, an environment value, the diagnostic's detail or location, or any field of the eligibility record. Minting is gated on eligibility resolved at the command boundary: the committed `.aih-config.json` must be present and valid, its context directory must be exactly `ai-coding`, the run's resolved execution context directory must be exactly `ai-coding`, and the record's root digest must be the one this operation bound. The preview module stays capability-free -- it reads no file, setting, marker, or environment value and holds no callback that could -- and accepts only the frozen, `WeakSet`-branded eligibility record, so a plain object, spread copy, proxy, accessor-bearing look-alike, prototype child, altered protocol string, or mismatched root mints nothing and yields the fixed `unavailable` outcome. The preview remains `executable: false`; nothing here captures consent, spends a claim, or runs executor, custody, or verifier code. | `src/governance-doctor/operational-v1.ts`, `src/governance-doctor/repair-eligibility-v1.ts`, `src/governance-doctor/repair-plan-preview-v1.ts`, and `src/governance-doctor/command-v1.ts`. | `tests/governance-doctor/operational-v1.test.ts` (`maps the exact doctor tuple to one fixed low finding and forwards no diagnostic code`, `keeps evidence-gap for the same check under a different code, verdict, or name`, `keeps evidence-gap when an unmapped non-pass verdict accompanies the mapped one`, `refuses the same tuple reported by a different diagnostic`), `tests/governance-doctor/repair-eligibility-v1.test.ts` (`mints nothing for an alternate, absolute, traversal, nested, or case-varied marker path`, `mints nothing when the resolved execution context dir is not the canonical path`, `refuses every hostile substitution for a minted record`, `loads no platform capability and opens no dynamic seam`), `tests/governance-doctor/repair-plan-preview-v1.test.ts` (`mints one create-managed-directory effect at the code-owned canonical path`, `creates no plan for any hostile substitute for eligibility`, `keeps no-mechanical-repair for a healthy audit even when eligibility holds`, `keeps its whole import closure free of platform capability`), and `tests/governance-doctor/command-v1.test.ts` (`previews one create-managed-directory effect at the canonical path when the marker agrees`, `maps the tuple into the audit when it is the only non-pass verdict`, `mints no plan without a committed marker, or from a marker naming another directory`, `mints no plan when a context-dir override disagrees with the committed marker`, `still reports an evidence gap, because a fresh repository skips far more than the context dir`). |
| <a id="CM-70"></a>CM-70 | The precondition for the one mechanical Repair recipe is a narrow, code-owned probe rather than the full `aih doctor` result, because the full result is the wrong authority boundary for a repository-local repair: a fresh root reports two dozen non-pass checks, and at least two of them -- whether an AI CLI is runnable and whether `rg`/`fd`/`jq` are on `PATH` -- are host facts no repository state can clear, so gating on that whole result would make "is `jq` installed" part of the authority to create a directory. The probe observes exactly two shipped checks and reports eligible only when their complete non-pass projection equals one frozen causal bundle, in order and cardinality: context-dir `skip` with code `canon.context-dir-missing`, and the code-less canon-markdown-lint `skip`. The second tuple is deliberately not unique -- `canonLintCheck` reports the same triple for informational findings -- and is safe only inside a bundle-exact match, because that branch sits after the directory-exists guard and so can only fire while the first check reports `pass`, at which point the observed set is no longer this bundle; bundle-exactness *is* the disambiguation, and neither tuple may be lifted out and keyed alone. That argument covers one reading and not two, so a reading that would qualify is confirmed by a second one before eligible is reported and the confirming reading is what the record carries -- a directory created between the bundle's two checks would otherwise yield tuple 1 from the first observation and tuple 2 from the second, reproducing the pair over a state the tree was never in. The confirming read bounds that window rather than closing it, and the limit is stated: a change that lands and reverts entirely between the two readings is not observable, and leaves the tree in the state the record reports. One frozen list declares both the checks observed and the tuples expected, so the positional comparison cannot come to mean "in whatever order the calls appear". The probe accepts only a `WeakSet`-branded scope it cannot itself mint, and refuses every substitute -- a bare absolute path, a plain object, a spread copy, a proxy, an accessor-bearing look-alike, a parse, a prototype child -- before any filesystem call. The mint takes no path, target, or recipe: it reads the root out of a branded operational context, refuses a run whose resolved context directory is not the literal `ai-coding` (so `--context-dir`, `AIH_CONTEXT_DIR`, or a marker pointing elsewhere buys no scope and no filesystem call), and requires that root to be absolute and its own native canonical form. Only `{name, verdict, code}` is projected out of each check: no raw `Check`, `detail`, or location is retained, returned, hashed, or forwarded, and the root travels only as a domain-separated digest. One limit is stated rather than implied: the brand proves package-internal construction, not which caller built the context. Eligibility no longer rests on reachability alone -- it also requires the no-follow `unoccupied` verdict described in CM-72, so the earlier gap between "unreachable" and "unoccupied" is closed rather than delegated to a consumer. The probe is reachable from production only through the separate `aih repair` staged attempt path; no flag, callback, or caller-authored path can invoke it directly. The completion record in CM-73 digests the precondition as evidence through the same live-attempt provenance. | `src/governance-doctor/repair-scope-v1.ts` (scope mint, canonical-root and canonical-context-dir rules, `WeakSet` brand) and `src/governance-doctor/repair-precondition-v1.ts` (frozen bundle, sanitized projection, exact-bundle eligibility, observation brand), over the single `canonContextDirCheck` implementation extracted into `src/lint/run.ts` and consumed by both `src/doctor.ts` and the probe. | `tests/governance-doctor/repair-precondition-v1.test.ts` (`is eligible when the canonical directory is absent, and reports the frozen identity`, `stops being eligible once the directory exists, and is eligible again when it is removed`, `cannot qualify while the context-dir check passes, whatever the lint reports`, `observes the same context-dir check the Doctor probe runs`, `is unaffected by the host gaps that make the full Doctor result ineligible`, `refuses every unbranded scope before it touches the filesystem`, `makes no filesystem observation for an unbranded scope`, `refuses every substitute for an observed record`, `binds one run to its own canonical root and this recipe's literal target`, `refuses a run whose resolved context directory is not the canonical one`, `refuses an operational context AIH did not brand`, `refuses a root that is not absolute`, `refuses a root that is not its own canonical form`, `refuses a root that cannot be resolved`) and `tests/governance-doctor/repair-precondition-lint-ambiguity-v1.test.ts` (`cannot qualify, even though its tuple is identical to the bundle's second member`). |
| <a id="CM-71"></a>CM-71 | An audit that found real problems *and* could not see part of the workstation reports `partial`, and can never report as a completed one. An Audit has always carried findings and per-diagnostic refusals side by side, but every surface asked only whether the refusal list was empty, so the mixed run was labelled `evidence-gap` -- the same word used for a run that resolved nothing at all -- with its findings still in the payload underneath. The classification is now made once, by a single code-owned derivation over the branded Audit, and travels as a frozen record rather than being re-derived per surface: `completed` when every declared diagnostic resolved (findings present or not), `partial` when some diagnostics produced findings and some did not resolve, and `evidence-gap` when nothing resolved into a finding and at least one diagnostic did not resolve. The first and third are exactly what these surfaces reported before, for exactly the runs they reported them for; only the mixed run changes, and its exit code does not, because only a completed audit ever passed. `completed` is defined by the absence of an unresolved diagnostic and by nothing else, so no count, severity, or finding can argue a partial run up into a complete one. The record cannot contradict itself: transport re-checks the state against the contents it summarizes, so a value claiming `completed` while carrying an unresolved diagnostic is refused rather than believed, as are unknown, missing, or renamed fields, duplicate diagnostic identities, an unresolved collection wider than the closed diagnostic set, a state outside the closed three, a finding count outside its bound, an incompatible protocol, and proxies, accessors, sparse arrays, and foreign prototypes. The field is named `unresolved`, not `evidenceGaps`, because a diagnostic fails to resolve in five distinct ways -- `missing-adapter`, `evidence-gap`, `missing-credential`, `unsupported-host`, `unmanaged-drift` -- and each is carried with its own state so a reader learns why that part of the workstation went unseen. The classification is visible in the JSON `outcome`, in a human line that says in words that this is not a completed audit and that nothing is known about what the unresolved diagnostics cover, and in the `--repair-plan` preview's own `auditCompleteness` field -- so a derived plan can never be read as evidence that the audit was complete. | `src/governance-doctor/audit-completeness-v1.ts` (the single derivation, the closed record, and its self-consistency rule), consumed by `src/governance-doctor/command-v1.ts` (presentation outcome and human text) and `src/governance-doctor/repair-plan-preview-v1.ts` (preview payload). | `tests/governance-doctor/audit-completeness-v1.test.ts` (`is completed only when every declared diagnostic resolved`, `is partial when findings and unresolved diagnostics coexist`, `keeps the existing evidence-gap state when nothing resolved`, `treats every per-diagnostic refusal state as unresolved`, `counts a declared diagnostic with no adapter as unresolved`, `binds the classification to the audit it read`, `refuses an audit-level refusal, which has no completeness to report`, `refuses every substitute for a branded audit`, `refuses unknown, missing, and renamed fields`, `refuses an incompatible protocol`, `refuses duplicate diagnostic identities`, `refuses an unbounded unresolved collection`, `refuses hostile shapes`, `refuses every state that contradicts its own contents`, `refuses a look-alike that is otherwise perfectly shaped`), `tests/governance-doctor/command-v1.test.ts` (`reports a declared diagnostic with no code-owned adapter as missing-adapter`, `exits non-zero from the CLI when a dispatched diagnostic yields an evidence gap`, `still reports an evidence gap, because a fresh repository skips far more than the context dir`), and `tests/governance-doctor/repair-plan-preview-v1.test.ts` (`mints one create-managed-directory effect at the code-owned canonical path`, `still reports partial when a plan is derived from a partial audit`, `creates no plan for any hostile substitute for eligibility`). |
| <a id="CM-72"></a>CM-72 | Eligibility for the one mechanical Repair recipe requires the literal `ai-coding` name to be proved genuinely free, established by no-follow inspection rather than inherited from a reachability check. `aih doctor` answers "is the canon reachable" with `existsSync`, which returns `false` for at least three different situations -- the name is free, the name is held by a link that resolves to nothing, or the lookup failed and nothing was learned -- and a repair may proceed in only the first. Absence is therefore established directly: `ENOENT` alone, only beneath a parent already proved by `lstat` to be a real non-link directory, and only while the root is still the same filesystem object the scope was minted against. That is an identity comparison, not a path one: the scope captures the root's `dev`/`ino` at mint under custody's own rule, refusing an unusable inode, and the probe re-compares it -- so a directory renamed away and replaced by a fresh one at the same canonical path is `indeterminate` rather than a checkout whose `ai-coding` is free. `throwIfNoEntry: false` is deliberately not used, because it suppresses `ENOTDIR` as well as `ENOENT` -- measured, not assumed -- and would return a path whose parent is a regular file as absent. The verdict is one of three: `unoccupied`, the only one a repair may act on; `occupied`, for a directory, a regular file, a live link, a dangling link, or anything else holding the name, since this recipe creates rather than replaces; and `indeterminate`, kept distinct from `occupied` so a refusal is never mistaken for a fact about the tree when it records only that the tree could not be read. Permissions, transient errors, symlinks and reparse points, malformed ancestry, and any uncertain occupancy all reach one of the two refusing verdicts; no branch reports `unoccupied` on the strength of an error. The probe takes only the branded scope and refuses every substitute before any syscall, and its record carries the scope's root digest, the recipe identity, and the literal target -- never a path, an errno, or OS text. It is bounded rather than atomic and is stated as such: three syscalls establish a verdict that describes the instant it was taken, which is why a consumer re-observes before spending a claim and again at the effect boundary, where custody re-proves the same facts under the mutation grant and its no-clobber sequence is what closes the window. Custody owns that rule at the boundary and cannot be imported by a capability-light probe, so the rule is stated twice and a test pins that the two agree on absence over the same trees. `aih doctor` behavior and the public/package export surface are unchanged. | `src/governance-doctor/repair-target-occupancy-v1.ts`, consumed by `src/governance-doctor/repair-precondition-v1.ts`, whose eligibility now requires the bundle-exact reading *and* an `unoccupied` verdict; the boundary rule it is pinned against is `lstatSafe`/`walk` in `src/governance-doctor/repair-custody-v1.ts`. | `tests/governance-doctor/repair-target-occupancy-v1.test.ts` (`reports unoccupied only for a genuinely free name under a real directory`, `reports occupied for a dangling link the shipped checks read as absent`, `reports occupied for every other thing that can hold the name`, `reports indeterminate rather than unoccupied when the root is not a real directory`, `binds the verdict to the scope and refuses every substitute for it`, `refuses every substitute for an observed record`, `calls the name absent in exactly the same cases custody does`) and `tests/governance-doctor/repair-precondition-v1.test.ts` (`is not eligible when a dangling link occupies the target`). |
| <a id="CM-73"></a>CM-73 | A Repair attempt's outcome is recorded as three facts that never stand in for one another: whether the literal `ai-coding` effect independently verified, what a *fresh* post-execution audit says about the workstation, and whether the Repair is complete. Creating one directory is a small verifiable thing, and the temptation once it verifies is to let that success speak for the workstation; each step of that is a different claim and only the first was proved. `complete` requires all of it -- the effect verified, the fresh audit healthy, every trusted join holding, and the receipt itself verifying. `partial` is the single shape where a verified effect coexists with a workstation that still reports problems: not a failure, not a success, and not rounded to whichever is nearer. `failed` covers an unverified effect, a post-audit that is unavailable or malformed, and any trusted join that does not hold. A receipt records what an executor did and is never read as a certificate of its own success: verification is a separate record from a separate verifier against the live tree, and the effect counts as verified only when this exact effect identity is both reported applied by the receipt and confirmed by exactly one matching verifier check. `healthy` is deliberately stricter than Audit's own `completed` -- an audit that resolved every diagnostic and still found problems is `completed` for a diagnostic and `partial` here -- and Audit V1's own schema and XOR are untouched, because a mutation's outcome does not belong inside the record a read-only diagnostic mints. Refusal and failure stay distinct: an input that is not what it claims to be (unbranded, forged, a look-alike parse) is refused and mints nothing, because a completion record implies the attempt was real, while a genuine input that does not agree -- a claim spent for another plan, consent, or checkout, or a verification of another receipt -- mints a `failed` record, because that is something that happened. The record binds plan, consent, spent claim, resolved-scope, precondition-evidence, receipt, post-audit, and report digests, plus the exact effect identity, and its residual is a bounded, de-duplicated, code-unit-ordered list of AIH-owned finding codes and closed refusal-state names with counts -- no diagnostic detail, path, OS error, timestamp, or callback has a field to occupy. Transport re-checks every state against the contents it summarizes, so a record claiming `complete` on an unverified effect or a non-healthy audit is refused however it was produced. Freshness is the caller's obligation, which the module cannot verify and does not claim to: what it guarantees is that reusing the pre-execution audit cannot manufacture `complete`, because that audit is the one that reported the missing canon and its residual is non-empty. The command route reaches completion only through the staged live-attempt provenance wrapper, not as a caller-supplied completion API. | `src/governance-doctor/repair-completion-v1.ts`, over `src/governance-doctor/audit-completeness-v1.ts`, `repair-outcome-v1.ts`, `repair-claim-v1.ts`, and the precondition-evidence digest in `repair-precondition-v1.ts` bound to the resolved root by `repair-scope-v1.ts`. | `tests/governance-doctor/repair-completion-v1.test.ts` (`is complete only when the effect verified and the fresh audit is healthy`, `is partial when the effect verified and unrelated evidence remains`, `is failed when no valid post-execution audit was produced`, `is failed when a trusted join does not hold, even though the parts are real`, `refuses an effect identity that is not this recipe's managed directory`, `is failed when a sibling effect in the same plan did not apply`, `cannot be talked into complete by reusing the audit that reported the problem`, `refuses an input that is not what it claims to be, rather than recording a failure`, `carries canonical bytes only for a record it minted`, `refuses every state that contradicts its own contents`, `refuses hostile shapes and out-of-bound residual entries`) and `tests/governance-doctor/repair-broker-v1.test.ts` (`keeps its importer set closed to the foundation and its internal executor`). |
| <a id="CM-74"></a>CM-74 | `aih repair` is a separate top-level Governance Doctor Repair route with a dry-run-by-default, zero-write preview and an explicit-apply mutation boundary. The live command-only canonical derivation is separate from the read-only `aih governance-doctor --repair-plan` preview: it joins the real operation Audit/Guide, branded eligibility, scoped precondition, and shipped profile, then derives only the reviewed single `create-managed-directory("ai-coding")` effect when the literal target is freshly observed `unoccupied`. The preview discloses the target, full lowercase Plan/Precondition/Summary SHA-256 digests, target occupancy, and Audit completeness without consent, claim spend, executor, verifier, or writes; the read-only `--repair-plan` payload remains `executable: false` and does not widen its effect mapping. An `indeterminate` target-occupancy result is disclosed as a distinct refusal state, never treated as free, while occupied and no-mechanical-repair outcomes remain non-mutating. Apply accepts only the literal CLI `--apply`, rejects JSON/ambient or injected authorization and caller-supplied confirmation paths, requires both process streams to be TTYs, and accepts only the exact raw Plan digest through the local prompt. After confirmation, the staged live attempt re-observes the branded precondition before claim and at the effect boundary, spends one durable claim, applies only the canonical directory effect, independently verifies it, runs a fresh post-audit, and reports effect verification, post-audit state, repair state, and bounded residual entries separately. A verified effect with unrelated remaining findings is `partial`/qualified success, while post-claim refusal or unavailable verification remains incomplete/failed; no other diagnostic or effect is made executable by this route. | `src/governance-doctor/repair-command-v1.ts`, `src/governance-doctor/repair-live-canon-plan-v1.ts`, `src/governance-doctor/repair-plan-preview-v1.ts`, `src/governance-doctor/repair-confirmation-v1.ts`, `src/governance-doctor/repair-attempt-v1.ts`, `src/governance-doctor/repair-completion-v1.ts`, `src/commands/index.ts`, and `src/internals/plan.ts`. | `tests/governance-doctor/repair-command-v1.test.ts` (`is a separate mutating, zero-write, dry-run-by-default CommandSpec`, `presents a bounded canonical plan and both full lower-case digests without authority`, `derives one live canonical plan on a temporary root despite an unrelated diagnostic refusal`, `requires live interactive exact-digest consent before the staged attempt, then verifies and post-audits`, `reports a verified effect with an unrelated partial post-audit as qualified success`, `qualifies indeterminate target occupancy in dry-run and refuses apply before prompt or claim`, `refuses non-authoritative apply paths before prompt, consent, claim, or effect`), `tests/governance-doctor/repair-cli-integration-v1.test.ts` (`keeps a bare registered invocation dry-run, with no confirmation, claim, or effect`, `routes literal --apply through the custom executor and creates only the canonical target`, `refuses the registered apply route when local terminal confirmation is non-interactive`, `keeps the applied effect and mutation summaries honest when post-effect verification fails`), `tests/governance-doctor/repair-confirmation-v1.test.ts` (`requires both real process streams to be TTYs and honors AIH_NO_PROMPT before opening readline`, `itself displays the canonical effect summary and both complete digests, preserving the raw answer`), `tests/governance-doctor/repair-attempt-v1.test.ts` (`creates only the canonical managed directory from a fresh branded scope and spends one claim`, `refuses a target that became occupied after observation but before its live pre-claim check without spending a claim`, `refuses a same-root target occupation after its durable claim without applying an effect`, `refuses at the effect boundary after a durable claim when its bound root is replaced`), `tests/governance-doctor/repair-completion-v1.test.ts` (`is partial when the effect verified and unrelated evidence remains`), and `tests/governance-doctor/command-v1.test.ts` (`appends the preview-only repair plan section under --repair-plan`, `emits the closed preview JSON shape under --repair-plan and carries no root path`, `previews one create-managed-directory effect at the canonical path when the marker agrees`). |
| <a id="CM-75"></a>CM-75 | Sharded ECC baseline vetting requires one strict static preflight receipt before scanner fan-out and at final fan-in. The preflight lexically checks the preview generator's complete relative dependency closure without loading upstream code, binds the exact ECC catalog identity, ordered installer-runtime paths, code-owned target contract, check version, and whole-source tree hash, and refuses if the tree changes while the receipt is minted. Every shard and every `--reuse-from` assembly must present the same live-verifiable receipt; the completed ECC evidence must carry that same whole-source digest, so a change after receipt verification cannot assemble evidence for an unchecked tree. Missing, stale, forged, sharded, linked, oversized, reordered, or source-mismatched inputs fail closed. This early receipt distributes evidence, not authority and does not claim dynamic adapter compatibility: the authorized preview boundary independently repeats its dependency-closure check after both sources have valid analyzer evidence, executes the upstream generator only there, and then validates the generated adapter output. Ordinary non-sharded vet/check flows retain their existing interface. | `src/baseline-evidence/ecc-preflight-receipt.ts`, `src/baseline-evidence/ecc-preview-boundary.ts`, `src/baseline-evidence/generate.ts`, `src/ecc/install-targets.ts`, `src/ecc/install-preview-generate.ts`, and `src/ecc/install-preview-validate.ts`. | `tests/baseline-evidence/ecc-preflight-receipt.test.ts` (`runs structural checks without executing upstream and binds the complete source identity`, `refuses cross-source and sharded receipts without exposing absolute paths or source text`, `refuses a new source file after preflight rather than treating the old receipt as current`, `refuses linked and oversized receipt files before parsing them`, `reads a valid receipt from a regular file outside the ECC source root`, `keeps receipt paths outside the source root and refuses linked parents into it`, `joins completed ECC evidence back to the preflight source digest`, `accepts matching completed evidence and reaches the authorized preview step`), `tests/baseline-evidence/ecc-preview-boundary.test.ts` (`bounds outside-closure diagnostics and honors the runtime path whitelist`, `keeps the authorized preview boundary check independent of any preflight receipt`), and `tests/baseline-evidence/generate.test.ts` (`keeps ordinary non-sharded generation compatible without a receipt`, `requires a receipt output for static preflight and rejects incompatible modes`, `requires and accepts the ECC preflight receipt at shard and fan-in boundaries`, `rejects missing values at the CLI boundary instead of consuming another flag`, `joins sharded ECC evidence to the full-source preflight digest`). |
| <a id="CM-76"></a>CM-76 | Superpowers content-acceptance maintenance is reproducible without weakening the runtime gate. `check:scan-acceptance` accepts only an explicit canonical Git root outside AI-Harness, verifies a clean detached checkout at the exact shipped `obra/Superpowers` commit and origin before and after inspection, disables Git replacement-object substitution, rejects ignored content and index flags that can hide tracked bytes, reuses the runtime content inspector, and deterministically reports exact accepted, stale, missing, new, and critical code/path/LF-normalized-content-digest observations. Malformed or duplicate acceptance entries, ambiguous paths, checkout identity changes, critical findings, and mismatches never become accepted. The command writes nothing, emits no candidate artifact, and always reports `authorizes: false`. After the exact pinned audit showed that none of 62 historical ledger rows joins current scanner output, #804 removed those rows and left the shipped accepted set empty; the 87 current advisory observations remain unaccepted, with detector, severity, pin, critical-finding, and runtime authorization semantics unchanged. | `src/binding/scan-acceptance-check.ts`, `src/binding/scan-gate.ts`, and `src/binding/scan-acceptance.json`. | `tests/binding/scan-acceptance-check.test.ts` (`ships no historical accepted rows`, `uses real inspector pins with CRLF compatibility and repeatable byte-identical JSON`, `reports stale, missing, new, and critical scanner findings without caller observations`, `rejects malformed, duplicate, absolute, and traversal acceptance entries`, `rejects ignored checkout content before inspection`, `accepts constrained GitHub Superpowers origin equivalents`, `rejects ambiguous GitHub Superpowers origin equivalents before inspection`, `rejects skip-worktree and assume-unchanged tracked files before inspection`, `rejects an actual sparse checkout before inspection`, `rejects malformed index tag output before inspection`, `rejects replacement refs that substitute the detached checkout tree`, `fails closed for wrong, mutable, unreadable, and mutation-during-inspection checkouts`, `rejects the AI-Harness checkout and writes no report file`, `accepts only an explicit absolute checkout argument and emits deterministic JSON`, and the canonical-root regression), plus `tests/binding/scan-gate.test.ts` (`ships the #804 empty acceptance ledger`). |

| <a id="CM-77"></a>CM-77 | A governance decision can affect projection only through an externally verified authority receipt V2 and an exact live join to its candidate, immutable source/evidence identity, AIH-shipped reviewed-control digest, policy version, requested targets, registered effect set, trusted issuer, time window, and observed dispositionable findings. `approved` requires clean observed coverage; `accepted-with-conditions` requires exact accepted coverage and a current review deadline; rejection and separately signed revocation withhold effects. The generic record reserves named-gap coverage for an explicitly registered waivable gap class, but the current resolver registers none: `acceptedGaps` and `observedGaps` remain empty, while evidence gaps remain fenced danger/blocker state that no decision can waive. Every fenced prerequisite remains blocking, custom candidates remain unprojectable, and legacy approvals keep their narrower evidence-gap semantics. Requested/effective state, full observed findings, accepted coverage, decision identity, derived risk, and blockers are deterministic across evaluate/doctor and managed-settings output, while signed conditions stay out of public effect artifacts. Claude/Kiro MCP ownership is minted only as strict V2 and usage-hook ownership only as V3 with surface-separated integrity digests; those local receipts compare current authority and permit exact rollback but never grant it. Legacy MCP V1 and hook V2 state is cleanup/upgrade-only. All planned ownership and content transitions are pinned to observed regular-file bytes and applied in one filesystem transaction. | `src/org-policy/authority.ts`, `src/org-policy/effective.ts`, `src/org-policy/evaluate.ts`, `src/org-policy/project.ts`, `src/config/marker.ts`, `src/mcp/managed-projection.ts`, `src/mcp/kiro-managed-projection.ts`, and `src/mcp/projection-marker.ts`. | `tests/org-policy/governance-projection.test.ts` (`keeps accepted findings visible while an exact current reviewed decision unlocks`, `keeps managed settings and policy-evaluate JSON decision views public-safe and truthful`, `never lets accepted coverage waive a fenced prerequisite`, `fails closed for decision coverage, binding, time, revocation, and signed rejection`, `reports policy-scope decision blockers in effective checks and projection refusal for $name`, `refreshes legacy Claude ownership and managed settings when a new decision summary changes full settings`, `keeps exact single-target and dual-target MCP decision bindings on their own surfaces`, and `fails closed on recomputed-digest v3 receipt shape and binding adversaries`), `tests/config/marker.test.ts` (`uses stable domain-bound v2 hashes and preserves v2 bindings on revocation`), `tests/mcp/kiro-managed-projection.test.ts` (`does not treat a changed v2 decision binding as idempotent`), `tests/mcp/projection-marker.test.ts` (`refuses conflicting replacements for the same ownership receipt key`), and `tests/org-policy/org-policy.test.ts` (`accepts only sorted decision authority references and keeps approval ids disjoint`). |

| <a id="CM-78"></a>CM-78 | The portable Policy Workbench accepts one standalone `GovernanceDecisionV1` only as inert, unverified inspection data. The headless import/export helpers use the canonical parser and bytes directly; the self-contained browser embeds the generated strict schema and mirrors the parser's bounded text, identifier, digest, timestamp/window, sorted-set, disposition, review, and accepted-coverage semantics. The imported decision lives in state separate from the authored policy and preflight receipt, is rendered only through text nodes, and is explicitly labeled unverified and not effective. Its canonical preview/download is deterministic but grants no authority. Invalid replacements preserve the displayed valid decision, and a latest-selection token prevents an older asynchronous file read from winning; the snapshot from the latest import's start is restored if that import is invalid. The surface has no decision editor, verifier, signer, authority fetch, effective resolver, projector, materializer, network call, or policy/approval/receipt copy path. | `src/org-policy/studio-model.ts` and `src/org-policy/studio-template.ts`. | `tests/org-policy/generate.test.ts` (`round-trips one standalone governance decision through canonical UI-only transport`, `keeps standalone decision import strict, inert, and parity-checked in the browser`, and the out-of-order import regression). |

| <a id="CM-79"></a>CM-79 | `aih report` emits one deterministic, read-only governed-subject review that reuses the existing effective-policy resolution and strict receipt readers. Every subject remains ordinal and untruncated while request/effect, evidence/findings/blockers, bounded decision/approval/revocation facts, projector/target capability, materialization receipt state, and usage coverage stay separate. An explicit MCP-server identity is authoritative even when unknown and never falls through to name matching; otherwise only one unique heuristic match can attribute an event, and an event counts for at most one subject. Heuristic matches, unknown or ambiguous subject evidence, ordinary non-subject activity, malformed and unknown-kind rows, no capture, installed-zero-observed, and exact-attributed states remain distinguishable bounded facts. Names and rejected payload bytes never render; usage branch/commit/path, raw environment, token/cost, prompt, argument, and hook payload fields have no output slot. Non-effective subjects report `not-projected`; any surface-wide receipt remains a separately named fact rather than a subject materialization claim. Capture installation requires the effective `usage-metering` subject and an active strict hook receipt, never path presence. Low or zero counts do not change effective state, infer value, or recommend trim, retirement, revocation, uninstall, or another effect. Invalid, absent, or non-governing policy and invalid/absent receipt states remain explicit and redacted. Package Graph identity is omitted because no exact per-subject join exists. | `src/report/governance-review.ts`, `src/report/local.ts`, `src/usage/events.ts`, and the existing bounded resolution/receipt seams in `src/org-policy/evaluate.ts` and `src/org-policy/project.ts`. | `tests/report/governance-review.test.ts` (`keeps all governed subjects in deterministic ordinal order and never includes raw unmatched names`, `derives capture ownership only from an effective usage-metering subject and an active strict receipt`, `keeps each selected MCP target's strict receipt verdict`, `keeps observed and not-observed subjects distinct without inferring action`, `keeps heuristic-only and mixed attribution globally partial`, `keeps explicit unknown MCP identities unmatched and routine tool events non-subject`, `marks blocked subjects not-projected while retaining the surface receipt separately`, and the absent/invalid/not-governing regressions), `tests/usage/usage.test.ts` (`retains valid events while separately counting malformed and unknown-kind rows`), and `tests/report/local.test.ts` (`resolves the effective policy at most once before composing the governance review`). |

| <a id="CM-80"></a>CM-80 | Vendor baseline evidence can be packaged as one deterministic, separately attestable artifact without granting runtime or publication authority. The closed, bounded layout carries canonical metadata, the evidence bundle and manifest, the exact shipped vendor lock, and a checksum subject covering every file. The builder and new-directory writer reject hostile wrappers, malformed or oversized bytes, duplicate or incomplete layouts, and linked or pre-existing output custody. Verification settles every local byte, checksum, schema, source-pin, publisher, and policy join before invoking a caller-owned GitHub attestation verifier, then binds its verified claim to the exact subject, issuer, repository, workflow, ref, and protected environment. The manual-only workflow keeps candidate preparation in an unprivileged job and transfers the complete hidden-file layout to a separate environment-gated attestation job that has no checkout, package install, repository-code execution, release, or package-publication step. Merge does not trigger it; the first real dispatch still requires separate exact-SHA publication authorization. | `src/baseline-evidence/vendor-artifact-v1.ts`, `src/baseline-evidence/vendor-artifact.ts`, and `.github/workflows/vendor-baseline-evidence.yml`. | `tests/baseline-evidence/vendor-artifact-v1.test.ts` (`builds a deterministic, exact-pin/schema-bound subject with every file checksummed`, `verifies exact bytes, checksums, source pins, schema, and a caller-owned GitHub claim`, `rejects an exact-count artifact with an incomplete expected layout`, `rejects oversized builder lock bytes before parsing or hashing`, `refuses to build an artifact that exceeds its own verifier bounds`, `does not call the attestation boundary until all local bytes have passed`, `claims a new output directory and refuses to merge into an existing directory`, `refuses a symlink or junction parent without writing outside the output custody`) and `tests/workflows/vendor-baseline-evidence.test.ts` (`separates unprivileged candidate preparation from environment-gated attestation`). |
| <a id="CM-81"></a>CM-81 | The verified ECC Codex bind replaces the pinned vendor helper's floating Chrome DevTools launcher with a narrow Core-owned exact package identity from the active external-pin ledger. Every non-governed direct or verified call defaults to the deterministic exact-version scoped server, and the vendor MCP merge helper is never a fallback; governed calls remain MCP-empty. An exact legacy AIH npm, Bun, pnpm, or Yarn vendor stanza migrates only when matching live AIH state still claims it; requested managed entries are re-rendered from the exact specification, and direct runs retain other valid state-claimed scoped entries. A same-name operator server remains unowned and is not overwritten or recorded in AIH install state. The vendor baseline helper receives only an exclusive candidate inside a unique temporary directory, narrowed to `0600`/`0700` modes where the platform enforces POSIX modes; live config remains untouched through candidate preparation and later managed-file/AGENTS work. Config and AIH state commit only when both exact pre-helper byte observations still match, with fail-closed config/state races, safe AGENTS/config rollback, and exact non-recursive temporary cleanup. The reviewable static driver is deterministically compressed below Windows command-line limits while live paths and state remain separate arguments. Later removal requires the one structurally valid AIH-managed MCP fence; config and AGENTS subtraction are bound to exact source hashes, the same install-state bytes are asserted in the filesystem transaction before config mutation, and cleanup re-observes exact state and expected config hashes before deletion. Absent or malformed custody preserves operator-adopted tables, present-invalid state is an explicit refusal, and remaining claimed custody retains AIH state and fails cleanup visibly. Recognized equal transports may coexist under existing-user-wins semantics, while semantic duplicate, array, encoded, descendant, dotted, inline, unknown, mixed, stale, or otherwise ambiguous identity fails closed before install or cleanup. The vendor-specific replacement does not widen generic mutable-package authority or reinterpret the ECC repository pin as npm-package authorization. | `src/ecc/codex.ts`, `src/ecc/index.ts`, and `src/internals/external-pin-ledger.json`. | `tests/ecc/ecc.test.ts` (`projects the Chrome DevTools default from Core's exact pin, never ECC's floating merge`, `defaults a direct non-governed Codex action to Core's exact Chrome DevTools pin`, `does not claim an operator-owned %s Chrome DevTools server while retaining the Core projection`, `fails closed for an %s Chrome DevTools transport it cannot classify`, `fails closed when project and global Chrome DevTools definitions are both mixed`, `preserves an operator-adopted Chrome DevTools table after an %s`, `refuses direct apply for an unowned non-root Chrome representation: %s`, `removes only a state-claimed Chrome DevTools table from a valid managed fence`, `writes Core's exact Chrome DevTools pin for a claimed mutable table %s the managed fence`, `refuses a %s in live Codex MCP state before effects`, `keeps AIH state when prune cannot safely remove a claimed pre-fence Chrome table`, `refuses a stale Codex config removal instead of overwriting a concurrent operator edit`, `refuses a stale Codex AGENTS removal instead of overwriting a concurrent operator edit`, and `reobserves the live Codex config before a planned state cleanup`) plus `tests/prune/command.test.ts` (`reports a malformed claimed Codex AIH state without planning its cleanup`, `refuses a post-plan Codex state custody change before subtracting its config`), `tests/ecc/verified.test.ts` (`keeps Codex on the add-only merge path inside the same sequential driver`), and `tests/internals/external-pin-ledger.test.ts` (`binds refreshed MCP and repo-tool runtimes to production generators`). |

| <a id="CM-82"></a>CM-82 | Binding scan output preserves its raw finding list and adds a deterministic per-file rollup whose ordering includes every current identity field and keeps absent, false, and true acceptance states distinct; duplicate findings are never merged or masked. Visible-typography classification keeps Turkish dotted and dotless I advisory only in proven prose, comment, and string contexts, while identifier, key, code, bidi-control, and standalone variation-selector positions retain blocking treatment. Dotted I keeps the detector's raw medium code and severity; the binding gate applies blocking policy only when the shared occurrence classifier proves a machine-sensitive context. That classification reread must match the finding's LF-normalized content hash through a bounded, contained, regular-file read; missing, changed, oversized, linked, or escaping paths are uncertain, block, and cannot be accepted. The maintainer regeneration command is source-only and targets the module-owned acceptance ledger independently of the caller's working directory. It first requires the existing observational checker to prove the exact configured obra/Superpowers origin and commit, detached HEAD, clean and fully materialized index, no replacement refs, stable checkout identity, no critical observations, and `authorizes: false`; it emits only canonical empty acceptance, refuses linked or non-regular ledger custody, uses an exclusive temporary file plus atomic replacement, and makes check mode read-only. Claude settings are a harmless husk only when their recognized hook/plugin/MCP/override containers contain no loadable behavior; malformed containers remain visible warnings, and every actual hook command, plugin, or MCP remains contamination. | `src/binding/scan-gate.ts`, `src/binding/visible-typography.ts`, `src/binding/scan-acceptance-regenerate.ts`, `src/binding/scan-acceptance.json`, and `src/binding/hosts/claude/contamination.ts`. | `tests/binding/scan-gate.test.ts` (`%s has the correct end-to-end gate outcome`, `keeps dotted İ prose advisory when a different glyph blocks the same file`, `fails closed when a cached dotted İ finding's checkout content is %s`, `fails closed when a deep visible-unicode path escapes the checkout by %s`), `tests/binding/scan-gate.test.ts` (`groups every raw finding deterministically without masking duplicate identities`, `is byte-stable across permutations and preserves advisory and content identities`, `keeps absent, false, and true acceptance states deterministically distinct`), `tests/binding/scan-acceptance-regenerate.test.ts` (`writes canonical empty ledger bytes without accepting observations and check mode is read-only`, `ships the exact pinned canonical ledger at its module-owned target`, `refuses a linked target before writing and anchors its default target independently of cwd`), `tests/binding/visible-typography.test.ts` (`keeps Turkish İ/ı prose, comments, and strings advisory while code positions block`, `classifies each dotted İ occurrence without letting a separate blocker taint prose`, plus the existing bidi and variation-selector regressions), and `tests/binding/hosts/claude/contamination.test.ts` (`classifies empty and schema-only user settings as harmless husks, but keeps active content contaminated`, `warns for a non-object settings document and never labels it a harmless husk`). |

| <a id="CM-83"></a>CM-83 | Offline authority continuation remains disabled, but its prerequisite revocation transport and replay state now have one strict, inert V1 contract. A canonical snapshot binds the exact issuer, positive safe-integer sequence, UTC-second `issuedAt` and `validUntil`, and a complete sorted unique set of at most 4,096 exact decision ids; validity is 60 seconds through 90 days. A domain-separated DSSE/in-toto statement binds the canonical snapshot digest and administrator signer identity. Parsing rejects noncanonical or unknown shapes, invalid UTF-8/Unicode, hostile proxies/accessors without invoking their traps, signature/context substitution, and malformed, future, or out-of-range material. Resolution re-verifies signed bytes every time against caller-supplied expected issuer, signer identity, signer-root digest, and verifier; cached verification is never authority. Snapshot issuance must cover the decision issuance and `notBefore`, while `validUntil` cannot exceed decision expiry, review deadline, or receipt expiry. Missing or expired coverage is the distinct frozen `stale-authority` result; future, unverifiable, conflicting, or malformed authority is invalid. Per-issuer durable sequence/full-signed-digest state accepts only a higher sequence, treats an identical equal sequence as unchanged, rejects rollback and equal-sequence substitution, and exposes one synchronous compare-and-swap custody seam with live observation before and after the claim. A race, async callback, failed claim, post-claim mismatch, or crash-corrupted observed state reports contention and never authorizes another effect. Current resolution reports only the exact decision's revocation fact and remains `materializable: false`; the module imports no filesystem, process, or network authority and has no CLI, Workbench, public-library, deletion, subtraction, stop, or materialization route. | `src/org-policy/offline-revocation-snapshot-v1.ts`. | `tests/org-policy/offline-revocation-snapshot-v1.test.ts` (`canonicalizes a separately signed, complete exact revocation set deterministically`, `reverifies caller-supplied administrator trust on every resolution and binds DSSE context`, `returns a distinct frozen stale-authority no-effect result when coverage is absent or expired`, `honors exact revocation only after all decision and receipt coverage bounds hold`, `fails closed on malformed, future, unknown-version, and out-of-range signed material`, `rejects nested proxy snapshot and signatures material without running traps`, `rejects accessor-backed signatures without reading the accessor`, `rejects proxy parser bytes without invoking traps`, `classifies explicit missing snapshot coverage as stale authority`, `rejects rollback and equal-sequence substitution while producing a deterministic next durable state`, `claims before writing and then live-reobserves durable issuer state, refusing a race`, `treats crash-corrupted observed durable state as a no-write custody contention`, `cannot poison high-water state with a structurally valid parsed forged signature`, `settles future and expired snapshots before custody callbacks`, `binds current authority to exact durable state and rejects verifier, issuer, noncanonical, and async substitutions`, and `has no filesystem, network, process, mutation, or materialization route`). |
| <a id="CM-84"></a>CM-84 | `aih policy generate <admin-root> --apply` resolves the exact vendor baseline-evidence channel before the supported catalog and Workbench render; omission leaves the portable route acquisition-free, while a non-applying administrator root fails before network, process, cache, or output effects. A separate canonical administrator bootstrap pins credential-free HTTPS artifact/attestation locators, exact publisher repository/workflow/issuer/ref/environment identity, the shipped ECC and Superpowers source commits, schema bounds, and cache age. Enterprise uses only its fixed OS/admin-managed root; Vibe uses only the canonical root under the supplied administrator path. Resolution is fresh → reverified last-downloaded → packaged and advances only on the exact plain-data unavailable sentinel, emitted solely for HTTP 404 or 410 on the first artifact request. DNS, TLS, connection, request, timeout, every redirect, and every non-200 status other than 404 or 410 are terminal so reachability cannot force a cache or package downgrade; the baseline origin must answer before the downstream catalog stage runs. Partial, malformed, oversized, stale, wrong-source, wrong-pin, wrong-schema, untrusted, or cache-commit results are also terminal. Fresh and cached subjects repeat the complete checksum/layout/schema/source/publisher verification and a bounded `gh attestation verify` against the exact certificate, SLSA predicate, subject digest, repository, workflow, issuer, ref, and GitHub-hosted runner before use; a cached verdict is never authority. Fresh verified bytes commit through a bootstrap-derived contained owner-only cache slot before the result is rendered. The Workbench model carries only tier, source ids, schema version, digest, age, and resolution time; locators, paths, credentials, signatures, raw attestations, roots, and machine detail remain outside the model. Developer seats never enter this route or run analyzers. | `src/org-policy/admin-baseline-evidence-bootstrap-v1.ts`, `src/org-policy/admin-baseline-evidence-cache-v1.ts`, `src/org-policy/admin-baseline-evidence-operations-v1.ts`, `src/org-policy/generate.ts`, `src/org-policy/studio-model.ts`, and `src/org-policy/studio-template.ts`. | `tests/org-policy/admin-baseline-evidence-operations-v1.test.ts` (`uses fresh evidence before cache and only falls through on literal unavailable`, `treats a request-level acquisition error as terminal before cache custody`, `reverifies cache after literal unavailable and never masks malformed fresh input`, `composes bootstrap, five raw files, live gh verification, and reverified custody cache`), `tests/org-policy/admin-baseline-evidence-cache-v1.test.ts` (`claims one contained cache slot and treats present invalid bytes as terminal`), and `tests/org-policy/admin-baseline-evidence-cli-route.test.ts` (`does not construct the baseline stage without an applied administrator root`). |
| <a id="CM-85"></a>CM-85 | Repository and fetched-source verification state is accepted only in complete, canonical, phase-honest shapes. Managed Codex `enabled_tools` must be a string array before missing tools can be ordinary drift. Git counts reject prefixes, signs, whitespace, overflow, and extra fields; analytics omit unavailable panels/snapshots rather than manufacturing zero. Workspace branch, exact 40- or 64-character lowercase-hex commit, dirty state, and ahead/behind counts come from one porcelain-v2 observation re-read after dependent facts; unavailable or changed observations omit every volatile fact and remain explicit through snapshot, hydrate, and report. A detached HEAD, or an observed branch label not safe for downstream ref use, remains a complete SHA-backed observation: the label is omitted while exact SHA and dirty state are retained, and it is never accepted as a checkout ref. A baseline with an incomplete observation or no branch/SHA evidence reports `UNKNOWN`, never `UNCHANGED`. Applied GitHub trust scans and skill vets read a bounded single-link regular metadata file through the shared safe reader, require its closed fields to bind the exact requested quarantine, and re-observe the same record after analyzers run. Missing, unreadable, malformed, mismatched, or replaced metadata emits one of four blocking `trust.fetch-metadata-*` codes; the requested pin is never fetched provenance. Filesystem transaction property tests exercise bounded generated sequences for inert preview, last-operation semantics, conflicts, successful disk/result state, injected-failure rollback, and concurrent operator-edit preservation. | `tools/repo-ai-tools.mjs`, `src/internals/git.ts`, `src/report/history.ts`, `src/report/velocity.ts`, `src/workspace/state.ts`, `src/workspace/hydrate.ts`, `src/report/workspace.ts`, `src/trust/fetch.ts`, `src/trust/scan.ts`, `src/skill/vet.ts`, `src/internals/fsxn.ts`, `src/internals/verify.ts`, and `src/support/findings.ts`. | `tests/internals/repo-ai-tools.test.ts` (`reports a malformed Codex managed enabled_tools list when it is %s`, `keeps a valid but incomplete Codex enabled_tools list as ordinary drift`), `tests/internals/git.test.ts` (`rejects malformed or unsafe git count %j`, `preserves complete safe non-negative base-10 counts, including zero`), `tests/report/history.test.ts` (`returns unavailable when numstat records contain extra fields`, cumulative overflow regressions), `tests/report/dashboard.test.ts` (`omits velocity panels when numstat records contain extra fields`), `tests/workspace/state.test.ts` (`anchors revision, dirtiness, and divergence in one Git process`, `omits Git's detached HEAD marker from a serialized workspace lock while retaining its SHA`, `omits an unsafe porcelain branch head while retaining coherent SHA evidence`, `omits a Git-valid but unsafe-for-ref-use branch label %s`, `fails closed on a noncanonical branch object ID %j`, explicit diverged/unavailable regressions), `tests/report/workspace-report.test.ts` (`does not present a %s Git observation as clean or unchanged`, `does not treat a %s baseline observation as an unchanged revision`, `does not treat a baseline without SHA or branch evidence as unchanged`), `tests/workspace/hydrate.test.ts` (`hydrates a detached canonical SHA-256 workspace lock SHA without a branch ref`, coherent incomplete-lock acceptance/refusal), `tests/skill/vet.test.ts` (`keeps a %s fetched GitHub metadata record untrusted`, mutation-during-scan regression), `tests/trust/fetch.test.ts` (bounded, linked, malformed, and mismatched metadata classification), `tests/trust/scan.test.ts` (`fails closed when fetched GitHub metadata is %s`, mutation-during-scan and malformed-policy co-failure regressions), and `tests/internals/fsxn.test.ts` (`keeps preview inert and commits every non-conflicting staged sequence as modeled`, `rolls back injected write failures to their pre-transaction state`, `preserves operator-mutated files while rolling back generated writes`). |
| <a id="CM-86"></a>CM-86 | Strict V2 keeps organization evidence and AIH-supported provenance as separate qualification routes under the same exact externally verified V3 organization decision; catalog membership never grants admission. AIH-Supported Qualification Receipt V2 is the only supported receipt version: its canonical bytes bind the exact subject and catalog basis plus entry id, signer key, sequence, predecessor, replay identity, and head validity, with a 4,096-byte canonical source ceiling and 5,970-byte receipt ceiling. Core reads only the fixed receipt, verifies an owner-only copy with an absolute external GitHub CLI against dedicated support repository/workflow roots, then re-observes the original bytes. The package root exposes only the inert V2 artifact verdict. `aih policy supported accept --apply` converts the opaque production verification into bounded versioned signer, replay, head, and head-scoped member custody under the fixed Enterprise/Vibe root discipline; exact slots, a fixed cooperative lock, immutable claims, head CAS, validity, capacity, complete-graph, and postcondition checks refuse gaps, rollback, replay, key mismatch, links, races, partial state, and foreign state. `aih policy supported inspect` is deterministic, scrubbed, and read-only: it separates current-head members from occupied and remaining physical member-record capacity. Superseded member records remain immutable replay/audit evidence and count toward the fixed cap; AIH does not prune them automatically, and capacity exhaustion requires separately authorized administrator migration or archival. `aih policy observe npm-package` selects mutually exclusive qualification branches from the current exact decision: organization-qualified requires its canonical evidence envelope, while AIH-supported rejects that option and requires the freshly reverified receipt plus current durable member custody. The fixed npm observer and lifecycle can then report or persist the exact installed package observation without installing, executing, projecting, signing, releasing, or publishing anything; the opaque qualification and custody binding never leave package-internal process custody. | `src/index.ts`, `src/org-policy/governance-decision-v2.ts`, `src/org-policy/evidence-custody-v1.ts`, `src/org-policy/policy-resolve-v1.ts`, `src/org-policy/npm-package-observer-v1.ts`, `src/org-policy/npm-package-lifecycle-v1.ts`, `src/org-policy/qualification-v1.ts`, `src/org-policy/supported-qualification-receipt-v2.ts`, `src/org-policy/supported-admin-v2.ts`, `src/org-policy/supported-admin-command-v2.ts`, `src/org-policy/upstream-observation-receipt-v1.ts`, `src/org-policy/authority.ts`, `src/internals/execute.ts`, `src/config/json-schema.ts`, `schemas/aih-organization-evidence-envelope-v1.schema.json`, and `schemas/aih-supported-qualification-receipt-v2.schema.json`. | `tests/org-policy/supported-qualification-receipt-v2.test.ts` (`accepts only exact canonical UTF-8 receipt bytes and rejects malformed transport`, `accepts the producer's exact 4,096-byte source and 5,970-byte receipt ceilings`, `requires dedicated roots, verifies an exact private copy, and mints no execution authority`, `re-reads the original fixed receipt after verification and rejects substitution`), `tests/org-policy/supported-admin-v2.test.ts` (`keeps a dry-run phase-honest and writes no custody files`, `makes exact reacceptance write-free and verifies its postcondition`, `commits a valid direct successor with immutable slots before the head CAS`, `reports retained member-record capacity separately from current-head members`, `rejects a store whose active head lost its current member`, `rejects a schema-valid orphan signer claim`, `rejects a schema-valid replay claim whose signer is absent`, `registers real supported accept and scrubbed read-only inspect commands`), `tests/org-policy/npm-package-observer-v1.test.ts` (`keeps verified authority phase-honest when organization evidence is missing, unsafe, or malformed`, `routes an AIH-supported npm decision without organization evidence, then refuses absent durable custody`, `invalidates a branded supported receipt when its custody head is superseded`, `rejects a caller evidence path for an AIH-supported decision before it can select a weaker branch`, `observes only the signed exact installed package without executing it`), `tests/org-policy/npm-package-lifecycle-v1.test.ts` (`persists an evidence-free AIH-supported lifecycle only after the branded custody accept path`, `refuses a supported lifecycle when a foreign %s custody entry appears after transaction assertions`), `tests/internals/execute.test.ts` (`accepts only a contained, non-linked absolute external lock shared by different target roots`), and `tests/config/json-schema.test.ts` (`keeps the committed schema files in sync with zod`). |
| <a id="CM-87"></a>CM-87 | `aih policy lifecycle npm-package` turns the fixed, decision-selected npm observation into preview-first durable audit history without becoming a package manager. The organization-qualified route requires its canonical evidence envelope; the AIH-supported route rejects that option and requires the freshly verified Receipt V2 plus current durable supported custody. Every apply freshly re-observes the exact V3 authority, current Decision V2, applicable qualification, npm v3 lockfile, installed manifest, and effect. It writes immutable content-addressed records before advancing a subject head, binds one stable subject/registry/package/target/effect lineage, appends exact version or integrity bumps, and keeps authenticated revocation non-effective without claiming package removal. Apply pins every observed input, uses one fixed store-wide cooperative lease and a canonical aggregate capacity guard, refuses stale or foreign plans before mutation, and re-reads the exact claim, binding, record, head, capacity state, and complete bounded lineage before success. The supported route additionally pins its receipt and signer/replay/member/head assertions and revalidates the complete bounded supported-custody graph after lifecycle writes. Deadlines, capacity ceilings, link and path checks, scratch custody, lock ownership, rollback, crash-orphan, replay, fork, collision, and detached-state checks fail closed. This is cooperative local AIH writer coordination rather than hostile-process isolation; coordinated deletion or rollback of all target-local indexes remains detectable only with organization-controlled whole-store evidence. Preview writes nothing, and the command never installs, updates, removes, configures, executes, signs, releases, or publishes a package. | `src/org-policy/npm-package-lifecycle-v1.ts`, `src/org-policy/npm-package-observer-v1.ts`, `src/org-policy/supported-admin-v2.ts`, `src/internals/plan.ts`, `src/internals/execute.ts`, and `src/internals/fsxn.ts`. | `tests/org-policy/npm-package-lifecycle-v1.test.ts` (`delegates an evidence-free AIH-supported lifecycle request to the observer without preview or apply writes`, `persists an evidence-free AIH-supported lifecycle only after the branded custody accept path`, `refuses a supported lifecycle when its fixed receipt changes after transaction assertions`, `keeps preview zero-write, then appends immutable observation records before advancing a subject head`, `keeps a complete store at 256 heads readable and refuses an official 257th lineage without writes`, `refuses a stale cross-lineage last-slot plan before any losing lifecycle mutation`, `uses one stable store-wide lock across first-time lineage changes`, `uses one stable lineage across an exact npm version bump and chains the new record`, `persists the full authenticated V3 revocation once, without claiming package removal`, `refuses an apply after the sealed observation deadline without creating lifecycle state`, `accepts a valid staged record at capacity and refuses record 4,097`, `reports a detached store rather than claiming fulfilled after an apply-time postcondition failure`, `reports a detached store when a non-cooperative writer forks the committed lineage`), `tests/internals/fsxn.test.ts` (transaction preflight, lease ownership, deadline, rollback, scratch, concurrency, and durable-rename regressions), and `tests/internals/execute.test.ts` (`passes a write-local scratch precondition to the transaction`, `refuses a noncanonical root-relative plan commit lock before writing`, `accepts only a contained, non-linked absolute external lock shared by different target roots`). |

| <a id="CM-88"></a>CM-88 | The governed effective-state resolver, `aih policy evaluate <root> --no-log --json`, and governance review consume the fixed npm lifecycle V1 store as read-only observed state. The reader incrementally bounds and sorts heads, partitions, claims, subjects, and per-lineage records before materializing their names, with explicit ceilings of 256 active lineages, 16,384 aggregate records, and 4,096 records in one lineage; rejects linked or noncanonical custody; validates exact bounded subject and decision identifiers, supported targets, head filenames, subject claims and bindings, current record digests, complete ancestry, and detached/forked residue; and recognizes a transaction backup only when it is the exact canonical predecessor of the current head. Resolution freshly verifies the V3 authority receipt, exact-matches its current Decision V2 package/npm subject, source, target, authority digest, observation, and subject-wide rejection/revocation state, refuses future-dated observations, accepts `install` within a valid multi-effect decision without widening the recorded effect, and keeps `observed-effective`, `partial`, `withheld`, `refused`, `revoked`, `stale`, and `drifted` distinct. Every current non-effective lifecycle head blocks evaluation and remains explicit in deterministic digest/report data. Capacity exhaustion is reported distinctly as `over-capacity`, never as corruption, and blocks both evaluation and projection; operators retain the complete store as organization-controlled evidence and reconcile onto a newly governed target rather than pruning target-local audit history. Observation expiry alone does not freeze unrelated policy projection; unsafe custody, authority failure, rejection, revocation, drift, and every other non-effective lifecycle reason still block projection. The adapter has no policy-candidate schema, projector, package-manager, target-write, run-ledger, install, update, removal, configuration, execution, signing, publication, or independent qualification route; organization evidence and AIH-supported custody remain observer/lifecycle preconditions, and existing custom stdio and remote MCP candidates remain non-projectable. | `src/org-policy/npm-package-lifecycle-v1.ts`, `src/org-policy/npm-package-effective-state-v1.ts`, `src/org-policy/runtime.ts`, `src/org-policy/effective.ts`, `src/org-policy/evaluate.ts`, and `src/report/governance-review.ts`. | `tests/org-policy/npm-package-lifecycle-v1.test.ts` (`exposes an intact current npm lifecycle chain as read-only observed effective state`, `refuses a self-consistent future durable observation before it can become effective`, `reports external over-capacity state distinctly and blocks evaluate and projection`, `bounds aggregate effective-state history work before walking hostile chains`, `bounds reporting lifecycle partition enumeration at the configured head cap`, `keeps a 24-hour observation effective after 60 seconds, while stale history blocks evaluate but not projection`, `accepts a current multi-effect decision when its durable observation remains install-only`, `refuses hostile durable lineage and decision identifiers before they can render`, `refuses current authority substitutions of the recorded decision, source, subject, target, or effect`, `treats a removed head with durable records as partial and blocks policy evaluate`, and deterministic/zero-write regressions), `tests/report/governance-review.test.ts` (`surfaces observed npm lifecycle as read-only state rather than a projected subject`), and existing custom stdio/remote non-projectability regressions in `tests/org-policy/effective.test.ts` and `tests/org-policy/governance-projection.test.ts`. |

| <a id="CM-89"></a>CM-89 | `aih policy managed usage-metering` is one closed AIH-owned adapter, not a generic materializer. Its code-derived descriptor fixes the `tool/usage-metering` subject/source, adapter id/version/digest, `configure` effect, and `claude` and `codex` targets; callers can provide only an exact current Decision V2 id/digest, a contained canonical organization-evidence path, and one fixed target. Preview and inspect are zero-write. Apply requires live verified organization authority and exact organization qualification, commits a strict canonical V4 claim before effect, re-observes authority/evidence/ownership, caps every mutation by the live authorization expiry and a short commit deadline, writes only the fixed recorder/hook/ignore marker, and finalizes only while exact output assertions hold. Internal receipt transitions use exact CAS so a real Git configure/revoke lifecycle does not mistake Core's own newly written custody for administrator dirt; ordinary mutation of a pre-existing dirty host or ignore file remains gated. The bounded successor receipt rejects V1-V3, links, malformed grammar, replayed history, non-code-derived ownership, output forgery, drift, and ambiguous custody. Authenticated revocation records `revoking` before subtracting only the exact code-derived recorder, hook entry, and ignore marker, then retains `revoked` history; self-digested pre-existence metadata is never deletion authority, non-AIH host/ignore content remains, and no process termination is claimed. The packed clean-root proof uses a protected external PolicyBundle V2 to exercise successful configure, inspect, and authenticated revoke plus fail-closed missing and malformed authority. It proves Core's exact-byte custody boundary, not the adopter host's ACL, and uses no fabricated GitHub authority. | `src/org-policy/aih-managed-usage-adapter-v1.ts`, `src/org-policy/aih-managed-usage-audit-v1.ts`, `src/org-policy/aih-managed-usage-command-v1.ts`, `src/org-policy/aih-managed-usage-result-v1.ts`, `src/org-policy/aih-managed-usage-revocation-v1.ts`, `src/org-policy/authority.ts`, `src/org-policy/project.ts`, `src/commands/index.ts`, and `tools/verify-cold-aih-managed-usage.mjs`. | `tests/org-policy/aih-managed-usage-adapter-v1.test.ts` (`derives one fixed configure descriptor from packed Core code`, `commits no custody claim after the authorized transaction window closes`, `completes code-owned configure and revoke phases in a real Git worktree`, `configures and revokes from one protected policy file without an authority workflow`, `keeps the ordinary worktree gate on pre-existing administrator output`, plus forged ownership/output/history, links, idempotence, refresh, revocation, and result-honesty regressions), `tests/org-policy/authority-live-custody.test.ts` (`mints no authority when the live receipt changes while gh verifies its private copy`, `mints no authority when the live receipt gains a hard link during verification`), `tests/commands/policy-managed-usage-command.test.ts` (`exposes only the fixed describe, reconcile, and inspect lifecycle`), `tests/org-policy/aih-managed-usage-packed-proof.test.ts` and `tools/verify-cold-aih-managed-usage.mjs` (packed protected-file configure/revoke proof without fabricated authority), plus `tests/org-policy/effective.test.ts` and `tests/org-policy/governance-projection.test.ts` (custom stdio/MCP and third-party executable sources remain non-projectable). |
| <a id="CM-90"></a>CM-90 | `aih policy observe upstream-artifact` governs exact organization-chosen tool, skill, MCP, or package files absent from AIH catalogs without becoming an installer or projector. The strict canonical manifest binds the organization-qualified Decision V2 id, exact subject/source digests, target/effect, accountable integration owner/version, and sorted bounded root-relative file digests; its raw digest must be present in the attributable organization evidence. The same canonical evidence/manifest request-path grammar is enforced before authority verification and by durable lifecycle parsing. Mixed-case paths remain valid while portable case duplicates, trailing-dot/space and Windows-device aliases, repeated physical identities, AIH custody aliases, and linked evidence fail closed. The fixed observer accepts no executable, callback, network, installer, or path escape and re-observes authority, evidence, manifest, and every regular single-link file before success. `aih policy lifecycle upstream-artifact --apply` records immutable content-addressed observations, exact request paths, owner-stable version/source updates, and authenticated revocations before advancing the exact lineage head; revocation remains failing/non-effective and never claims removal or process termination. Claims, aggregate capacity, file assertions, one store-wide commit lock, record-before-head order, authenticated backups, complete ancestry, orphan detection, and postconditions fail closed on links, substitution, TOCTOU, ambiguity, forks, rollback, races, or detached state. Evaluate and governance review freshly verify authority once, repeat the fixed read-only observation for each current record, then re-read and exact-compare the bounded lifecycle snapshot before exposing any `observed-effective` state; missing or drifted live inputs and substituted stored verifier/installed identities remain non-effective. The packed installed-byte proof uses a protected PolicyBundle V2 in a disposable target to prove successful file authority, observation, lifecycle update, and authenticated revocation. It does not claim that Core proves the deployment's administrator ACL. | `src/org-policy/upstream-artifact-manifest-v1.ts`, `src/org-policy/upstream-artifact-observer-v1.ts`, `src/org-policy/upstream-artifact-lifecycle-v1.ts`, `src/org-policy/upstream-artifact-effective-state-v1.ts`, `src/org-policy/runtime.ts`, `src/org-policy/effective.ts`, `src/org-policy/evaluate.ts`, `src/report/governance-review.ts`, `src/commands/index.ts`, `src/config/json-schema.ts`, `schemas/aih-upstream-artifact-manifest-v1.schema.json`, and `tools/verify-cold-upstream-artifact-lifecycle.mjs`. | `tests/org-policy/upstream-artifact-manifest-v1.test.ts` (`round-trips one canonical manifest and derives a domain-separated digest`, `accepts exact mixed-case visible artifact paths`, `rejects noncanonical bytes and the byte ceiling before parsing`), `tests/org-policy/upstream-artifact-observer-v1.test.ts` (`exposes no observer, verifier, effect, file, command, callback, or network override`, `rejects non-canonical request paths before verification or lifecycle writes`, `rejects two manifest rows that resolve to one hard-linked file identity`, `withholds effective state after organization evidence gains a hard link`, `does not promote stale lifecycle state when the store is %s during live observation`, `does not promote a lifecycle record after its live inputs disappear`, `re-observes persisted paths live and withholds effective state after artifact drift`, `drifts a self-consistent stored verifier or installed identity from the fresh receipt`, `fails closed when an observed file is linked or changes after validation`, `keeps preview zero-write, then records immutable lifecycle history without changing observed files`, `appends an exact version and source update and rejects a rollback`, `rejects malformed canonical lifecycle records and detached store structure`, `records a current authenticated revocation as non-effective without changing artifact files`, `keeps authority loss, authority drift, rejection, decision drift, and store corruption distinct`, `governs one catalog-absent organization tool with a protected PolicyBundle and no authority workflow`), `tests/config/json-schema.test.ts` and `tests/config/upstream-artifact-manifest-package.test.ts` (generated schema and package surface), `tests/cli.test.ts` and `tests/internals/exec-locality.test.ts` (closed command registration), `tests/report/governance-review.test.ts` (`surfaces catalog-independent artifact lifecycle without claiming installation or projection`), and `tools/verify-cold-upstream-artifact-lifecycle.mjs` (packed protected-file authority proof without fabricated authority). |

| <a id="CM-91"></a>CM-91 | Enterprise may supply organization authority as one Workbench-generated PolicyBundle V2 file selected by an absolute `AIH_ORG_POLICY` path outside the governed target. The browser accepts structured issuer, exact GitHub, npm, PyPI, OCI, remote-content, or AIH source identity, artifact kind, target/effect, evidence, policy/control, actor, and reason fields; explicitly authors either an organization-qualified basis or the exact signer/catalog/head/member binding from an independently verified AIH-supported Qualification Receipt V2; computes the canonical Decision V2 source, subject, and revocation digests with Web Crypto; and offers only a read-only bundle preview and file download, not editable raw JSON. The supported receipt qualifies the exact subject bytes but remains `not-authoritative` for organization admission. Repeated decisions cover exact tools, skills, MCP servers, packages, and profiles. The bundle reuses OrgPolicy, the V3 Decision V2 authority payload, and existing lifecycle stores; Vibe and PolicyBundle V1 do not gain authority. Core requires strict bounded JSON with duplicate-key and invalid-UTF-8 rejection, matching bundle/authority issuance, a current at-most-90-day window, a regular single-link file, no symlinked parents, two identity-and-byte observations at minting, live re-observation at every consumer, and an exact external file assertion before any authority-dependent transaction effect. Assertion preflight happens before lock-anchor creation and repeats under the acquired lock. Nested target resolution, ECC and Superpowers evidence, standalone MCP planning, and governed ECC materialization reuse one verified policy observation. The direct ECC writer revalidates the same assertion and lease before and after its rollback-capable effects. Child-process effects run under the same renewable, ownership-checked cooperative lease with immediate before/after authority revalidation; a post-effect authority failure blocks later effects and deferred writes while stating that the completed external process cannot be rolled back. Missing, malformed, stale, linked, inside-target, replaced, or raced custody fails closed. Existing lifecycle history binds prior authority digests, but a fresh target has no global bundle-version high-water mark; the administrator's distribution system must prevent rollback to older still-valid bytes. The GitHub-attested receipt remains an optional transport. Core never writes the authority file and cannot prove its host ACL or actor identity; the administrator/MDM deployment owns file replacement and process-configuration access. | `src/contract/native-strict-json-object-v1.ts`, `src/org-policy/studio-model.ts`, `src/org-policy/studio-template.ts`, `src/org-policy/studio-protected-authority.ts`, `src/org-policy/authority-v3.ts`, `src/org-policy/authority.ts`, `src/org-policy/schema.ts`, `src/org-policy/bundle.ts`, `src/org-policy/drift.ts`, `src/org-policy/project.ts`, `src/org-policy/validate.ts`, `src/bootstrap-ai/index.ts`, `src/adopt/apply.ts`, `src/baseline-evidence/pipeline.ts`, `src/ecc/pipeline.ts`, `src/superpowers/pipeline.ts`, `src/mcp/catalog.ts`, `src/mcp/index.ts`, `src/ecc/governed-lifecycle.ts`, `src/ecc/materialization.ts`, `src/internals/plan.ts`, `src/internals/execute.ts`, `src/internals/fsxn.ts`, `src/org-policy/npm-package-observer-v1.ts`, `src/org-policy/npm-package-lifecycle-v1.ts`, `src/org-policy/upstream-artifact-observer-v1.ts`, `src/org-policy/supported-qualification-receipt-v2.ts`, and `src/org-policy/supported-admin-v2.ts`. | `tests/org-policy/studio-administrator-journey.test.ts` (`authors a protected Enterprise policy file from form fields without raw JSON`, `authors the exact AIH-supported qualification basis required by a signed receipt`, `refuses an AIH-supported decision with incomplete catalog binding`, `refuses protected-file authority in Vibe posture and outside the 90-day window`, `authors tool, skill, Agent, MCP, and package approvals and a valid revocation`, `authors the exact AIH release identity used by an AIH-managed adapter`, `authors every exact Decision V2 provider identity without hand-authored JSON`), `tests/org-policy/bundle.test.ts` (`accepts the authority-bearing V2 envelope and rejects unknown versions`, `rejects a V2 envelope without its exact V3 decision authority`), `tests/org-policy/file-policy-authority.test.ts` (`loads the embedded policy and mints exact V3 decision authority without a workflow`, `admits the verified protected file as the policy-project managed mutation source`, `applies standalone MCP from a pinned protected external policy`, `keeps the protected A observation through an ECC evidence pipeline A-to-B-to-A swap`, `does not grant file authority to a developer-controlled bundle inside the target`, `refuses expired, hard-linked, and symlink-parent policy authority`, `rejects ambiguous and oversized policy bytes before authority fallback`, `rejects non-canonical Unicode, negative zero, and non-object policy roots`, `pins the exact external file inside a mutating transaction`), `tests/superpowers/evidence.test.ts` (`refuses an ordinary policy override before any Superpowers source or receipt effect`), `tests/adopt/adopt.test.ts` (`reuses its outer policy observation so nested planning cannot widen A with B`), `tests/ecc/governed-lifecycle-command.test.ts` (`refuses protected authority A-to-B before the governed writer creates any effect`), `tests/ecc/materialization.test.ts` (`refuses a changed authority assertion before it materializes any owned bytes`, `rolls back every materialized byte when authority changes during the guarded commit`), `tests/internals/execute.test.ts` (`fails closed when authority changes during a successful child effect`, `fails closed when authority changes during a failing child effect`), `tests/internals/fsxn.test.ts` (`renews an owned lease while a guarded async effect remains pending`, `fails closed when a guarded async effect loses its lease before renewal`, `revalidates authority after a guarded async effect rejects`), `tests/org-policy/authority-live-custody.test.ts` (optional attested-transport TOCTOU), `tests/org-policy/upstream-artifact-observer-v1.test.ts` (`governs one catalog-absent organization tool with a protected PolicyBundle and no authority workflow`), `tests/org-policy/supported-qualification-receipt-v2.test.ts` (`combines protected-file organization authority with the separate support attestation root`), and `tests/org-policy/npm-package-lifecycle-v1.test.ts` (`pins the exact authority, evidence, lockfile, and installed manifest used by observation`). |

CM-91 policy-selection amendment: the shared CLI accepts `--policy <file>` for one explicit team or
project policy source. The flag overrides `AIH_ORG_POLICY`; an explicitly selected missing or unreadable
file fails closed instead of falling back to `aih-org-policy.json`. The Workbench may download the policy
under one safe administrator-chosen JSON filename; browser download folder selection remains a browser
operation and does not change authority or custody. Scanner evidence remains a separate attributable
record: browser evidence import is inspection/preflight only, while runtime `--evidence <file>` remains
root-relative and is revalidated by the existing evidence bindings. Proof:
`tests/commands/run.test.ts` (`lets an explicit --policy file select the org floor and override
AIH_ORG_POLICY`, `fails closed when an explicit --policy file is missing`),
`tests/org-policy/studio-custom-next-action.test.ts` (`downloads the policy under one safe custom team
filename and names its --policy command`, `rejects a folder path as a policy download filename`), and
`tests/contract/command-surface.test.ts` (shared command-surface contract).

CM-47 review-workspace amendment: the Artifacts surface creates one mixed, explicitly
non-authoritative intake for up to 100 MCP, Skill, and Agent items and consumes one Scanner evidence
bundle for that batch. A bounded team review workspace can save and restore the current draft policy,
artifact intake, and validated evidence-bundle history in one strict JSON object. Its required
`authority.state` is `not-authority`; unknown members, unsafe policy filenames, malformed policy,
invalid intake, duplicate evidence bundles, and altered evidence digests fail before any review state
is replaced. The workspace is resumable review state, never the deployable protected policy or an
evidence/organization authority. Proof: `tests/org-policy/studio-artifact-intake.test.ts` (`saves and
restores one non-authoritative team workspace with policy, intake, and evidence`, `rejects an unsafe or
corrupted team workspace without partially replacing review state`).

CM-91 composition amendment: ECC request construction, ordinary ECC profile lifecycle acquisition
and mutation, standalone Usage ownership, and Init composition retain the exact verified authority
observation. ECC profile install/update composes projection and native registration under the same
file assertions, deadline, and cooperative lock; receipt-bound subtractive uninstall remains
independently authorized by installed custody. Init carries every nested phase's file assertions,
earliest deadline, and cooperative lock, and rejects a conflicting assertion or lock before it can
expose a mixed-authority plan. The implementation seams are `src/ecc/pipeline.ts`,
`src/ecc-profile/command.ts`, `src/usage/index.ts`, and `src/init/index.ts`. Proof:
`tests/org-policy/file-policy-authority.test.ts` (`keeps protected policy A for ECC request selection
across an A-to-B-to-A swap`, `refuses a protected-policy A-to-B swap before ordinary ECC lifecycle
effects`), `tests/ecc-profile/command.test.ts` (`rolls back the compound install when native
registration preflight fails`), `tests/usage/usage.test.ts` (`refuses a governed policy substituted
after an ungoverned ownership observation`), and `tests/init/init.test.ts` (`refuses an MCP phase that
observes protected policy B after init verified policy A`).

CM-58 diagnostic amendment: blocked projector evaluations retain the stable
`missing-projector` and `unsupported-target` danger codes and also name the
resolver cause: a disabled invocation, an unselected or unsupported target, or
an authorable-only custom source that has no integrity-enforcing materializer.
The `supported` field remains the candidate's shipped adapter capability even
when posture disables the invocation; `available` remains the selected runtime
target set, and coverage stays `blocked`. Thus a Vibe Kiro evaluation reports
`supported=claude,kiro; available=kiro; blocked`, while an unsupported custom
stdio materializer still reports `supported=none`.
The proof is `tests/ecc/command.test.ts` (`lists every governed lifecycle target
and scopes Kiro to rules and skills`), `tests/org-policy/effective.test.ts`
(`names vibe posture when it disables the requested projector`, `explains that a custom Kiro stdio source has no
integrity-enforcing materializer`), and
`tests/org-policy/governance-projection.test.ts` (`keeps an externally evidenced
custom stdio MCP authorable but blocked without an integrity-enforcing
projector`, `maps the resolved vibe posture to its actionable projector reason`).

## Posture Table

`src/config/posture.ts` converts each governance finding into an `allow`, `warn`,
or `deny` verdict for the active posture.

| Control family | vibe | enterprise |
| --- | --- | --- |
| `allow` findings | allow | allow |
| `trust-danger` | deny | deny |
| `risk-gates` | warn | warn |
| `secrets` | warn | deny |
| `path-portability` | warn | deny |
| `contract-freshness` | warn | deny |
| `command-policy`, `mcp`, `ca-trust`, `verify`, `trust-origin` | warn | deny |
| baseline evidence missing/mismatch | warn; no receipt | deny |
| baseline evidence blocked/danger | deny | deny |

Regression proof: `tests/config/posture.test.ts` covers the posture normalizer,
precedence (`flag > marker > env > default` before org floors), org-policy floor
clamping, `AIH_ORG_POLICY`, and the table above.

## Offline and Network Boundary

The package dependencies are `commander`, `jsonc-parser`, `yaml`, and `zod`; the
core CLI does not depend on an HTTP client library. Normal command plans are
local actions, probes, digests, or operator docs. Network-capable behavior exists
only in named surfaces where the operator invokes that capability: release
verification (`npm`, `gh`, `cosign`), source acquisition/trust flows, marketplace
or fleet-bundle attestation, cert/TLS probes such as `curl -Iv https://pypi.org`,
and generated telemetry docs/fetcher scripts that the operator must run.

The statement this repo supports is therefore: no default phone-home and no hidden
telemetry transmission. It is not a claim that every command is offline-only.

## Canon MUST Map

Issue #507 acceptance: each imperative line in the generated Layer-2 canon maps
to its enforcing check, or is labeled agent-directed. The authored source of the
generated text is `src/bootstrap-ai/canon.ts`. The regression gate is
`tests/bootstrap-ai/canon-must-map.test.ts`: it regenerates the reachable canon
surface (both canon modes, all supported CLIs, all registered baselines, the
three test-routing branches, bootloaders as written with their managed-block
markers), extracts imperative lines by the token set below, and fails closed
both ways — a generated imperative line with no row here, or a row whose anchors
no longer match any generated line.

Imperative tokens (word-anchored; `must-have` forms and mid-sentence `none`/`not`
do not match): `must`, `never`, `do not`, `don't`, `require`/`requires`/`required`,
`need`/`needs`, a line-leading `No `, and `no silent failures`.

Classes:

- **generation-invariant** — a property of the generated tree; a drift probe or
  canon lint fails `aih bootstrap-ai --verify` on violation.
- **governance** — a governance gate enforces it (`aih secrets --verify`,
  guardrail artifacts, posture grading).
- **agent-directed** — instructions to the consuming agent; not aih-enforceable
  and labeled `agent-directed, not aih-gated`. A named backstop is partial and
  does not change the class.

Each anchor below is an exact substring of the generated line(s) it maps.

| ID | Canon directive (anchors) | Class | Enforcing check |
| --- | --- | --- | --- |
| CANON-01 | `do not edit by hand` — the shared-block marker note on each bootloader | generation-invariant | `bootloaderProbe` in `src/bootstrap-ai/index.ts` fails `aih bootstrap-ai --verify` with `cli.bootloader-drift` when the fenced block differs from the canonical body; `mergeManagedBlock` in `src/internals/markers.ts` regenerates only the fenced region. |
| CANON-02 | `Never hand-edit inside the markers` (REGENERATION.md); `Never hand-edit inside a shared-block marker` (harness-update.md) | generation-invariant | Same drift gate: `bootloaderProbe` and `generatedTextProbe` in `src/bootstrap-ai/index.ts` (`cli.bootloader-drift`, `canon.generated-drift`); edits outside the markers survive via `mergeManagedBlock` in `src/internals/markers.ts`. |
| CANON-03 | `aih never regenerates it` — the carved `rules/project-canon-extension.md` | generation-invariant | `src/adopt/apply.ts` writes the carved extension with `once: true`; an existing write-once file resolves to the `kept` effect in `src/internals/execute.ts`; `bootstrapAiPlan` in `src/bootstrap-ai/index.ts` plans no write to that path. |
| CANON-04 | `never overwritten` — the write-once / author-fill file class in harness-update.md | generation-invariant | Write-once plans (`once: true` in `src/internals/plan.ts`) resolve to the `kept` effect in `src/internals/execute.ts`; `src/scaffold/index.ts` marks the author-owned context files write-once. |
| CANON-05 | `No secrets in code` — the shared invariant bullet | governance | Secret scan gate: `src/secrets/scan.ts` with posture-graded `--verify` probes in `src/secrets/probes.ts`; `gradeVerdict` in `src/config/posture.ts` denies secrets findings at enterprise (CM-16); gitleaks and pre-commit artifacts via `src/guardrails/gitleaks.ts` and `src/guardrails/precommit.ts` (CM-17). |
| CANON-06 | `Do not open` `.env*` / `secrets/**`; `validate secret presence with` `aih secrets --verify` | governance | Generated read-deny artifacts: `settingsDenyPatch` and `.claudeignore` in `src/secrets/templates.ts`; the shell secret-reader deny tier in `src/guardrails/command-policy.ts` (CM-17); the filename scan reads no `.env*` or root `secrets/**` contents (CM-16). Read-avoidance in a tool that consumes none of these artifacts remains agent compliance. |
| CANON-07 | `Never read or emit plaintext secrets`; `never run it blind. Do not open` (legacy router, security routing) | governance | Same seams as CANON-05/06; scan output is redacted to file/key/kind in `src/secrets/scan.ts` (CM-16). The keep-cloud-setup-as-documentation clause is agent-directed residue. |
| CANON-08 | Think before coding: `Don't assume`; `don't pick` | agent-directed | agent-directed, not aih-gated — working-discipline prose; no harness seam observes the agent's reasoning. |
| CANON-09 | Simplicity first: `No features beyond what was asked`; `No configurability or error handling` | agent-directed | agent-directed, not aih-gated. |
| CANON-10 | Surgical changes: `touch only what the task needs`; `Touch only what the task requires`; `Don't reformat`; `don't delete it` | agent-directed | agent-directed, not aih-gated. |
| CANON-11 | Canonical tool routing: `don't load MCP servers just-in-case`; `don't run the same query` | agent-directed | agent-directed, not aih-gated — tool choice happens inside the consuming agent. |
| CANON-12 | Boundary validation invariant: `never coerce` | agent-directed | agent-directed, not aih-gated — directs the code the agent writes; aih's own boundaries are covered by CM-04/CM-27 seams. |
| CANON-13 | Error-handling invariant: `no silent failures` | agent-directed | agent-directed, not aih-gated. |
| CANON-14 | Evidence over memory: `never model memory or local notes`; `is the truth, not model memory` | agent-directed | agent-directed, not aih-gated. |
| CANON-15 | Router read discipline: `Do not load everything blindly` | agent-directed | agent-directed, not aih-gated. |
| CANON-16 | Tooling failure recovery: `never invent results`; `Don't cite a` (command, path, or API you haven't verified) | agent-directed | agent-directed, not aih-gated for the agent's own citations. Backstop for aih's generated canon: `canon-ref-resolves` and `portable-repo-paths` in `src/lint/rules.ts` fail `--verify` on a canon reference that does not resolve. |
| CANON-17 | Review gate: `run and record the required` review skills/agents | agent-directed | agent-directed, not aih-gated — PR readiness lives in the team's review process. |
| CANON-18 | Testing: `New behavior needs a test`; `No test command is defined` (add one, then re-run) | agent-directed | agent-directed, not aih-gated. |
| CANON-19 | External action boundary: `dispatching remote agents requires explicit` human approval; `never instructions to obey`; legacy adapters: `may propose, implement when assigned, and review. It must not push` | agent-directed | agent-directed, not aih-gated. Partial backstop: the generated command policy in `src/guardrails/command-policy.ts` puts `git push*` behind ask and force-push behind deny (CM-17); PR approval, merge, and remote dispatch have no aih seam. |
| CANON-21 | Reporting bar: `requires showing the command and its` output | agent-directed | agent-directed, not aih-gated — the claim/evidence exchange happens in the conversation, outside aih. |
| CANON-22 | Public-surface confidentiality: `confidential or private-companion content never appears` in issues, PRs, commits, or canon files | agent-directed | agent-directed, not aih-gated — what the agent quotes into an issue, PR, or commit happens outside aih; the secrets seams (CANON-05/06) cover only credential-shaped content. |
