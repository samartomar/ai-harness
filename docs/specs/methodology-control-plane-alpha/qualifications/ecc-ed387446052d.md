# ECC Q7 official real-inert qualification

> Status: `QUALIFICATION_BLOCKED` at `plannable`. This Phase A source-data result
> does not authorize install, activation, switching, or Phase B research.

## Source preparation boundary

The operator specifically authorized use of the official `affaan-m/ECC` repository
for this reevaluation. The evaluator resolved `main` with `git ls-remote --symref`,
then fetched the returned immutable commit into a new local staging repository and
checked it out detached with Git hooks disabled and submodules disabled. The staging
action did not run ECC code, an ECC installer, a hook, preview, dry-run, repair,
updater, or uninstaller.

This manually authorized staging action is not an AIH product remote-acquisition
feature. The Phase A product boundary continues to require an operator-supplied,
exact local checkout and does not acquire provider sources itself.

## Exact selected source

| Field | Value |
| --- | --- |
| Local root | `C:/Users/samar/Documents/Codex/2026-07-15/do-not-weaken-trust-rules-and/work/baseline-inputs/ECC-official` |
| Git remote identity | `affaan-m/ECC` (`https://github.com/affaan-m/ECC.git`) |
| Resolved branch at source preparation | `main` |
| Resolved commit | `ed387446052dfbc6b52de149406b70efa65edc59` |
| Source tree SHA-256 | `da82d9f572a7e1f0bf0d6aec138943fbda1a8b7e384aa38b088f521d9c34a3e6` |
| Canonical repository expected by the adapter | `affaan-m/ECC` |
| Canonical source match | yes |

The detached staging checkout was clean before and after qualification. The earlier
fork result in [ecc-4ba9cf058c19.md](ecc-4ba9cf058c19.md) is not evidence for this
official source tuple.

## Inert discovery

The adapter read only the exact source tree, `package.json`, and fixed source paths.
It called `git -C <source-root> rev-parse --verify HEAD` before and after hashing the
tree. It did not import, invoke, preview, dry-run, install, repair, update,
uninstall, or otherwise execute ECC code.

| Field | Value |
| --- | --- |
| Provider kind | `hybrid-catalog-runtime` |
| Installer-contract fingerprint | `665a23c41c92e5765acebceefdb27cdb15fb6874de7cf64544cd2e94eec7cc4b` |
| Inert proposed-plan digest | `92e1a05986fae3eecb8972233f06de622802899047e2bd523a1f101122d4da86` |
| Provider code executed | no |
| Write, process, service, network, updater, runtime, uninstall impacts | all `unknown` |

The observed source topology contains `.agents`, `.codex`, `agents`, `commands`,
`hooks`, `mcp-configs`, `rules`, and `skills`. Static installer-related entries are
`install.ps1`, `install.sh`, `scripts/install-apply.js`, `scripts/install-plan.js`,
and `scripts/uninstall.js`; static update and repair entries are
`scripts/auto-update.js` and `scripts/repair.js`. These are source facts only. Their
presence does not prove an enabled updater, a destination, an installer contract, or
runtime behavior.

Declared source requirements are Node `>=18` and the pinned Yarn package-manager
string in the selected manifest. All proposed project-native, profile-home, and
machine-exclusive destinations remain `unknown`.

## Exact compatibility outcome

The evaluated tuple used Codex `0.144.1`, build `cbacbb97`, host-contract version
`codex-0.144.1-windows-x64-v1` with `partial` coverage, Windows `10.0.26200` x64,
Node `24.13.1`, npm `11.8.0`, profile-home isolation, and policy
`enterprise-core-v1`. The adapter implementation hash was
`3e53ebfdd837fb7aee460a1235c3d733f6662586ed48a7c1c9e66901b5b3935d` for this
evaluation.

No reviewed compatibility row matches that exact tuple. The result is:

```text
classification: QUALIFICATION_BLOCKED
support level: plannable
finding: ADAPTER_COMPATIBILITY_UNKNOWN
mutation research eligible: no
provider code executed: no
```

No existing methodology evidence joined. The current AIH baseline lock names a
different repository and commit (`samartomar/ECC` at
`16563d4a30f17d097cc4629f6d97e02adf823016`) and stores component-level baseline
evidence rather than this exact whole-source methodology tuple. It cannot be
inherited by this result.

## Stop condition

Canonical repository identity removes the prior
`PROVIDER_REPOSITORY_NONCANONICAL` finding, but it does not establish compatibility,
isolation, or a complete Codex load-surface contract. Do not add this tuple to the
supported compatibility set or begin Phase B. A later retry requires independently
reviewed compatibility and isolation evidence; provider execution remains out of
scope until a separate Phase B authorization.
