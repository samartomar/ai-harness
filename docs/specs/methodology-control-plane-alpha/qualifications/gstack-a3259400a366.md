# gstack Q8 official real-inert qualification

> Status: `QUALIFICATION_BLOCKED` at `plannable`. This Phase A source-data result
> does not authorize setup, install, activation, switching, or Phase B research.

## Source preparation boundary

The operator authorized continuation with the canonical `garrytan/gstack` source.
The evaluator resolved `main` with `git ls-remote --symref`, then fetched the
returned immutable commit into a new local staging repository and checked it out
detached with Git hooks disabled and submodules disabled. The staging action did not
run gstack code, setup, installer, hook, preview, dry-run, updater, daemon, browser,
or uninstaller.

This manually authorized staging action is not an AIH product remote-acquisition
feature. The Phase A product boundary continues to require an operator-supplied,
exact local checkout and does not acquire provider sources itself.

## Exact selected source

| Field | Value |
| --- | --- |
| Local root | `C:/Users/samar/Documents/Codex/2026-07-15/do-not-weaken-trust-rules-and/work/baseline-inputs/gstack-official` |
| Git remote identity | `garrytan/gstack` (`https://github.com/garrytan/gstack.git`) |
| Resolved branch at source preparation | `main` |
| Resolved commit | `a3259400a366593e0c909dd9ac3e59752efd2488` |
| Source tree SHA-256 | `9e05fdf3ebe44f63d5b75ef4c5b9498840e8de2222e6c8f13539f6e584d36677` |
| Canonical repository expected by the adapter | `garrytan/gstack` |
| Canonical source match | yes |

The detached staging checkout was clean before and after qualification. This is the
first gstack result; no other repository or commit inherits its evidence.

## Inert discovery

The adapter read only the exact source tree, `package.json`, and fixed source paths.
It called `git -C <source-root> rev-parse --verify HEAD` before and after hashing the
tree. It did not import, invoke, preview, dry-run, set up, install, update, run a
daemon or browser, uninstall, or otherwise execute gstack code.

| Field | Value |
| --- | --- |
| Provider kind | `hybrid-host-setup` |
| Installer-contract fingerprint | `d28642fefe2d0728966c36ef53f991d26f073cd9c68b8659d6f2c6c81bc4031f` |
| Inert proposed-plan digest | `b4924ce105602dcbd25045a333ef6898c770f2bfc3d3dc45d94b31b727bc38b3` |
| Provider code executed | no |
| Write, process, service, network, updater, runtime, uninstall impacts | all `unknown` |

The observed source topology contains `agents`, `claude`, `codex`, `gstack`, and
`hosts`. Static setup-related entries are `bin/gstack-team-init`,
`bin/gstack-gbrain-install`, and `scripts/setup-scc.sh`; static uninstall-related
entries are `bin/gstack-uninstall` and `bin/gstack-brain-uninstall`. The static update
entry is `bin/gstack-update-check`. Browser-related directories are `browse` and
`open-gstack-browser`; static daemon-related entries are `bin/gstack-ios-qa-daemon`
and `design/src/daemon.ts`. These are source facts only. Their presence does not
prove an enabled updater, a destination, a process, a service, or runtime behavior.

The selected manifest declares Bun `>=1.0.0`; Bun was unavailable on this host, so no
Bun runtime claim entered the compatibility tuple. Static Codex and team-mode source
paths were observed, but all proposed project-native, profile-home, and
machine-exclusive destinations remain `unknown`.

## Exact compatibility outcome

The evaluated tuple used Codex `0.144.1`, build `cbacbb97`, host-contract version
`codex-0.144.1-windows-x64-v1` with `partial` coverage, Windows `10.0.26200` x64,
Node `24.13.1`, npm `11.8.0`, profile-home isolation, and policy
`enterprise-core-v1`. The adapter implementation hash was
`3f6d88b7925e0f9e56cc259be507955e3e0641095f056d2c545aa7d2d561b645` for this
evaluation.

No reviewed compatibility row matches that exact tuple. The result is:

```text
classification: QUALIFICATION_BLOCKED
support level: plannable
finding: ADAPTER_COMPATIBILITY_UNKNOWN
mutation research eligible: no
provider code executed: no
```

No existing methodology evidence joined. The partial Codex host contract also cannot
establish isolation or negative activation proof, and the inert source analysis cannot
establish updater state. Those facts are not relaxed merely because the source is
canonical.

## Stop condition

Canonical repository identity establishes only that this result uses the selected
official source. It does not establish compatibility, a Bun-capable runtime, isolation,
updater state, or a complete Codex load-surface contract. Do not add this tuple to the
supported compatibility set or begin Phase B. A later retry requires independently
reviewed compatibility, runtime, and isolation evidence; provider execution remains
out of scope until a separate Phase B authorization.
