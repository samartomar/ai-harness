node_modules/
dist/
coverage/
*.tsbuildinfo

# aih runtime artifacts
.aih-scratch/
.aih-workflow/

# logs / os
*.log
.DS_Store
Thumbs.db

# local env (the harness scans for these; never commit them)
.env
.env.*
!.env.example

# aih-managed (backup, temp, generated reports; keep committed recorder)

# internal (non-public) docs — clone samartomar/ai-harness-internal here for
# cross-agent access without exposing competitive/roadmap/handoff docs publicly
.internal/

# Claude Code session artifacts (agent worktrees, local settings)
.claude/

# aih-managed (backup, temp, and generated reports)
*.aih.bak
*.aih.tmp
!.aih/
.aih/*
!.aih/usage-record.mjs
